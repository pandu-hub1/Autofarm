-- made By Pandu-X
local Library = loadstring(game:HttpGetAsync("https://raw.githubusercontent.com/pandu-hub1/PanduUi/refs/heads/main/PanduUi"))()

local Window = Library:CreateWindow({
    Title = "Oxio Hub",
    Subtitle = "Violance District BETA",
    SubtitleColor = Color3.fromRGB(190, 140, 255),
    Logo = "rbxassetid://118755181202304",
    LogoSize = 32,
    SphereText = false,
    SphereWords = "ZX",
    SphereImage = "rbxassetid://118755181202304",
    SphereIconSize = 38,
})

local Players = game:GetService("Players")
local LP = Players.LocalPlayer
local player = LP
local LocalPlayer = LP
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local GuiService = game:GetService("GuiService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local CoreGui = game:GetService("CoreGui")
local PlayerGui = LP:WaitForChild("PlayerGui")
local Camera = workspace.CurrentCamera
local CollectionService = game:GetService("CollectionService")

_G.Ox = {}

-- ==================================================
-- VARIABLES
-- ==================================================
local TouchID = 8822
local ActionPath = "Survivor-mob.Controls.action.check"
_G.Ox.AntiBlindEnabled = false
_G.Ox.GotBlindedRemote = nil
_G.Ox.NoSlowdownEnabled = false
_G.Ox.BypassLeapEnabled = false
_G.Ox.InfAbyssalEnabled = false

local VALID_PARRY_IDS = {
    ["122812055447896"] = "Veil lunge",
    ["133963973694098"] = "Mayers Basic",
    ["117042998468241"] = "Mayers lunge",
    ["135002183282873"] = "cure lunge",
    ["121216847022485"] = "cure Basic",
    ["132817836308238"] = "Jeff Basic",
    ["129784271201071"] = "Jeff lunge",
    ["82666958311998"] = "Jeff Frenzy",
    ["78432063483146"] = "Abyssal Basic",
    ["118907603246885"] = "Abyssal lunge",
    ["139369275981139"] = "Jason Basic",
    ["110355011987939"] = "Jason lunge",
    ["111920872708571"] = "Masked Basic",
    ["105374834496520"] = "Masked lunge",
    ["138720291317243"] = "Masked Tony",
    ["106871536134254"] = "Masked Alex",
    ["130593238885843"] = "Masked Cobra",
    ["115244153053858"] = "Masked Cobra lunge",
    ["74968262036854"] = "Hidden Basic",
    ["113255068724446"] = "Hidden lunge",
    ["98163597193511"] = "Hidden S1",
    ["80411309607666"] = "Abyssal S1"
}

_G.Ox.ParryConfig = {
    AutoParry = false,
    ParrySafety = false,
    ParryAggressive = false,
    ParryCircle = true,
    ParryRadius = 15,
    ParryFace = 0.7,
    AutoCrouch = false,
    Ignored_Skills = {}
}

_G.Ox.AttachedParry = {}
if not _G.Ox.State then _G.Ox.State = {} end
_G.Ox.State.ParryCooldown = false
_G.Ox.State.ParryCooldownTime = 60
_G.Ox.State.AutoParryAdornment = nil
_G.Ox.State.ParryCooldownThread = nil

_G.Ox.AutoParryCustom = {
    Gui = nil,
    IsActive = false,
    GuiVisible = false,
}

function IsDowned(char)
    if not char then return false end
    return char:GetAttribute("Knocked") == true or char:GetAttribute("IsHooked") == true
end

function IsKiller(p)
    return p and p.Team and p.Team.Name == "Killer"
end

function TriggerCrouch()
    pcall(function()
        local b = LP:FindFirstChild("PlayerGui")
        for segment in string.gmatch("Survivor-mob.Controls.crouch.icon", "[^%.]+") do
            if b then b = b:FindFirstChild(segment) end
        end
        if b and b:IsA("GuiObject") and b.Visible and b.Parent and b.Parent:IsA("GuiButton") then
            local btn = b.Parent
            if UserInputService.TouchEnabled and type(firesignal) == "function" then
                firesignal(btn.MouseButton1Click)
                task.wait(1.4)
                firesignal(btn.MouseButton1Click)
            else
                VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.LeftControl, false, game)
                task.wait(1.4)
                VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.LeftControl, false, game)
            end
        else
            VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.LeftControl, false, game)
            task.wait(1.4)
            VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.LeftControl, false, game)
        end
    end)
end

function IsSafeToParry(char)
    if not _G.Ox.ParryConfig.ParrySafety then return true end
    if not char then return false end
    local interactObj = char:FindFirstChild("CheckInterractable")
    if interactObj then
        if interactObj:GetAttribute("isVaulting") == true then return false end
        if interactObj:GetAttribute("isRepairing") == true then return false end
        if interactObj:GetAttribute("isUnhooking") == true then return false end
        if interactObj:GetAttribute("isHealing") == true then return false end
        if interactObj:GetAttribute("isSliding") == true then return false end
    end
    return true 
end

function tapMobileParryButton()
    local playerGui = LP:FindFirstChild("PlayerGui")
    if not playerGui then return end
    local survivorMob = playerGui:FindFirstChild("Survivor-mob")
    local parryBtn = survivorMob and survivorMob:FindFirstChild("Controls") and survivorMob.Controls:FindFirstChild("Gui-mob")
    if parryBtn and parryBtn.Visible then
        if firesignal then
            pcall(function()
                firesignal(parryBtn.MouseButton1Down)
                task.wait(0.01)
                firesignal(parryBtn.MouseButton1Up)
            end)
        end
    else
        pcall(function()
            if mouse2click then mouse2click() return end
            if mouse2press and mouse2release then
                mouse2press()
                task.wait(0.01)
                mouse2release()
                return
            end
            VirtualInputManager:SendMouseButtonEvent(0, 0, 1, true, game, 0)
            task.wait(0.01)
            VirtualInputManager:SendMouseButtonEvent(0, 0, 1, false, game, 0)
        end)
    end
end

function ExecuteParry()
    if _G.Ox.State.ParryCooldown then return end
    pcall(function()
        local parryRemote = ReplicatedStorage:FindFirstChild("Remotes"):FindFirstChild("Items"):FindFirstChild("Parrying Dagger"):FindFirstChild("parry")
        if parryRemote then
            for i = 1, 10 do parryRemote:FireServer() end
        end
        task.spawn(tapMobileParryButton)
    end)
end

function ListenToParryResult()
    task.spawn(function()
        local remotes = ReplicatedStorage:WaitForChild("Remotes", 5)
        local dagger = remotes and remotes:WaitForChild("Items", 5):WaitForChild("Parrying Dagger", 5)
        local parryResultRemote = dagger and dagger:WaitForChild("parryResult", 5)
        if parryResultRemote then
            parryResultRemote.OnClientEvent:Connect(function(arg1, arg2)
                local cdDur = tonumber(arg2) or ((arg1 == true) and 90 or 60)
                _G.Ox.State.ParryCooldown = true
                if _G.Ox.State.ParryCooldownThread then task.cancel(_G.Ox.State.ParryCooldownThread) end
                _G.Ox.State.ParryCooldownThread = task.delay(cdDur, function()
                    _G.Ox.State.ParryCooldown = false
                end)
            end)
        end
    end)
end
ListenToParryResult()

function AttachParrySensor(kChar)
    if not kChar or _G.Ox.AttachedParry[kChar] then return end
    _G.Ox.AttachedParry[kChar] = true
    local humanoid = kChar:WaitForChild("Humanoid", 5)
    if not humanoid then return end
    local animator = humanoid:FindFirstChildOfClass("Animator") or humanoid:WaitForChild("Animator", 5)
    if not animator then return end

    humanoid.ChildAdded:Connect(function(child)
        if child:IsA("Animator") then
            _G.Ox.AttachedParry[kChar] = nil
            AttachParrySensor(kChar)
        end
    end)

    kChar.AncestryChanged:Connect(function(_, parent)
        if not parent then _G.Ox.AttachedParry[kChar] = nil end
    end)

    animator.AnimationPlayed:Connect(function(track)
        local animId = track.Animation and track.Animation.AnimationId or ""
        local id = animId:match("%d+")
        local attackName = VALID_PARRY_IDS[id]
        if not attackName then return end
        
        if id == "80411309607666" and _G.Ox.ParryConfig.AutoCrouch then
            local myChar = LP.Character
            if IsDowned(myChar) then return end
            local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
            local kHRP = kChar:FindFirstChild("HumanoidRootPart")
            if myHRP and kHRP then
                local dist = (myHRP.Position - kHRP.Position).Magnitude
                if dist <= 40 then TriggerCrouch() end
            end
            return 
        end
        
        if not _G.Ox.ParryConfig.AutoParry then return end
        if _G.Ox.State.ParryCooldown then return end 
        if _G.Ox.ParryConfig.Ignored_Skills and _G.Ox.ParryConfig.Ignored_Skills[attackName] then return end

        local myChar = LP.Character
        if IsDowned(myChar) or not IsSafeToParry(myChar) then return end
        local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
        local kHRP = kChar:FindFirstChild("HumanoidRootPart")
        if not myHRP or not kHRP then return end
        
        local delta = myHRP.Position - kHRP.Position
        local startDistance = delta.Magnitude

        if _G.Ox.ParryConfig.ParryAggressive then
            local aggressiveRadius = 12
            local detectionRadius = _G.Ox.ParryConfig.ParryRadius + 5
            if startDistance > detectionRadius then return end
            if startDistance <= aggressiveRadius then
                ExecuteParry()
            else
                local tracker, startTime = nil, os.clock()
                tracker = RunService.Heartbeat:Connect(function()
                    if os.clock() - startTime >= 1.5 or _G.Ox.State.ParryCooldown or not myHRP or not kHRP or IsDowned(myChar) then
                        if tracker then tracker:Disconnect() end
                        return
                    end
                    local currentDist = (myHRP.Position - kHRP.Position).Magnitude
                    if currentDist <= aggressiveRadius then
                        ExecuteParry()
                        if tracker then tracker:Disconnect() end
                    end
                end)
            end
        else
            if startDistance > _G.Ox.ParryConfig.ParryRadius then return end
            local myPosFlat = Vector3.new(myHRP.Position.X, 0, myHRP.Position.Z)
            local kPosFlat = Vector3.new(kHRP.Position.X, 0, kHRP.Position.Z)
            local flatDelta = myPosFlat - kPosFlat
            if flatDelta.Magnitude > 0 then
                local isFacing = Vector3.new(kHRP.CFrame.LookVector.X, 0, kHRP.CFrame.LookVector.Z).Unit:Dot(flatDelta.Unit)
                if isFacing < _G.Ox.ParryConfig.ParryFace then return end
            end
            ExecuteParry()
        end
    end)
end

function TryAttachParry(p)
    if p ~= LP and IsKiller(p) and p.Character then AttachParrySensor(p.Character) end
end

function SetupPlayerParry(p)
    if p == LP then return end
    p.CharacterAdded:Connect(function() TryAttachParry(p) end)
    p:GetPropertyChangedSignal("Team"):Connect(function() TryAttachParry(p) end)
    if p.Character then TryAttachParry(p) end
end

for _, p in ipairs(Players:GetPlayers()) do SetupPlayerParry(p) end
Players.PlayerAdded:Connect(SetupPlayerParry)

-- [ GUI PARRY CUSTOM ]
function UpdateCustomParryGUI(isOn)
    if not _G.Ox.AutoParryCustom.Gui then return end
    local frame = _G.Ox.AutoParryCustom.Gui:FindFirstChild("Frame")
    if not frame then return end
    local btn = frame:FindFirstChild("ActionButton")
    local stroke = frame:FindFirstChild("UIStroke")
    if isOn then
        if btn then btn.Text = "PARRY [ON]"; btn.TextColor3 = Color3.fromRGB(180, 255, 180); btn.BackgroundColor3 = Color3.fromRGB(40, 40, 45) end
        if stroke then stroke.Color = Color3.fromRGB(100, 200, 100) end
    else
        if btn then btn.Text = "PARRY [OFF]"; btn.TextColor3 = Color3.fromRGB(200, 200, 200); btn.BackgroundColor3 = Color3.fromRGB(30, 30, 35) end
        if stroke then stroke.Color = Color3.fromRGB(90, 90, 95) end
    end
end

function ToggleParryStatus()
    _G.Ox.ParryConfig.AutoParry = not _G.Ox.ParryConfig.AutoParry
    _G.Ox.AutoParryCustom.IsActive = _G.Ox.ParryConfig.AutoParry
    UpdateCustomParryGUI(_G.Ox.ParryConfig.AutoParry)
    Library:Notify({ Title = "Auto Parry", Description = _G.Ox.ParryConfig.AutoParry and "Aktif" or "Nonaktif", Duration = 2 })
end

function CreateCustomParryGUI()
    if _G.Ox.AutoParryCustom.Gui then _G.Ox.AutoParryCustom.Gui:Destroy() end
    local gui = Instance.new("ScreenGui")
    gui.Name = "AutoParryCustomGui"
    gui.ResetOnSpawn = false
    gui.IgnoreGuiInset = true
    gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    gui.Parent = CoreGui
    gui.Enabled = false
    _G.Ox.AutoParryCustom.Gui = gui
    _G.Ox.AutoParryCustom.GuiVisible = false

    local frame = Instance.new("Frame", gui)
    frame.Name = "Frame"
    frame.Size = UDim2.fromOffset(110, 36)
    frame.Position = UDim2.fromScale(0.85, 0.35)
    frame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    frame.BackgroundTransparency = 0.1
    frame.Active = true
    frame.BorderSizePixel = 0
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 8)

    local stroke = Instance.new("UIStroke", frame)
    stroke.Name = "UIStroke"
    stroke.Color = Color3.fromRGB(90, 90, 95)
    stroke.Thickness = 1.5
    stroke.Transparency = 0.2

    local button = Instance.new("TextButton", frame)
    button.Name = "ActionButton"
    button.Size = UDim2.new(1, 0, 1, 0)
    button.Text = "PARRY [OFF]"
    button.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
    button.Font = Enum.Font.GothamBold
    button.TextSize = 12
    button.TextColor3 = Color3.fromRGB(200, 200, 200)
    button.BorderSizePixel = 0
    button.AutoButtonColor = false
    Instance.new("UICorner", button).CornerRadius = UDim.new(0, 8)

    local dragging, dragMoved, canDrag = false, false, false
    local dragStart, startPos, holdThread = nil, nil, nil

    function update(input)
        if not dragging or not canDrag or not dragStart or not startPos then return end
        local delta = input.Position - dragStart
        if not dragMoved and (math.abs(delta.X) > 18 or math.abs(delta.Y) > 18) then dragMoved = true end
        if dragMoved then
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end

    button.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging, dragMoved, canDrag = true, false, false
            dragStart = input.Position
            startPos = frame.Position
            if holdThread then task.cancel(holdThread) end
            holdThread = task.delay(0.18, function() if dragging then canDrag = true end end)
        end
    end)
    button.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            if holdThread then task.cancel(holdThread) end
            if dragging and not dragMoved then ToggleParryStatus() end
            dragging, dragMoved, canDrag, dragStart, startPos = false, false, false, nil, nil
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and canDrag and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            update(input)
        end
    end)

    if _G.Ox.AutoParryCustom._statusConn then _G.Ox.AutoParryCustom._statusConn:Disconnect() end
    _G.Ox.AutoParryCustom._statusConn = RunService.Heartbeat:Connect(function()
        if not _G.Ox.AutoParryCustom.Gui or not _G.Ox.AutoParryCustom.Gui.Parent then
            if _G.Ox.AutoParryCustom._statusConn then _G.Ox.AutoParryCustom._statusConn:Disconnect() end return
        end
        if _G.Ox.AutoParryCustom.IsActive ~= _G.Ox.ParryConfig.AutoParry then
            _G.Ox.AutoParryCustom.IsActive = _G.Ox.ParryConfig.AutoParry
            UpdateCustomParryGUI(_G.Ox.ParryConfig.AutoParry)
        end
    end)
end

LP.CharacterAdded:Connect(function()
    task.wait(1)
    if _G.Ox.AutoParryCustom.GuiVisible and _G.Ox.AutoParryCustom.Gui then
        _G.Ox.AutoParryCustom.Gui.Enabled = true
        UpdateCustomParryGUI(_G.Ox.ParryConfig.AutoParry)
    end
end)

task.spawn(function()
    task.wait(1.5)
    CreateCustomParryGUI()
end)

_G.Ox.InstantTPGate = {
    Enabled = false,
    GateClientModule = nil,
    oldGateNew = nil,
    oldGateCanUse = nil,
}

_G.Ox.KorlessMorph = {
    Enabled = false,
    Connection = nil,
}

_G.Ox.SelfHeal = {
    Enabled = false,
}

_G.Ox.FlowstateNoCD = {
    Enabled = false,
    Connection = nil,
    CharAddedConn = nil,
}

_G.Ox.NoFall = {
    Enabled = false,
}

_G.Ox.BulletBlockConfig = {
    Enabled = false
}

_G.Ox.BypassGate = {
    Enabled = false,
}

_G.Ox.Config = _G.Ox.Config or {}
_G.Ox.Config.Surv_PerfectVault = false

_G.Ox.autoVaultEnabled = false
_G.Ox.autoPalletSlideEnabled = false
_G.Ox.lastActionTime = 0
_G.Ox.COOLDOWN = 1.5

_G.Ox.StreamerMode = {
    Enabled = false,
    SpooferConns = {},
}

_G.Ox.InfinityZoom = {
    Enabled = false,
}

_G.Ox.StretchRes = {
    Enabled = false,
    Amount = 0.67,
    Conn = nil,
}

_G.Ox.CameraFOV = {
    Enabled = false,
    Value = 90,
    Connection = nil,
}

_G.Ox.Auto = {
    SkillCheck = false,
    SkillCheckMode = "Legit",
}

_G.Ox.BypassGenEnabled = false
_G.Ox.BypassGenMode = "Manual Repair"
_G.Ox.RepairAnimTrack = nil
_G.Ox.ProcessedGens = {}
_G.Ox.AutoRepairEnabled = false
_G.Ox.AutoRepairThread = nil
_G.Ox.AutoCurrentPoint = nil
_G.Ox.AutoCurrentGenModel = nil
_G.Ox.LastFireTime = 0
_G.Ox.AutoFireInterval = 0.5
_G.Ox.ScriptAlive = true
_G.Ox.BypassToggleValue = false

_G.Ox.UnlimitedVaultEnabled = false

_G.Ox.State = {
    busy = false,
    RandomMode_IsNormal = false,
}

_G.Ox.Connections = {
    SkillHeartbeat = nil,
}

_G.Ox.Speed = {
    Enabled = false,
    Value = 0.1,
    Connection = nil,
    Loop = nil,
}

_G.Ox.AutoRun = {
    PC = false,
    Mobile = false,
    MobileThread = nil,
}

_G.Ox.Utility = {
    InvisEnabled = false,
    BandageEnabled = false,
    BandageSpeed = 50,
}

_G.Ox.ConfigData = {
    Surv_SkillFrequency = 10,
    Surv_SkillSpeed = 1
}

_G.Ox.Teleport = {
    GenIndex = 1,
    HookIndex = 1,
    GateIndex = 1,
    PalletIndex = 1,
    WindowIndex = 1,
}

local Cache = {
    Generators = {},
    Hooks = {},
    Gates = {},
    Pallets = {},
    Windows = {},
}

-- THIRD PERSON KILLER
local ThirdPerson = {
    Enabled = false,
    Conn = nil,
}

-- State untuk koneksi Third Person
if not _G.Ox.State then _G.Ox.State = {} end
_G.Ox.State.ThirdPersonConn = nil

-- Arm parts untuk transparency
local armParts = {
    ["Left Arm"] = true, 
    ["Right Arm"] = true,
    ["LeftHand"] = true, 
    ["RightHand"] = true,
    ["LeftLowerArm"] = true, 
    ["RightLowerArm"] = true,
    ["LeftUpperArm"] = true, 
    ["RightUpperArm"] = true
}

-- ==================================================
-- FAKE PERKS VARIABLES
-- ==================================================
_G.Ox.FakePerks = {
    QuickRecoveryEnabled = false,
    PerfectLandingEnabled = false,
    FlowstateEnabled = false,
    
    PerkCooldown = 10,
    
    boostActive = false,
    perfectLandingBoostActive = false,
    fsOnCooldown = false,
    
    lastQRTime = 0,
    lastPLTime = 0,
    lastFSTime = 0,
    
    qrConnection = nil,
    plConnection = nil,
    fsConnection = nil,
    fsAnimConnection = nil,
}

-- ==================================================
-- PERFORMANCE VARIABLES
-- ==================================================
_G.Ox.FullBright = false
_G.Ox.NoFog = false
_G.Ox.NoShadow = false
_G.Ox.TimeOfDayValue = 14

-- ===== SILENT VEIL (PANDU) =====
_G.Ox.AimConfig = {
    Aim_SilentVeilV2 = false,
    Veil_ShowFOV = true,
    SpearSmart_enable = false,
    Veil_FOV = 150,
    SPEAR_Speed = 165,
    SPEAR_Gravity = 103,
    SPEAR_AuraSpeed = 165,
    SPEAR_AuraGravity = 96.5,
    SPEAR_MaxDist = 500,
    Veil_LeadMultiplier = 1.4,
    AIM_TargetPart = "Torso",
    Pistol_BlockKnocked = false,
    LockAim = false,
    Flash_Silent = false,
    Flash_YOffset = 1.5,
}

_G.Ox.SurvivorFOVFrame = nil
_G.Ox.SurvivorFOVGui = nil
_G.Ox.isAimbotHolding = false
_G.Ox.lockedAimbotTarget = nil
_G.Ox.survivorAimbotHighlight = nil

_G.Ox.isKillerAimbotHolding = false
_G.Ox.lockedKillerAimbotTarget = nil
_G.Ox.killerAimbotHighlight = nil

_G.Ox.Config = {
    SpearSmart_enable = false,
    Surv_Aimbot_Enabled = false,
    Surv_Aimbot_ShowFOV = true,
    Surv_Aimbot_Radius = 150,
    Surv_Aimbot_MaxDist = 300,
    Surv_Aimbot_Smoothness = 0.5,
    Surv_Aimbot_Predict = 0.01,
    Killer_Aimbot_Enabled = false,
    Killer_Aimbot_MaxDist = 12,
    Killer_Aimbot_Smoothness = 0.5,
}

local isFiringSpear = false
local SpearInterceptorHooked = false
local VeilTrackerEnabled = false
local isAimingFlash = false

-- ==================================================
-- ESP FULL (SAMA PERSIS PANDU HUB)
-- ==================================================

-- ===== CONFIG ESP =====
_G.Ox.ESPConfig = {
    Master = false,
    Player = false,
    Killer = false,
    Outline = false,
    Name = false,
    Distance = false,
    KillerWarn = false,
    ItemIcon = false,
    Generator = false,
    GeneratorName = true,
    Pallet = false,
    Hook = false,
    Gate = false,
    Window = false,
    SCP = false,
    HookCount = false,
}

-- ===== COLORS =====
_G.Ox.ESPColors = {
    Player = Color3.fromRGB(57, 255, 20),
    Killer = Color3.fromRGB(255, 40, 40),
    Generator = Color3.fromRGB(255, 255, 0),
    GeneratorDone = Color3.fromRGB(0, 255, 0),
    Pallet = Color3.fromRGB(255, 128, 0),
    Hook = Color3.fromRGB(255, 105, 180),
    Gate = Color3.fromRGB(0, 255, 255),
    Window = Color3.fromRGB(0, 200, 255),
    SCP = Color3.fromRGB(255, 0, 255),
}

-- ===== CACHE =====
local CacheESP = {
    Generators = {},
    Windows = {},
    Pallets = {},
    Hooks = {},
    Gates = {},
    SCPs = {},
}

-- ==================================================
-- UTILITY FUNCTIONS
-- ==================================================
local function maskUserName(str)
    if not str or str == "" then return "***" end
    return string.sub(str, 1, 3) .. "***"
end

local function GetGreeting()
    local h = tonumber(os.date("%H"))
    if h < 11 then return "Good Morning"
    elseif h < 15 then return "Good Afternoon"
    elseif h < 18 then return "Good Evening"
    else return "Good Night" end
end

function GetRole()
    if not LocalPlayer.Team then return "Unknown" end
    local name = LocalPlayer.Team.Name
    if name == "Killer" then return "Killer"
    elseif name == "Survivors" then return "Survivor"
    else return "Spectator" end
end

function GetDistance(pos) 
    local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not root then return math.huge end
    return (pos - root.Position).Magnitude 
end

local function GetHealth()
    local char = LP.Character
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    return hum and math.floor(hum.Health) or 0
end

local function GetPlayers()
    return #Players:GetPlayers() .. "/" .. Players.MaxPlayers
end

local function GetPing()
    return math.floor(LP:GetNetworkPing() * 1000) .. "ms"
end

local function GetExecutor()
    if identifyexecutor then
        local name, ver = identifyexecutor()
        return tostring(name or "Unknown"), tostring(ver or "-")
    end
    return "Unknown", "-"
end

local function SafeSetToggle(toggleObj, value)
    if toggleObj and type(toggleObj.SetValue) == "function" then
        pcall(function()
            toggleObj:SetValue(value)
        end)
    end
end

-- ==================================================
-- TELEPORT FUNCTIONS
-- ==================================================
local lastScanTime = 0
local function ScanMap()
    if tick() - lastScanTime < 2 then return end
    lastScanTime = tick()

    table.clear(Cache.Generators)
    table.clear(Cache.Windows)
    table.clear(Cache.Pallets)
    table.clear(Cache.Hooks)
    table.clear(Cache.Gates)

    local mapFolder = workspace:FindFirstChild("Map") or workspace
    local descendants = mapFolder:GetDescendants()

    for i, v in ipairs(descendants) do
        if i % 200 == 0 then task.wait() end
        if v:IsA("Model") then
            local name = string.lower(v.Name)
            if name == "generator" then
                local p = v:FindFirstChildWhichIsA("BasePart")
                if p then table.insert(Cache.Generators, {model=v, part=p}) end
            elseif name == "hook" and v:FindFirstChild("HookPoint") then
                table.insert(Cache.Hooks, v)
            elseif name == "gate" and v:FindFirstChild("ExitLever") then
                table.insert(Cache.Gates, v)
            elseif name == "window" then
                table.insert(Cache.Windows, v)
            end
        elseif v:IsA("BasePart") and v.Name == "PrimaryPartPallet" then
            local modelUtamaPallet = v.Parent
            if modelUtamaPallet and not table.find(Cache.Pallets, modelUtamaPallet) then
                table.insert(Cache.Pallets, modelUtamaPallet)
            end
        end
    end
end

local function TeleportToPart(part)
    if not part then return end
    local char = LP.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if hrp then
        local offset = Vector3.new(0, 3, 0)
        if part:IsA("BasePart") then
            hrp.CFrame = part.CFrame + offset
        elseif part:IsA("Model") then
            local p = part:FindFirstChildWhichIsA("BasePart")
            if p then hrp.CFrame = p.CFrame + offset end
        end
        Library:Notify({Title = "Teleport", Description = "Berhasil!", Duration = 1})
    end
end

-- ==================================================
-- FULL BRIGHT FUNCTIONS (PANDU STYLE)
-- ==================================================
function ApplyFullBright()
    local Lighting = game:GetService("Lighting")
    if not Lighting then return end
    if _G.Ox.FullBright then
        Lighting.Brightness = 5
        Lighting.ClockTime = _G.Ox.TimeOfDayValue
        Lighting.FogEnd = 100000
        Lighting.Ambient = Color3.fromRGB(255, 255, 255)
        Lighting.OutdoorAmbient = Color3.fromRGB(255, 255, 255)
    end
end

function ApplyNoFog()
    local Lighting = game:GetService("Lighting")
    if not Lighting then return end
    if _G.Ox.NoFog then
        Lighting.FogStart = 0
        Lighting.FogEnd = 100000
        Lighting.FogColor = Color3.fromRGB(255, 255, 255)
        local atmosphere = Lighting:FindFirstChildOfClass("Atmosphere")
        if atmosphere then
            atmosphere.Density = 0
            atmosphere.Offset = 0
            atmosphere.Glare = 0
            atmosphere.Haze = 0
        end
    end
end

function ApplyNoShadow()
    local Lighting = game:GetService("Lighting")
    if not Lighting then return end
    Lighting.GlobalShadows = not _G.Ox.NoShadow
end

function ReapplyPerformance()
    task.wait(1)
    local Lighting = game:GetService("Lighting")
    if not Lighting then return end
    ApplyFullBright()
    ApplyNoFog()
    ApplyNoShadow()
end

-- ==================================================
-- AUTO SCAN MAP (SAMA PERSIS PANDU HUB)
-- ==================================================

function ScanMapESP()
    if tick() - lastScanTime < 2 then return end
    lastScanTime = tick()
    
    table.clear(CacheESP.Generators)
    table.clear(CacheESP.Windows)
    table.clear(CacheESP.Pallets)
    table.clear(CacheESP.Hooks)
    table.clear(CacheESP.Gates)
    table.clear(CacheESP.SCPs)
    
    local mapFolder = workspace:FindFirstChild("Map") or workspace
    local descendants = mapFolder:GetDescendants()
    
    for i, v in ipairs(descendants) do
        if i % 200 == 0 then task.wait() end
        if v:IsA("Model") then
            local name = string.lower(v.Name)
            if name == "generator" then 
                local p = v:FindFirstChildWhichIsA("BasePart")
                if p then table.insert(CacheESP.Generators, {model=v, part=p}) end
            elseif name == "hook" and v:FindFirstChild("HookPoint") then 
                table.insert(CacheESP.Hooks, v)
            elseif name == "gate" and v:FindFirstChild("ExitLever") then
                table.insert(CacheESP.Gates, v)
            elseif name == "window" then 
                table.insert(CacheESP.Windows, v)
            end
        elseif v:IsA("BasePart") and v.Name == "PrimaryPartPallet" then
            local modelUtamaPallet = v.Parent
            if modelUtamaPallet and not table.find(CacheESP.Pallets, modelUtamaPallet) then
                table.insert(CacheESP.Pallets, modelUtamaPallet)
            end
        end
    end

    local workspaceDescendants = workspace:GetDescendants()
    for i, v in ipairs(workspaceDescendants) do
        if i % 200 == 0 then task.wait() end
        if v:IsA("Model") and string.match(string.lower(v.Name), "^scp") then
            if not table.find(CacheESP.SCPs, v) then
                table.insert(CacheESP.SCPs, v)
            end
        end
    end
end

function ForceRefreshMap()
    ScanMapESP()
    
    if _G.Ox.ESPConfig.Master then
        ClearAllESP()
        task.wait(0.1)
        UpdatePlayerESP()
        UpdateSCPESP()
        UpdateStaticESP()
    end
end

-- ===== AUTO SCAN MAP - LAYER 1: SCAN PERTAMA =====
task.spawn(function()
    task.wait(2)
    ScanMapESP()
end)

-- ===== AUTO SCAN MAP - LAYER 2: SCAN SAAT RESPAWN =====
LocalPlayer.CharacterAdded:Connect(function()
    task.wait(1.5)
    ScanMapESP()
    
    if _G.Ox.ESPConfig.Master then
        task.spawn(function()
            task.wait(0.5)
            ClearAllESP()
            task.wait(0.2)
            UpdatePlayerESP()
            UpdateSCPESP()
            UpdateStaticESP()
        end)
    end
end)

-- ===== AUTO SCAN MAP - LAYER 3: SCAN SAAT MAP BERUBAH =====
workspace.ChildAdded:Connect(function(child)
    if child.Name == "Map" then
        task.wait(1)
        ScanMapESP()
    end
end)

-- ===== AUTO SCAN MAP - LAYER 4: SCAN PERIODIK =====
task.spawn(function()
    while _G.Ox.ScriptAlive do
        task.wait(40)
        if _G.Ox.ESPConfig.Master then
            ScanMapESP()
            if _G.Ox.ESPConfig.Master then
                pcall(UpdateStaticESP)
                pcall(UpdatePlayerESP)
                pcall(UpdateSCPESP)
            end
        end
    end
end)

-- ===== AUTO SCAN MAP - LAYER 5: PANTAU WORKSPACE CHANGES =====
local mapCheckConn = nil
local lastMapCheck = 0

mapCheckConn = RunService.Heartbeat:Connect(function()
    local now = tick()
    if now - lastMapCheck < 3 then return end
    lastMapCheck = now
    
    local map = workspace:FindFirstChild("Map")
    if map then
        local childCount = #map:GetChildren()
        if not getgenv()._lastMapChildCount then
            getgenv()._lastMapChildCount = childCount
        elseif getgenv()._lastMapChildCount ~= childCount then
            getgenv()._lastMapChildCount = childCount
            task.spawn(function()
                task.wait(0.5)
                ScanMapESP()
            end)
        end
    else
        getgenv()._lastMapChildCount = nil
    end
end)

pcall(function()
    game:BindToClose(function()
        if mapCheckConn then mapCheckConn:Disconnect() end
    end)
end)

-- ==================================================
-- AUTO RUN
-- ==================================================
local function GetMobileSprintButton()
    local pg = LP:FindFirstChild("PlayerGui")
    if not pg then return nil end
    local mob = pg:FindFirstChild("Survivor-mob")
    if not mob then return nil end
    local controls = mob:FindFirstChild("Controls")
    if not controls then return nil end
    local sprint = controls:FindFirstChild("sprint")
    if not sprint then return nil end

    if sprint:IsA("GuiButton") then return sprint end
    local icon = sprint:FindFirstChild("icon")
    if icon and icon:IsA("GuiButton") then return icon end
    if icon and icon.Parent and icon.Parent:IsA("GuiButton") then return icon.Parent end
    if sprint.Parent and sprint.Parent:IsA("GuiButton") then return sprint.Parent end
    return sprint
end

local function PressSprint()
    local btn = GetMobileSprintButton()
    if not btn then return false end
    pcall(function()
        if type(firesignal) == "function" then
            firesignal(btn.MouseButton1Click)
            firesignal(btn.MouseButton1Down)
            task.wait(0.04)
            firesignal(btn.MouseButton1Up)
        else
            local pos = btn.AbsolutePosition
            local size = btn.AbsoluteSize
            local inset = GuiService:GetGuiInset()
            local x = pos.X + size.X / 2 + inset.X
            local y = pos.Y + size.Y / 2 + inset.Y
            local id = 9901
            VirtualInputManager:SendTouchEvent(id, 0, x, y)
            task.wait(0.04)
            VirtualInputManager:SendTouchEvent(id, 2, x, y)
        end
    end)
    return true
end

local function IsMoving()
    local char = LP.Character
    if not char then return false end
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hum then return false end
    if hum.MoveDirection.Magnitude > 0.12 then return true end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if hrp then
        local v = hrp.AssemblyLinearVelocity
        if Vector3.new(v.X, 0, v.Z).Magnitude > 1.5 then return true end
    end
    return false
end

local function IsCrouching()
    local char = LP.Character
    if not char then return false end
    return char:GetAttribute("Crouching") == true or char:GetAttribute("Crouchingserver") == true
end

local function IsActuallySprinting()
    local char = LP.Character
    if not char then return false end
    return char:GetAttribute("Sprinting") == true or char:GetAttribute("IsRunning") == true
end

local function StartAutoRunMobile()
    if _G.Ox.AutoRun.MobileThread then return end
    _G.Ox.AutoRun.MobileThread = task.spawn(function()
        while _G.Ox.AutoRun.Mobile do
            local moving = IsMoving()
            local crouching = IsCrouching()
            local sprinting = IsActuallySprinting()
            if crouching then
                if sprinting then PressSprint() end
            else
                if moving and not sprinting then
                    PressSprint()
                elseif not moving and sprinting then
                    PressSprint()
                end
            end
            task.wait(0.12)
        end
        _G.Ox.AutoRun.MobileThread = nil
    end)
end

local function StopAutoRunMobile()
    _G.Ox.AutoRun.Mobile = false
    if _G.Ox.AutoRun.MobileThread then
        task.cancel(_G.Ox.AutoRun.MobileThread)
        _G.Ox.AutoRun.MobileThread = nil
    end
    if IsActuallySprinting() then PressSprint() end
end

-- ==================================================
-- AUTO VAULT & AUTO PALLET SLIDE
-- ==================================================

-- Fungsi untuk PC (Keyboard)
task.spawn(function()
    while _G.Ox.ScriptAlive do
        task.wait(0.1)
        if not _G.Ox.ScriptAlive then break end
        if UserInputService.TouchEnabled then continue end
        if tick() - _G.Ox.lastActionTime < _G.Ox.COOLDOWN then continue end

        local wantVault = _G.Ox.autoVaultEnabled
        local wantSlide = _G.Ox.autoPalletSlideEnabled
        if not wantVault and not wantSlide then continue end

        local pcPrompts = PlayerGui:FindFirstChild("pcprompts")
        local frame = pcPrompts and pcPrompts:FindFirstChild("Frame")
        if not frame then continue end

        local vaultGui = frame:FindFirstChild("VaultPromptGui")
        local slideGui = frame:FindFirstChild("PalletSlidePromptGui")

        local vaultVisible = vaultGui and vaultGui.Visible
        local slideVisible = slideGui and slideGui.Visible

        if (wantVault and vaultVisible) or (wantSlide and slideVisible) then
            VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.Space, false, game)
            task.wait(0.01)
            VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.Space, false, game)
            _G.Ox.lastActionTime = tick()
        end
    end
end)

-- Fungsi untuk Mobile (Touch)
local function setupMobileVault()
    if not UserInputService.TouchEnabled then return end

    local mobileGui = PlayerGui:FindFirstChild("Survivor-mob") or PlayerGui:WaitForChild("Survivor-mob", 8)
    if not mobileGui then return end

    local controls = mobileGui:FindFirstChild("Controls") or mobileGui:WaitForChild("Controls", 5)
    local action = controls and (controls:FindFirstChild("action") or controls:WaitForChild("action", 5))
    if not action then return end

    local allowedNames = {
        vault = true,
        palletvault = true,
        palletslide = true,
        slide = true,
    }

    local function connectLabel(child)
        if not (child:IsA("ImageLabel") or child:IsA("ImageButton")) then return end
        local name = child.Name:lower()
        if not allowedNames[name] then return end

        child:GetPropertyChangedSignal("Visible"):Connect(function()
            if not child.Visible then return end
            if tick() - _G.Ox.lastActionTime < _G.Ox.COOLDOWN then return end

            local isVault = name:find("vault") and not name:find("pallet")
            local isSlide = name:find("pallet") or name:find("slide")

            if (isVault and _G.Ox.autoVaultEnabled) or (isSlide and _G.Ox.autoPalletSlideEnabled) then
                pcall(function()
                    firesignal(action.MouseButton1Down)
                    task.wait(0.01)
                    firesignal(action.MouseButton1Up)
                end)
                _G.Ox.lastActionTime = tick()
            end
        end)
    end

    for _, child in pairs(action:GetChildren()) do
        connectLabel(child)
    end
    action.ChildAdded:Connect(function(child)
        task.wait()
        connectLabel(child)
    end)
end

-- Jalankan setup mobile
task.spawn(setupMobileVault)

-- Re-setup saat karakter baru
LP.CharacterAdded:Connect(function()
    task.wait(2)
    setupMobileVault()
end)

-- Re-setup saat UI Survivor-mob muncul
PlayerGui.ChildAdded:Connect(function(child)
    if child.Name == "Survivor-mob" then
        task.wait(1)
        setupMobileVault()
    end
end)

-- ==================================================
-- SPEED
-- ==================================================
local function ApplySpeedBoost()
    local char = LP.Character
    if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hum then return end
    if _G.Ox.Speed.Enabled then
        local moveDir = hum.MoveDirection
        if moveDir.Magnitude > 0 then
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hrp then
                hrp.CFrame = hrp.CFrame + (moveDir * _G.Ox.Speed.Value)
            end
        end
    end
end

-- ==================================================
-- AUTO SKILL CHECK
-- ==================================================
local function ks()
    task.spawn(function()
        local GlobalRemotes = ReplicatedStorage:WaitForChild("Remotes", 10)
        local killerPerks = GlobalRemotes and GlobalRemotes:WaitForChild("KillerPerks", 5)
        local kingscourge = killerPerks and killerPerks:WaitForChild("kingscourge", 5)
        local kingScourgeStart = kingscourge and kingscourge:WaitForChild("KingScourgeStart", 5)
        if kingScourgeStart then
            kingScourgeStart.OnClientEvent:Connect(function()
                if _G.Ox.Auto.SkillCheckMode == "Random" then
                    _G.Ox.State.RandomMode_IsNormal = true
                end
            end)
        end
    end)
end
ks()

local function GetActionTarget()
    local current = PlayerGui
    if not current then return nil end
    local mob = current:FindFirstChild("Survivor-mob")
    if not mob then return nil end
    local controls = mob:FindFirstChild("Controls")
    if not controls then return nil end
    return controls:FindFirstChild("action") or controls:FindFirstChild("check")
end

local function TriggerSkillCheck()
    local btn = GetActionTarget()
    if btn and btn:IsA("GuiObject") and btn.Visible then
        if type(firesignal) == "function" then
            pcall(function()
                firesignal(btn.MouseButton1Down)
                task.wait(0.005)
                firesignal(btn.MouseButton1Up)
            end)
            return true
        end
    end
    if btn and btn:IsA("GuiObject") and btn.Visible then
        pcall(function()
            local pos = btn.AbsolutePosition
            local size = btn.AbsoluteSize
            local inset = GuiService:GetGuiInset()
            local touchId = TouchID + math.random(1, 9999)
            VirtualInputManager:SendTouchEvent(touchId, 0, pos.X + size.X/2 + inset.X, pos.Y + size.Y/2 + inset.Y)
            task.wait(0.005)
            VirtualInputManager:SendTouchEvent(touchId, 2, pos.X + size.X/2 + inset.X, pos.Y + size.Y/2 + inset.Y)
        end)
        return true
    end
    pcall(function()
        VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.Space, false, game)
        task.wait(0.005)
        VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.Space, false, game)
    end)
    return true
end

local function startSkillCheck()
    if _G.Ox.Connections.SkillHeartbeat then
        _G.Ox.Connections.SkillHeartbeat:Disconnect()
        _G.Ox.Connections.SkillHeartbeat = nil
    end
    _G.Ox.Connections.SkillHeartbeat = RunService.Heartbeat:Connect(function()
        if not _G.Ox.Auto.SkillCheck or _G.Ox.State.busy then return end
        local prompt = PlayerGui:FindFirstChild("SkillCheckPromptGui")
        if not prompt then return end
        local check = prompt:FindFirstChild("Check")
        if not check or not check.Visible then
            _G.Ox.State.RandomMode_IsNormal = false
            return
        end
        local line = check:FindFirstChild("Line")
        local goal = check:FindFirstChild("Goal")
        if not line or not goal then return end
        local isInstant = _G.Ox.Auto.SkillCheckMode == "Instant" or (_G.Ox.Auto.SkillCheckMode == "Random" and not _G.Ox.State.RandomMode_IsNormal)
        if isInstant then
            line.Rotation = goal.Rotation + 109
            _G.Ox.State.busy = true
            task.spawn(function()
                TriggerSkillCheck()
                task.wait(0.05)
                _G.Ox.State.busy = false
            end)
        else
            local isNormal = _G.Ox.Auto.SkillCheckMode == "Normal" or (_G.Ox.Auto.SkillCheckMode == "Random" and _G.Ox.State.RandomMode_IsNormal)
            local lr = line.Rotation % 360
            local gr = goal.Rotation % 360
            local startRange, endRange
            if isNormal then
                startRange = (gr + 116) % 360
                endRange = (gr + 140) % 360
            else
                startRange = (gr + 102) % 360
                endRange = (gr + 116) % 360
            end
            local inZone = false
            if startRange > endRange then
                inZone = (lr >= startRange or lr <= endRange)
            else
                inZone = (lr >= startRange and lr <= endRange)
            end
            if inZone then
                _G.Ox.State.busy = true
                task.spawn(function()
                    TriggerSkillCheck()
                    task.wait(0.05)
                    _G.Ox.State.busy = false
                end)
            end
        end
    end)
end

-- ==================== BYPASS GENERATOR ====================
local RepairEvent = game:GetService("ReplicatedStorage").Remotes.Generator.RepairEvent
local GenCache = {}
local GenCacheTimer = 0
function GetAllGenerators()
    local now = tick()
    if now - GenCacheTimer < 5 then return GenCache end
    GenCache = {}
    GenCacheTimer = now
    local mapFolder = workspace:FindFirstChild("Map")
    if not mapFolder then return GenCache end
    pcall(function()
        for _, v in pairs(mapFolder:GetDescendants()) do
            if not v:IsA("Model") then continue end
            if v.Name ~= "Generator" then continue end
            local isRealGen = v:GetAttribute("RepairProgress") ~= nil
                or v:GetAttribute("kickcount") ~= nil
                or v:GetAttribute("ProgressRepair") ~= nil
            if not isRealGen then continue end
            table.insert(GenCache, v)
        end
    end)
    return GenCache
end

_G.Ox.PlayRepairAnim = function()
    local char = LocalPlayer.Character
    if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    local animator = hum and hum:FindFirstChildOfClass("Animator")
    if not animator then return end
    if _G.Ox.RepairAnimTrack and _G.Ox.RepairAnimTrack.IsPlaying then return end
    pcall(function()
        local anim = Instance.new("Animation")
        anim.AnimationId = "rbxassetid://92960319113695"
        _G.Ox.RepairAnimTrack = animator:LoadAnimation(anim)
        _G.Ox.RepairAnimTrack.Priority = Enum.AnimationPriority.Action
        _G.Ox.RepairAnimTrack:Play()
    end)
end

_G.Ox.StopRepairAnim = function()
    if _G.Ox.RepairAnimTrack and _G.Ox.RepairAnimTrack.IsPlaying then
        pcall(function() _G.Ox.RepairAnimTrack:Stop() end)
    end
    _G.Ox.RepairAnimTrack = nil
end

GetGeneratorPoints = function(genModel)
    local points = {}
    pcall(function()
        for _, obj in pairs(genModel:GetChildren()) do
            if obj.Name:find("GeneratorPoint") and obj:IsA("BasePart") then
                table.insert(points, obj)
            end
        end
    end)
    return points
end

_G.Ox.StopAutoRepair = function()
    _G.Ox.StopRepairAnim()
    local pointToStop = _G.Ox.AutoCurrentPoint
    _G.Ox.AutoCurrentPoint = nil
    if pointToStop then
        pcall(function() RepairEvent:FireServer(pointToStop, false) end)
    end
end

_G.Ox.StartAutoRepairLoop = function(genModel)
    _G.Ox.AutoCurrentGenModel = genModel

    if _G.Ox.AutoRepairThread then
        task.cancel(_G.Ox.AutoRepairThread)
        _G.Ox.AutoRepairThread = nil
    end
    _G.Ox.StopAutoRepair()

    _G.Ox.AutoRepairThread = task.spawn(function()
        while _G.Ox.AutoRepairEnabled and _G.Ox.BypassGenEnabled do
            local char = LocalPlayer.Character
            local hrp = char and char:FindFirstChild("HumanoidRootPart")
            if not hrp then task.wait(0.1) continue end

            local foundPoint = nil
            for _, gen in pairs(GetAllGenerators()) do
                for _, point in pairs(GetGeneratorPoints(gen)) do
                    if (hrp.Position - point.Position).Magnitude <= 5 then
                        foundPoint = point
                        break
                    end
                end
                if foundPoint then break end
            end

            if foundPoint then
                _G.Ox.AutoCurrentPoint = foundPoint
                local now = tick()
                if now - _G.Ox.LastFireTime >= _G.Ox.AutoFireInterval then
                    _G.Ox.PlayRepairAnim()
                    pcall(function() RepairEvent:FireServer(foundPoint, true) end)
                    _G.Ox.LastFireTime = now
                end
            else
                if _G.Ox.AutoCurrentPoint then
                    _G.Ox.StopAutoRepair()
                    _G.Ox.AutoRepairEnabled = false

                    if _G.Ox.AutoCurrentGenModel then
                        _G.Ox.ProcessedGens[_G.Ox.AutoCurrentGenModel] = nil
                        _G.Ox.AutoCurrentGenModel = nil                    
                    end
                    break
                end
            end

            task.wait(0.1)
        end
        _G.Ox.StopAutoRepair()
    end)
end

waitForRepairing = function(point, timeout)
    local start = tick()
    while tick() - start < (timeout or 1) do
        if point:GetAttribute("IsRepairing") == true then
            return true
        end
        task.wait(0.05)
    end
    return false
end

function RecreateBypassButton()
    local oldUI = CoreGui:FindFirstChild("BypassGenUI")
    if oldUI then oldUI:Destroy() end

    _G.Ox.BypassUI = Instance.new("ScreenGui")
    _G.Ox.BypassUI.Name = "BypassGenUI"
    _G.Ox.BypassUI.ResetOnSpawn = false
    _G.Ox.BypassUI.IgnoreGuiInset = true
    _G.Ox.BypassUI.Parent = CoreGui

    _G.Ox.BypassButton = Instance.new("ImageButton")
    _G.Ox.BypassButton.Name = "BypassGenButton"
    _G.Ox.BypassButton.Size = UDim2.new(0, 55, 0, 55)
    _G.Ox.BypassButton.Position = UDim2.new(0.88, 0, 0.55, 0)
    _G.Ox.BypassButton.AnchorPoint = Vector2.new(0.5, 0.5)
    _G.Ox.BypassButton.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    _G.Ox.BypassButton.BackgroundTransparency = 0.15
    _G.Ox.BypassButton.AutoButtonColor = true
    _G.Ox.BypassButton.Visible = false
    _G.Ox.BypassButton.ZIndex = 10
    _G.Ox.BypassButton.Parent = _G.Ox.BypassUI

    local UICorner = Instance.new("UICorner")
    UICorner.CornerRadius = UDim.new(1, 0)
    UICorner.Parent = _G.Ox.BypassButton

    local UIStroke = Instance.new("UIStroke")
    UIStroke.Color = Color3.fromRGB(255, 255, 255)
    UIStroke.Thickness = 1.5
    UIStroke.Transparency = 0.4
    UIStroke.Parent = _G.Ox.BypassButton

    local BypassLabel = Instance.new("TextLabel")
    BypassLabel.Size = UDim2.new(1, 0, 1, 0)
    BypassLabel.BackgroundTransparency = 1
    BypassLabel.Text = "GEN"
    BypassLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    BypassLabel.TextScaled = true
    BypassLabel.Font = Enum.Font.GothamBold
    BypassLabel.ZIndex = 11
    BypassLabel.Parent = _G.Ox.BypassButton

    _G.Ox.BypassButton.Visible = _G.Ox.BypassToggleValue and UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled
end
RecreateBypassButton()

_G.Ox.DoMultiRepairPlain = function(targetPoint)
    local genModel = targetPoint.Parent
    if _G.Ox.ProcessedGens[genModel] then return end

    _G.Ox.ProcessedGens[genModel] = true

    local allPoints = GetGeneratorPoints(genModel)
    local character = LocalPlayer.Character
    local hrp = character and character:FindFirstChild("HumanoidRootPart")
    if not hrp then _G.Ox.ProcessedGens[genModel] = nil return end

    local originalCFrame = hrp.CFrame
    local ok = pcall(function()
        for _, point in pairs(allPoints) do
            if point ~= targetPoint and point.Parent then
                hrp.Anchored = true
                hrp.CFrame = point.CFrame
                task.wait(0.15)

                pcall(function() RepairEvent:FireServer(point, true) end)

                if not waitForRepairing(point, 0.8) then
                    pcall(function() RepairEvent:FireServer(point, false) end)
                    task.wait(0.1)
                    hrp.CFrame = point.CFrame
                    task.wait(0.15)
                    pcall(function() RepairEvent:FireServer(point, true) end)
                    waitForRepairing(point, 0.5)
                end

                hrp.Anchored = false
                task.wait(0.05)
            end
        end
    end)

    pcall(function()
        if hrp and hrp.Parent then
            hrp.Anchored = false
            hrp.CFrame = originalCFrame
        end
    end)

    task.wait(0.1)
    if _G.Ox.BypassGenMode == "Manual Repair" then
        pcall(function() RepairEvent:FireServer(targetPoint, false) end)
    elseif _G.Ox.BypassGenMode == "Auto Repair" then
        _G.Ox.AutoRepairEnabled = true
        _G.Ox.StartAutoRepairLoop(genModel)
    end
end

_G.Ox.BypassButton.MouseButton1Click:Connect(function()
    if not _G.Ox.BypassGenEnabled then return end

    local character = LocalPlayer.Character
    local hrp = character and character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local bestPoint, bestDist = nil, math.huge
    for _, gen in pairs(GetAllGenerators()) do
        for _, point in pairs(GetGeneratorPoints(gen)) do
            local d = (hrp.Position - point.Position).Magnitude
            if d < bestDist then
                bestDist = d
                bestPoint = point
            end
        end
    end

    if bestPoint and bestDist <= 8 then
        _G.Ox.DoMultiRepairPlain(bestPoint)
    else
        Library:Notify({ Title = "Gen Boost", Description = "Ga deket generator manapun!", Duration = 2 })
    end
end)

isGeneratorPromptVisible = function()
    local ok, frame = pcall(function()
        return LocalPlayer.PlayerGui.pcprompts.Frame.GeneratorRepair
    end)
    return ok and frame and frame.Visible
end

getNearestGenPoint = function()
    local character = LocalPlayer.Character
    local hrp = character and character:FindFirstChild("HumanoidRootPart")
    if not hrp then return nil end

    local bestPoint, bestDist = nil, math.huge
    for _, gen in pairs(GetAllGenerators()) do
        for _, point in pairs(GetGeneratorPoints(gen)) do
            local d = (hrp.Position - point.Position).Magnitude
            if d < bestDist then
                bestDist = d
                bestPoint = point
            end
        end
    end
    return bestPoint, bestDist
end

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.UserInputType ~= Enum.UserInputType.MouseButton1 then return end
    if not _G.Ox.BypassGenEnabled then return end
    if not isGeneratorPromptVisible() then return end

    local bestPoint, bestDist = getNearestGenPoint()
    if not bestPoint or bestDist > 8 then return end

    local genModel = bestPoint.Parent

    if _G.Ox.ProcessedGens[genModel] then
        return
    end

    _G.Ox.DoMultiRepairPlain(bestPoint)
end)

startProcessedGensWatcher = function()
    task.spawn(function()
        while _G.Ox.ScriptAlive do
            task.wait(2)
            if not _G.Ox.ScriptAlive then break end
            local character = LocalPlayer.Character
            local hrp = character and character:FindFirstChild("HumanoidRootPart")
            if not hrp then continue end

            for genModel in pairs(_G.Ox.ProcessedGens) do
                if not genModel or not genModel.Parent then
                    _G.Ox.ProcessedGens[genModel] = nil
                    continue
                end

                local points = GetGeneratorPoints(genModel)
                local nearAny = false
                for _, point in pairs(points) do
                    if point.Parent and (hrp.Position - point.Position).Magnitude <= 10 then
                        nearAny = true
                        break
                    end
                end

                if not nearAny then
                    _G.Ox.ProcessedGens[genModel] = nil
                end
            end
        end
    end)
end

startProcessedGensWatcher()

-- ==================================================
-- INVIS + BANDAGE
-- ==================================================
local INVIS_URL = "https://raw.githubusercontent.com/pandu-hub1/Autofarm/refs/heads/main/Invis.lua"
local InvisAPI = nil
local InvisLoaded = false
local isMobile = UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled

local function LoadInvis()
    if InvisLoaded then return true end
    local success, result = pcall(function()
        return loadstring(game:HttpGet(INVIS_URL))()
    end)
    if success and result then
        InvisAPI = result
        InvisLoaded = true
        return true
    end
    return false
end

local function ToggleInvisibility(state)
    if state then
        if not InvisLoaded then LoadInvis() end
        if InvisAPI then
            if isMobile then InvisAPI.ShowGUI() else InvisAPI.Enable() end
        end
    else
        if InvisAPI then
            if isMobile then InvisAPI.HideGUI() end
            InvisAPI.Disable()
        end
    end
end

local BANDAGE_URL = "https://raw.githubusercontent.com/pandu-hub1/Autofarm/refs/heads/main/Boostbandage.lua"
local FastBandageAPI = nil
local FastBandageLoaded = false

local function LoadFastBandage()
    if FastBandageLoaded then return true end
    local success, result = pcall(function()
        return loadstring(game:HttpGet(BANDAGE_URL))()
    end)
    if success and result then
        FastBandageAPI = result
        FastBandageLoaded = true
        return true
    end
    return false
end

local function ToggleFastBandage(state)
    if state then
        if not FastBandageLoaded then LoadFastBandage() end
        if FastBandageAPI then
            if isMobile then
                FastBandageAPI.ShowGUI()
            else
                local ok, err = FastBandageAPI.Start()
                if not ok then
                    _G.Ox.Utility.BandageEnabled = false
                end
            end
        end
    else
        if FastBandageAPI then
            if isMobile then FastBandageAPI.HideGUI() end
            FastBandageAPI.Stop()
        end
    end
end

-- ==================================================
-- DASH LOCK (AIM LOCK HIDDEN)
-- ==================================================
local DASH_LOCK_URL = "https://pastefy.app/Pu94U00g/raw"
local DashLockAPI = nil
local DashLockLoaded = false

-- ==================================================
-- DASH LOCK FUNCTIONS (SAMA PERSIS INVIs & FAST BANDAGE)
-- ==================================================
local function LoadDashLock()
    if DashLockLoaded then return true end
    local success, result = pcall(function()
        return loadstring(game:HttpGet(DASH_LOCK_URL))()
    end)
    if success and result then
        DashLockAPI = result
        DashLockLoaded = true
        return true
    end
    return false
end

local function ToggleDashLock(state)
    if state then
        if not DashLockLoaded then LoadDashLock() end
        if DashLockAPI then
            if isMobile then
                DashLockAPI.ShowGUI()
            else
                DashLockAPI.Enable()
            end
            Library:Notify({ Title = "Dash Lock", Description = "AKTIF - Klik kanan untuk lock", Duration = 3 })
        end
    else
        if DashLockAPI then
            if isMobile then DashLockAPI.HideGUI() end
            DashLockAPI.Disable()
            Library:Notify({ Title = "Dash Lock", Description = "NONAKTIF", Duration = 2 })
        end
    end
end

-- Auto Load (SAMA PERSIS)
task.spawn(function()
    task.wait(2)
    LoadDashLock()
end)

-- MASKED GUI SELECTION (PASTEFY)
local MaskedGUI = nil
local MaskedGUILoaded = false
local MASKED_GUI_URL = "https://pastefy.app/LNwYB2Uy/raw"
-- ==================================================
-- MASKED GUI SELECTION (PASTEFY)
-- ==================================================

local function LoadMaskedGUI()
    if MaskedGUILoaded then return true end
    local success, result = pcall(function()
        return loadstring(game:HttpGet(MASKED_GUI_URL))()
    end)
    if success and result then
        MaskedGUI = result
        MaskedGUILoaded = true
        return true
    end
    return false
end

local function ToggleMaskedGUI(state)
    if state then
        if not LoadMaskedGUI() then
            Library:Notify({ Title = "Mask GUI", Description = "Gagal load script!", Duration = 3 })
            return
        end
        local ok, msg = MaskedGUI.Toggle(true)
        if ok then
            Library:Notify({ Title = "Mask GUI", Description = msg or "Dimuat! (Tombol M)", Duration = 3 })
        else
            Library:Notify({ Title = "Mask GUI", Description = msg or "Kamu bukan The Masked!", Duration = 3 })
        end
    else
        if MaskedGUILoaded and MaskedGUI then
            MaskedGUI.Toggle(false)
            Library:Notify({ Title = "Mask GUI", Description = "Disembunyikan", Duration = 2 })
        end
    end
end

-- Auto load saat script jalan
task.spawn(function()
    task.wait(2)
    LoadMaskedGUI()
end)

-- ==================================================
-- SILENT VEIL — LOGIC PANDU PERSIS
-- ==================================================
function getTargetPartObject(char)
    if _G.Ox.AimConfig.AIM_TargetPart == "Head" then
        return char:FindFirstChild("Head")
    elseif _G.Ox.AimConfig.AIM_TargetPart == "Root" or _G.Ox.AimConfig.AIM_TargetPart == "HumanoidRootPart" then
        return char:FindFirstChild("HumanoidRootPart")
    else
        return char:FindFirstChild("Torso") or char:FindFirstChild("UpperTorso") or char:FindFirstChild("HumanoidRootPart")
    end
end

function getClosestSurvivor()
    local myChar = player.Character
    local myRoot = myChar and myChar:FindFirstChild("HumanoidRootPart")
    if not myRoot then return nil end
    local closestFovDist = _G.Ox.AimConfig.Veil_FOV
    local closestTarget = nil
    local cam = workspace.CurrentCamera
    local centerScreen = Vector2.new(cam.ViewportSize.X / 2, cam.ViewportSize.Y / 2)

    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= player and p.Team and p.Team.Name == "Survivors" and p.Character then
            local char = p.Character
            local hum = char:FindFirstChildOfClass("Humanoid")
            local targetPart = getTargetPartObject(char)
            if hum and hum.Health > 0 and targetPart then
                local dist3D = (targetPart.Position - myRoot.Position).Magnitude
                if dist3D <= _G.Ox.AimConfig.SPEAR_MaxDist then
                    local screenPos, onScreen = cam:WorldToViewportPoint(targetPart.Position)
                    if onScreen then
                        local targetPos2D = Vector2.new(screenPos.X, screenPos.Y)
                        local dist2D = (targetPos2D - centerScreen).Magnitude
                        if dist2D <= closestFovDist then
                            closestFovDist = dist2D
                            closestTarget = targetPart
                        end
                    end
                end
            end
        end
    end
    return closestTarget
end

-- ===== SURVIVOR AIMBOT FUNCTIONS =====
function getBestAimbotTarget()
    local bestTarget = nil
    local myRole = GetRole()
    
    local shortestFovDist = _G.Ox.Config.Surv_Aimbot_Radius
    local shortest3DDist = math.huge
    local centerScreen = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
            local isValidTarget = false
            
            if myRole == "Survivor" and _G.Ox.Config.Surv_Aimbot_Enabled and IsKiller(p) then
                isValidTarget = true
            elseif myRole == "Killer" and _G.Ox.Config.Killer_Aimbot_Enabled and not IsKiller(p) then
                local hum = p.Character:FindFirstChild("Humanoid")
                if hum and hum.Health > 0 and not IsDowned(p.Character) then
                    isValidTarget = true
                end
            end
            
            if isValidTarget then 
                local hrp = p.Character.HumanoidRootPart
                local dist3D = GetDistance(hrp.Position)
                
                if myRole == "Survivor" and dist3D <= _G.Ox.Config.Surv_Aimbot_MaxDist then
                    local screenPos, onScreen = Camera:WorldToViewportPoint(hrp.Position)
                    if onScreen then 
                        local targetPos2D = Vector2.new(screenPos.X, screenPos.Y)
                        local dist2D = (targetPos2D - centerScreen).Magnitude
                        if dist2D < shortestFovDist then 
                            shortestFovDist = dist2D
                            bestTarget = hrp 
                        end 
                    end
                    
                elseif myRole == "Killer" and dist3D <= _G.Ox.Config.Killer_Aimbot_MaxDist then
                    if dist3D < shortest3DDist then 
                        shortest3DDist = dist3D
                        bestTarget = hrp
                    end
                end
            end
        end
    end
    return bestTarget
end

function getBestKillerAimbotTarget()
    local bestTarget = nil
    local shortestDist = math.huge
    local myChar = LocalPlayer.Character
    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
    
    if not myHRP then return nil end
    
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Team and p.Team.Name == "Survivors" and p.Character then
            local hum = p.Character:FindFirstChildOfClass("Humanoid")
            local hrp = p.Character:FindFirstChild("HumanoidRootPart")
            
            if hum and hum.Health > 0 and hrp and not IsDowned(p.Character) then
                local dist = (hrp.Position - myHRP.Position).Magnitude
                if dist < shortestDist and dist <= _G.Ox.Config.Killer_Aimbot_MaxDist then
                    shortestDist = dist
                    bestTarget = hrp
                end
            end
        end
    end
    return bestTarget
end

function getKillerTargetForFlash()
    local bestTarget = nil
    local closestDist = math.huge
    local myChar = LocalPlayer.Character
    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
    
    if not myHRP then return nil end

    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Team and p.Team.Name == "Killer" and p.Character then
            local hum = p.Character:FindFirstChildOfClass("Humanoid")
            local hrp = p.Character:FindFirstChild("HumanoidRootPart")

            if hum and hum.Health > 0 and hrp and not IsDowned(p.Character) then
                local dist = (hrp.Position - myHRP.Position).Magnitude
                if dist < closestDist then
                    closestDist = dist
                    bestTarget = hrp 
                end
            end
        end
    end
    
    return bestTarget
end

function setupSpearInterceptor()
    if SpearInterceptorHooked then return end
    if not getrawmetatable or not setreadonly then return end

    local Spearthrow = nil
    pcall(function()
        Spearthrow = ReplicatedStorage.Remotes.Killers.Veil.Spearthrow
    end)

    local function solvePitch(v0, g, d, dy)
        d = math.max(d, 0.1)
        local s2 = v0 * v0
        local root = s2 * s2 - g * (g * d * d + 2 * dy * s2)
        if root < 0 then root = 0 end
        local tanTheta = (s2 - math.sqrt(root)) / (g * d)
        local theta = math.atan(tanTheta)
        local t = d / (v0 * math.cos(theta))
        return theta, t
    end

    local mt = getrawmetatable(game)
    setreadonly(mt, false)
    local oldNamecall = mt.__namecall

    mt.__namecall = newcclosure(function(self, ...)
        local method = getnamecallmethod()

        if method == "FireServer"
            and not checkcaller()
            and typeof(self) == "Instance"
            and self.ClassName == "RemoteEvent"
            and self.Name == "Spearthrow"
            and _G.Ox.AimConfig.Aim_SilentVeilV2
            and not isFiringSpear
        then
            local lookVec, speed, originPos = ...

            local myChar = LocalPlayer.Character
            local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
            local startPart = myChar and (myChar:FindFirstChild("Head") or myHRP)
            local isSpecial = myChar and myChar:GetAttribute("special") == true

            local v0, g
            if isSpecial then
                v0 = _G.Ox.AimConfig.SPEAR_AuraSpeed or 165
                g  = _G.Ox.AimConfig.SPEAR_AuraGravity or 96.5
            else
                v0 = _G.Ox.AimConfig.SPEAR_Speed or 165
                g  = _G.Ox.AimConfig.SPEAR_Gravity or 103
            end
            speed = v0

            originPos = originPos
                or (_G.Ox.Config.SpearSmart_enable and myHRP and myHRP.Position)
                or (startPart and startPart.Position)

            local bestDir = lookVec
            local targetPart = getClosestSurvivor()

            if targetPart and originPos then
                local targetHRP = targetPart:IsA("Model") and targetPart:FindFirstChild("HumanoidRootPart") or targetPart
                local targetPos = targetHRP.Position
                local targetVel = Vector3.new(0, 0, 0)

                local targetHum = targetPart.Parent and targetPart.Parent:FindFirstChildOfClass("Humanoid")
                if targetHum and targetHum.MoveDirection.Magnitude > 0 then
                    targetVel = targetHum.MoveDirection * targetHum.WalkSpeed
                elseif targetHRP:IsA("BasePart") then
                    targetVel = targetHRP.AssemblyLinearVelocity
                end
                targetVel = Vector3.new(targetVel.X, 0, targetVel.Z)

                local dir = targetPos - originPos
                local dist = dir.Magnitude
                local hDist = Vector3.new(dir.X, 0, dir.Z).Magnitude
                local dy = dir.Y

                if dist > 0.1 and dist <= (_G.Ox.AimConfig.SPEAR_MaxDist or 1000) then
                    local leadMul = _G.Ox.AimConfig.Veil_LeadMultiplier or 1.4
                    local _, tFlight = solvePitch(v0, g, hDist, dy)

                    if _G.Ox.Config.SpearSmart_enable and targetVel.Magnitude > 0.5 then
                        local lead = targetVel * (tFlight * leadMul)
                        local maxLead = math.clamp(dist * 0.6, 3, 45)
                        if lead.Magnitude > maxLead then
                            lead = lead.Unit * maxLead
                        end
                        local aimPoint = targetPos + lead
                        local ad = aimPoint - originPos
                        local ah = Vector3.new(ad.X, 0, ad.Z).Magnitude
                        local pitch = select(1, solvePitch(v0, g, math.max(ah, 0.1), ad.Y))
                        if ah > 0.001 then
                            bestDir = Vector3.new(ad.X, 0, ad.Z).Unit * math.cos(pitch) + Vector3.new(0, math.sin(pitch), 0)
                        else
                            bestDir = ad.Unit
                        end
                    else
                        local pitch = select(1, solvePitch(v0, g, math.max(hDist, 0.1), dy))
                        if hDist > 0.001 then
                            bestDir = Vector3.new(dir.X, 0, dir.Z).Unit * math.cos(pitch) + Vector3.new(0, math.sin(pitch), 0)
                        else
                            bestDir = dir.Unit
                        end
                    end
                end
            end

            isFiringSpear = true
            pcall(function()
                if Spearthrow then
                    Spearthrow:FireServer(bestDir, speed, originPos)
                else
                    self:FireServer(bestDir, speed, originPos)
                end
            end)
            isFiringSpear = false
            return
        end

        return oldNamecall(self, ...)
    end)

    setreadonly(mt, true)
    SpearInterceptorHooked = true
end

-- ==================================================
-- SETUP ANTI BLIND (TERPISAH DARI SILENT VEIL)
-- ==================================================
local function InitAntiBlind()
    local flashlightFolder = ReplicatedStorage:FindFirstChild("Remotes"):FindFirstChild("Items"):FindFirstChild("Flashlight")
    if flashlightFolder then
        _G.Ox.GotBlindedRemote = flashlightFolder:FindFirstChild("GotBlinded")
    end
end

local function SetupAntiBlind()
    if not getrawmetatable or not setreadonly then
        warn("[AntiBlind] Executor tidak support getrawmetatable!")
        return
    end

    InitAntiBlind()

    local mt = getrawmetatable(game)
    local oldNamecall = mt.__namecall

    setreadonly(mt, false)
    mt.__namecall = newcclosure(function(self, ...)
        local method = getnamecallmethod()

        -- ===== ANTI BLIND =====
        if not checkcaller() and (method == "FireServer" or method == "InvokeServer") then
            if _G.Ox.AntiBlindEnabled and _G.Ox.GotBlindedRemote and self == _G.Ox.GotBlindedRemote then
                local isKiller = (LP.Team and LP.Team.Name == "Killer")
                if isKiller then
                    return nil -- Blokir event blind
                end
            end
        end

        -- ===== BLOCK PELURU (TWIST OF FATE) =====
        if not checkcaller() and (method == "FireServer" or method == "InvokeServer") then
            if _G.Ox.BulletBlockConfig.Enabled and self and self.Name == "Fire" then
                local parent = self.Parent
                if parent and parent.Name == "Twist of Fate" then
                    return nil -- Blokir peluru
                end
            end
        end

        return oldNamecall(self, ...)
    end)
    setreadonly(mt, true)
end

setupSpearInterceptor()

InitAntiBlind()
SetupAntiBlind()

-- Auto re-init saat karakter baru
LP.CharacterAdded:Connect(function()
    task.wait(1)
    InitAntiBlind()
end)

-- ==================================================
-- SKILL HIDDEN NO CD (BYPASS LEAP COOLDOWN)
-- ==================================================
local Connections = Connections or {}
local function StartLeapBypass()
    if Connections.LeapBypass then
        task.cancel(Connections.LeapBypass)
        Connections.LeapBypass = nil
    end
    
    Connections.LeapBypass = task.spawn(function()
        local leapFunction, m2Function
        for _, v in pairs(getgc(true)) do
            if type(v) == "function" and islclosure(v) then
                local info = debug.getinfo(v)
                if info.name == "tryActivate" then leapFunction = v end
                if info.name == "playM2Animation" then m2Function = v end
                if leapFunction and m2Function then break end
            end
        end
        if not leapFunction and not m2Function then
            warn("[BypassLeap] Function tidak ditemukan.")
            return
        end
        while task.wait(0.1) do
            if not _G.Ox.BypassLeapEnabled then break end
            for _, fn in pairs({leapFunction, m2Function}) do
                if fn then
                    for i, val in pairs(debug.getupvalues(fn)) do
                        if type(val) == "boolean" and val == true then
                            debug.setupvalue(fn, i, false)
                        end
                    end
                end
            end
        end
    end)
end

-- ==================================================
-- INFINITE CORRUPT (ABYSSAL)
-- ==================================================
local isHoldingAbyssal = false

local function FireAbyssalSkill()
    if GetRole() ~= "Killer" then return end
    
    local selectedKillerObj = LP:FindFirstChild("SelectedKiller")
    local killerName = selectedKillerObj and selectedKillerObj.Value or LP:GetAttribute("SelectedKiller")
    
    if killerName and string.find(string.lower(tostring(killerName)), "abyss") then
        pcall(function()
            local corruptEvent = ReplicatedStorage:FindFirstChild("Remotes"):FindFirstChild("Killers"):FindFirstChild("Abysswalker"):FindFirstChild("corrupt")
            if corruptEvent then
                corruptEvent:FireServer()
            end
        end)
    end
end

local function StartInfiniteAbyssal()
    if isHoldingAbyssal then return end
    isHoldingAbyssal = true
    
    task.spawn(function()
        while isHoldingAbyssal and _G.Ox.InfAbyssalEnabled and GetRole() == "Killer" do
            FireAbyssalSkill()
            task.wait(0.5)
        end
    end)
end

local function StopInfiniteAbyssal()
    isHoldingAbyssal = false
end

-- ===== INFINITE CORRUPT - INPUT HANDLER =====

-- PC: Tekan Q untuk aktif, lepas Q untuk nonaktif
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if _G.Ox.InfAbyssalEnabled and input.KeyCode == Enum.KeyCode.Q then
        StartInfiniteAbyssal()
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.Q then
        StopInfiniteAbyssal()
    end
end)

-- Mobile: Tekan tombol move2
task.spawn(function()
    while _G.Ox.ScriptAlive do
        task.wait(0.5)
        if not _G.Ox.ScriptAlive then break end
        
        if _G.Ox.InfAbyssalEnabled and GetRole() == "Killer" then
            local playerGui = LP:FindFirstChild("PlayerGui")
            if playerGui then
                local killerUI = nil
                for _, gui in pairs(playerGui:GetChildren()) do
                    if string.sub(gui.Name, -4) == "-mob" and gui.Name ~= "Survivor-mob" then
                        killerUI = gui
                        break
                    end
                end
                
                if killerUI then
                    local controls = killerUI:FindFirstChild("Controls")
                    if controls then
                        local move2Btn = controls:FindFirstChild("move2")
                        if move2Btn and move2Btn.Visible then
                            -- Tombol move2 terlihat, aktifkan Infinite Abyssal
                            StartInfiniteAbyssal()
                        else
                            StopInfiniteAbyssal()
                        end
                    end
                end
            end
        else
            StopInfiniteAbyssal()
        end
    end
end)

-- FOV visual (Pandu)
local FOVGui = Instance.new("ScreenGui")
FOVGui.Name = "FOVCircleGui_Standalone"
FOVGui.ResetOnSpawn = false
FOVGui.IgnoreGuiInset = true
FOVGui.Parent = CoreGui

local VeilFOVFrame = Instance.new("Frame")
VeilFOVFrame.BackgroundTransparency = 1
VeilFOVFrame.AnchorPoint = Vector2.new(0.5, 0.5)
VeilFOVFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
VeilFOVFrame.Visible = false
VeilFOVFrame.Parent = FOVGui
Instance.new("UICorner", VeilFOVFrame).CornerRadius = UDim.new(1, 0)
local VeilFOVStroke = Instance.new("UIStroke", VeilFOVFrame)
VeilFOVStroke.Color = Color3.fromRGB(0, 255, 100)
VeilFOVStroke.Thickness = 1.5

local veilTargetHighlight = Instance.new("Highlight")
veilTargetHighlight.Name = "VD_VeilTarget"
veilTargetHighlight.FillColor = Color3.fromRGB(255, 0, 0)
veilTargetHighlight.OutlineColor = Color3.fromRGB(255, 255, 255)
veilTargetHighlight.FillTransparency = 0.5
veilTargetHighlight.OutlineTransparency = 0

-- ===== SURVIVOR AIMBOT FOV CIRCLE =====
_G.Ox.SurvivorFOVGui = Instance.new("ScreenGui")
_G.Ox.SurvivorFOVGui.Name = "SurvivorAimbotFOV"
_G.Ox.SurvivorFOVGui.ResetOnSpawn = false
_G.Ox.SurvivorFOVGui.IgnoreGuiInset = true
_G.Ox.SurvivorFOVGui.Parent = CoreGui

_G.Ox.SurvivorFOVFrame = Instance.new("Frame")
_G.Ox.SurvivorFOVFrame.BackgroundTransparency = 1
_G.Ox.SurvivorFOVFrame.AnchorPoint = Vector2.new(0.5, 0.5)
_G.Ox.SurvivorFOVFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
_G.Ox.SurvivorFOVFrame.Visible = false
_G.Ox.SurvivorFOVFrame.Parent = _G.Ox.SurvivorFOVGui
Instance.new("UICorner", _G.Ox.SurvivorFOVFrame).CornerRadius = UDim.new(1, 0)
local SurvivorFOVStroke = Instance.new("UIStroke", _G.Ox.SurvivorFOVFrame)
SurvivorFOVStroke.Color = Color3.fromRGB(255, 0, 0)
SurvivorFOVStroke.Thickness = 1.5
SurvivorFOVStroke.Transparency = 0.5

_G.Ox.survivorAimbotHighlight = Instance.new("Highlight")
_G.Ox.survivorAimbotHighlight.Name = "SurvivorAimbotTarget"
_G.Ox.survivorAimbotHighlight.FillColor = Color3.fromRGB(255, 0, 0)
_G.Ox.survivorAimbotHighlight.OutlineColor = Color3.fromRGB(255, 255, 0)
_G.Ox.survivorAimbotHighlight.FillTransparency = 0.4
_G.Ox.survivorAimbotHighlight.OutlineTransparency = 0.2

_G.Ox.killerAimbotHighlight = Instance.new("Highlight")
_G.Ox.killerAimbotHighlight.Name = "KillerAimbotTarget"
_G.Ox.killerAimbotHighlight.FillColor = Color3.fromRGB(255, 0, 0)
_G.Ox.killerAimbotHighlight.OutlineColor = Color3.fromRGB(255, 255, 0)
_G.Ox.killerAimbotHighlight.FillTransparency = 0.4
_G.Ox.killerAimbotHighlight.OutlineTransparency = 0.2

-- ==================================================
-- MAIN RENDERSTEPPED LOOP (GABUNGAN SEMUA)
-- ==================================================
RunService.RenderStepped:Connect(function()
    -- === SPEED BOOST ===
    if _G.Ox.Speed.Enabled then
        local char = LP.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then
                local moveDir = hum.MoveDirection
                if moveDir.Magnitude > 0 then
                    local hrp = char:FindFirstChild("HumanoidRootPart")
                    if hrp then
                        hrp.CFrame = hrp.CFrame + (moveDir * _G.Ox.Speed.Value)
                    end
                end
            end
        end
    end
    
    -- === STRETCH RES ===
    if _G.Ox.StretchRes.Enabled then
        local c = Camera
        if c then
            local y = _G.Ox.StretchRes.Amount
            c.CFrame = c.CFrame * CFrame.new(0, 0, 0, 1, 0, 0, 0, y, 0, 0, 0, 1)
        end
    end
    
    if isAimingFlash and _G.Ox.AimConfig.Flash_Silent then
        local targetPart = getKillerTargetForFlash()
        if targetPart then
            local myChar = LocalPlayer.Character
            local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
            local targetPos = targetPart.Position + Vector3.new(0, _G.Ox.AimConfig.Flash_YOffset, 0)
            
            local cam = workspace.CurrentCamera
            cam.CFrame = cam.CFrame:Lerp(CFrame.lookAt(cam.CFrame.Position, targetPos), 0.5)
            
            if myHRP then
                local goalHrp = CFrame.lookAt(myHRP.Position, Vector3.new(targetPos.X, myHRP.Position.Y, targetPos.Z))
                myHRP.CFrame = myHRP.CFrame:Lerp(goalHrp, 0.5)
            end
        end
    end
    
    -- ===== AIMBOT SURVIVOR (PER-FRAME) =====
    if _G.Ox.Config.Surv_Aimbot_Enabled then
        local myRole = GetRole()
        if myRole == "Survivor" then
            if _G.Ox.Config.Surv_Aimbot_ShowFOV then
                _G.Ox.SurvivorFOVFrame.Visible = true
                _G.Ox.SurvivorFOVFrame.Size = UDim2.new(0, _G.Ox.Config.Surv_Aimbot_Radius * 2, 0, _G.Ox.Config.Surv_Aimbot_Radius * 2)
            else
                _G.Ox.SurvivorFOVFrame.Visible = false
            end
            
            local target = getBestAimbotTarget()
            if target then
                _G.Ox.survivorAimbotHighlight.Parent = target.Parent
                local targetPos = target.Position
                local dist = GetDistance(targetPos)
                
                if dist < 100 then 
                    local distanceDiff = 100 - dist
                    local rightOffset = distanceDiff * _G.Ox.Config.Surv_Aimbot_Predict
                    targetPos = targetPos + (Camera.CFrame.RightVector * rightOffset) 
                end
                
                local goalCameraCFrame = CFrame.new(Camera.CFrame.Position, targetPos)
                Camera.CFrame = Camera.CFrame:Lerp(goalCameraCFrame, _G.Ox.Config.Surv_Aimbot_Smoothness)
                
                local char = LocalPlayer.Character
                local hrp = char and char:FindFirstChild("HumanoidRootPart")
                if hrp then
                    local goalHrpCFrame = CFrame.new(hrp.Position, Vector3.new(targetPos.X, hrp.Position.Y, targetPos.Z))
                    hrp.CFrame = hrp.CFrame:Lerp(goalHrpCFrame, _G.Ox.Config.Surv_Aimbot_Smoothness)
                end
            else
                _G.Ox.survivorAimbotHighlight.Parent = nil
            end
        end
    else
        _G.Ox.SurvivorFOVFrame.Visible = false
        _G.Ox.survivorAimbotHighlight.Parent = nil
    end

    -- ===== AIMBOT KILLER (PER-FRAME) =====
    if _G.Ox.Config.Killer_Aimbot_Enabled and _G.Ox.isKillerAimbotHolding then
        local myRole = GetRole()
        if myRole == "Killer" then
            if _G.Ox.lockedKillerAimbotTarget then
                local targetChar = _G.Ox.lockedKillerAimbotTarget.Parent
                local targetHum = targetChar and targetChar:FindFirstChild("Humanoid")
                
                if not targetHum or targetHum.Health <= 0 or IsDowned(targetChar) then
                    _G.Ox.lockedKillerAimbotTarget = getBestKillerAimbotTarget()
                end

                if _G.Ox.lockedKillerAimbotTarget then
                    local targetPos = _G.Ox.lockedKillerAimbotTarget.Position
                    if GetDistance(targetPos) > _G.Ox.Config.Killer_Aimbot_MaxDist then
                        _G.Ox.lockedKillerAimbotTarget = getBestKillerAimbotTarget()
                    else
                        _G.Ox.killerAimbotHighlight.Parent = targetChar
                        
                        local goalCameraCFrame = CFrame.new(Camera.CFrame.Position, targetPos)
                        Camera.CFrame = Camera.CFrame:Lerp(goalCameraCFrame, _G.Ox.Config.Killer_Aimbot_Smoothness)
                        
                        local char = LocalPlayer.Character
                        local hrp = char and char:FindFirstChild("HumanoidRootPart")
                        if hrp then
                            local goalHrpCFrame = CFrame.new(hrp.Position, Vector3.new(targetPos.X, hrp.Position.Y, targetPos.Z))
                            hrp.CFrame = hrp.CFrame:Lerp(goalHrpCFrame, _G.Ox.Config.Killer_Aimbot_Smoothness)
                        end
                    end
                end
            else
                _G.Ox.lockedKillerAimbotTarget = getBestKillerAimbotTarget()
            end
        end
    else
        if _G.Ox.killerAimbotHighlight then
            _G.Ox.killerAimbotHighlight.Parent = nil
        end
    end
    
    -- === SILENT VEIL FOV ===
    local char = player.Character
    local isSpearMode = char and char:GetAttribute("spearmode") == true
    if _G.Ox.AimConfig.Aim_SilentVeilV2 and _G.Ox.AimConfig.Veil_ShowFOV and isSpearMode then
        VeilFOVFrame.Visible = true
        VeilFOVFrame.Size = UDim2.new(0, _G.Ox.AimConfig.Veil_FOV * 2, 0, _G.Ox.AimConfig.Veil_FOV * 2)
    else
        VeilFOVFrame.Visible = false
    end
    
    if _G.Ox.AimConfig.Aim_SilentVeilV2 and isSpearMode then
        local targetPart = getClosestSurvivor()
        if targetPart and targetPart.Parent then
            veilTargetHighlight.Parent = targetPart.Parent
        else
            veilTargetHighlight.Parent = nil
        end
    else
        veilTargetHighlight.Parent = nil
    end
end)

-- ==================================================
-- MAIN HEARTBEAT LOOP (GABUNGAN)
-- ==================================================
local lastEspUpdate = 0

RunService.Heartbeat:Connect(function()
    if _G.Ox.ParryConfig.ParryCircle and _G.Ox.ParryConfig.AutoParry then
        local char = LP.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        if hrp then
            if not _G.Ox.State.AutoParryAdornment or _G.Ox.State.AutoParryAdornment.Parent ~= hrp then
                if _G.Ox.State.AutoParryAdornment then _G.Ox.State.AutoParryAdornment:Destroy() end
                _G.Ox.State.AutoParryAdornment = Instance.new("CylinderHandleAdornment")
                _G.Ox.State.AutoParryAdornment.Name = "AutoParryCircleESP"
                _G.Ox.State.AutoParryAdornment.Height = 0.05
                _G.Ox.State.AutoParryAdornment.Transparency = 0.3
                _G.Ox.State.AutoParryAdornment.Adornee = hrp
                _G.Ox.State.AutoParryAdornment.Parent = hrp
                _G.Ox.State.AutoParryAdornment.ZIndex = 0
                _G.Ox.State.AutoParryAdornment.AlwaysOnTop = false
            end
            local cR = _G.Ox.ParryConfig.ParryRadius
            _G.Ox.State.AutoParryAdornment.Radius = cR
            _G.Ox.State.AutoParryAdornment.InnerRadius = math.max(0.1, cR - 0.15)
            _G.Ox.State.AutoParryAdornment.CFrame = CFrame.new(0, -3, 0) * CFrame.Angles(math.rad(90), 0, 0)
            if _G.Ox.State.ParryCooldown then
                _G.Ox.State.AutoParryAdornment.Color3 = Color3.fromRGB(255, 128, 0)
            elseif _G.Ox.ParryConfig.ParryAggressive then
                _G.Ox.State.AutoParryAdornment.Color3 = Color3.fromRGB(255, 0, 0)
            else
                _G.Ox.State.AutoParryAdornment.Color3 = Color3.fromRGB(0, 255, 255)
            end
        elseif _G.Ox.State.AutoParryAdornment then
            _G.Ox.State.AutoParryAdornment:Destroy()
            _G.Ox.State.AutoParryAdornment = nil
        end
    elseif _G.Ox.State.AutoParryAdornment then
        _G.Ox.State.AutoParryAdornment:Destroy()
        _G.Ox.State.AutoParryAdornment = nil
    end
    -- === CAMERA FOV ===
    if _G.Ox.CameraFOV.Enabled then
        Camera.FieldOfView = _G.Ox.CameraFOV.Value
    end
    
    -- ===== ANTI SLOWDOWN KILLER =====
    if _G.Ox.NoSlowdownEnabled then
        local char = LP.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum and GetRole() == "Killer" then
                if hum.WalkSpeed < 16 then
                    hum.WalkSpeed = 16
                end
            end
        end
    end
    
    -- === ESP UPDATE (5 DETIK) ===
    if _G.Ox.ESPConfig.Master and tick() - lastEspUpdate >= 5 then
        lastEspUpdate = tick()
        UpdatePlayerESP()
        UpdateSCPESP()
        UpdateStaticESP()
    end
end)

-- ==================================================
-- ITEMS FOLDER UNTUK ICON
-- ==================================================
local ItemsFolder = ReplicatedStorage:WaitForChild("Items", 5)

function extractAssetId(str)
    return tostring(str):match("%d+")
end

function getItemIcon(itemName)
    if not ItemsFolder then return nil end
    local item = ItemsFolder:FindFirstChild(itemName)
    if not item then return nil end
    local tex
    pcall(function() tex = item.Texture or item.Image end)
    if tex and tex ~= "" then
        local id = extractAssetId(tex)
        if id then return ("rbxthumb://type=Asset&id=%s&w=420&h=420"):format(id) end
    end
    return nil
end

-- ==================================================
-- MODERN ESP (SAMA PERSIS PANDU HUB)
-- ==================================================

function CreateModernESP(parent, idName, config)
    local billboard = parent:FindFirstChild(idName)
    
    if not billboard then 
        billboard = Instance.new("BillboardGui")
        billboard.Name = idName
        billboard.Parent = parent
        billboard.AlwaysOnTop = true
        billboard.Size = UDim2.new(0, 200, 0, 25)
        
        local yOffset = config.offsetY or 3.5
        billboard.StudsOffset = Vector3.new(0, yOffset, 0) 
        
        local box = Instance.new("Frame")
        box.Name = "Box"
        box.AutomaticSize = Enum.AutomaticSize.X 
        box.Size = UDim2.new(0, 0, 0, 15)
        box.Position = UDim2.new(0.5, 0, 0, 0) 
        box.AnchorPoint = Vector2.new(0.5, 0)
        box.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
        box.BackgroundTransparency = 0 
        box.BorderSizePixel = 0
        box.ZIndex = 2
        box.Parent = billboard
        
        Instance.new("UICorner", box).CornerRadius = UDim.new(0, 3)
        
        local boxGradient = Instance.new("UIGradient")
        boxGradient.Transparency = NumberSequence.new({
            NumberSequenceKeypoint.new(0, 1),
            NumberSequenceKeypoint.new(0.15, 0.35),
            NumberSequenceKeypoint.new(0.85, 0.35),
            NumberSequenceKeypoint.new(1, 1)
        })
        boxGradient.Parent = box
        
        local padding = Instance.new("UIPadding", box)
        padding.PaddingLeft = UDim.new(0, 8)
        padding.PaddingRight = UDim.new(0, 8)
        
        local layout = Instance.new("UIListLayout", box)
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.VerticalAlignment = Enum.VerticalAlignment.Center
        layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
        layout.Padding = UDim.new(0, 3) 
        layout.SortOrder = Enum.SortOrder.LayoutOrder
        
        local icon = Instance.new("ImageLabel", box)
        icon.Name = "Icon"
        icon.Size = UDim2.new(0, 12, 0, 12)
        icon.BackgroundTransparency = 1
        icon.ZIndex = 3
        icon.LayoutOrder = 1
        icon.Visible = false
        
        local txt = Instance.new("TextLabel", box)
        txt.Name = "Text"
        txt.AutomaticSize = Enum.AutomaticSize.X
        txt.Size = UDim2.new(0, 0, 1, 0)
        txt.BackgroundTransparency = 1
        txt.Font = Enum.Font.GothamMedium 
        txt.TextSize = 10 
        txt.ZIndex = 3
        txt.LayoutOrder = 2
        txt.RichText = true
        txt.TextXAlignment = Enum.TextXAlignment.Center
        txt.TextYAlignment = Enum.TextYAlignment.Center

        local line = Instance.new("Frame")
        line.Name = "Line"
        line.Size = UDim2.new(0, 1, 0, 10) 
        line.Position = UDim2.new(0.5, 0, 0, 15) 
        line.AnchorPoint = Vector2.new(0.5, 0) 
        line.BorderSizePixel = 0
        line.ZIndex = 1
        line.Parent = billboard 

        local lineGradient = Instance.new("UIGradient")
        lineGradient.Rotation = 90
        lineGradient.Transparency = NumberSequence.new({
            NumberSequenceKeypoint.new(0, 0),
            NumberSequenceKeypoint.new(1, 1) 
        })
        lineGradient.Parent = line
    end
    
    billboard.Line.BackgroundColor3 = config.color
    
    local iconLabel = billboard.Box:FindFirstChild("Icon")
    if config.icon and config.icon ~= "" then
        if iconLabel then
            iconLabel.Image = config.icon
            iconLabel.Visible = true
        end
    else
        if iconLabel then
            iconLabel.Visible = false
        end
    end
    
    local hexColor = string.format("#%02X%02X%02X", config.color.R * 255, config.color.G * 255, config.color.B * 255)
    
    if config.distance then
        billboard.Box.Text.Text = string.format("<font color='#FFFFFF'>%s</font> <font color='%s'>[%dm]</font>", config.name, hexColor, config.distance)
    elseif config.subtext then
        billboard.Box.Text.Text = string.format("<font color='#FFFFFF'>%s</font> <font color='%s'>%s</font>", config.name, hexColor, config.subtext)
    else
        billboard.Box.Text.Text = string.format("<font color='#FFFFFF'>%s</font>", config.name)
    end
end

-- ==================================================
-- FAKE PERKS FUNCTIONS
-- ==================================================

local function IsSurvivorFake()
    return LP.Team and LP.Team.Name == "Survivors"
end

local function isSlowVaulting(char)
    local humanoid = char:FindFirstChildOfClass("Humanoid")
    if not humanoid then return false end
    
    local animator = humanoid:FindFirstChildOfClass("Animator")
    if not animator then return false end

    for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
        if track.Animation and string.find(tostring(track.Animation.AnimationId), "126081405469607") then
            return true
        end
    end
    return false
end

-- ===== QUICK RECOVERY =====
local function applyQuickRecovery(char)
    if not _G.Ox.FakePerks.QuickRecoveryEnabled then return end
    if not IsSurvivorFake() then return end
    if _G.Ox.FakePerks.boostActive then return end
    if (tick() - _G.Ox.FakePerks.lastQRTime) < _G.Ox.FakePerks.PerkCooldown then return end

    _G.Ox.FakePerks.boostActive = true
    _G.Ox.FakePerks.lastQRTime = tick()
    local startTime = tick()

    task.spawn(function()
        while _G.Ox.FakePerks.QuickRecoveryEnabled and (tick() - startTime) < 3 do
            if char and char.Parent then
                char:SetAttribute("speedboost", 1.4)
            end
            task.wait()
        end

        if char and char.Parent then
            char:SetAttribute("speedboost", 1)
        end
        _G.Ox.FakePerks.boostActive = false
    end)
end

local function setupQuickRecovery(char)
    if _G.Ox.FakePerks.qrConnection then
        _G.Ox.FakePerks.qrConnection:Disconnect()
        _G.Ox.FakePerks.qrConnection = nil
    end

    if not char then return end

    _G.Ox.FakePerks.qrConnection = char:GetAttributeChangedSignal("isvaulting"):Connect(function()
        if char:GetAttribute("isvaulting") == true then
            task.spawn(function()
                task.wait(0.05)

                local isSlow = isSlowVaulting(char)
                if not isSlow then
                    task.wait(0.05)
                    isSlow = isSlowVaulting(char)
                end

                if not isSlow and char:GetAttribute("isvaulting") == true then
                    applyQuickRecovery(char)
                end
            end)
        end
    end)
end

function ToggleQuickRecovery(value)
    _G.Ox.FakePerks.QuickRecoveryEnabled = value
    
    if value and LP.Character then
        setupQuickRecovery(LP.Character)
    elseif not value and _G.Ox.FakePerks.qrConnection then
        _G.Ox.FakePerks.qrConnection:Disconnect()
        _G.Ox.FakePerks.qrConnection = nil
    end
    
    Library:Notify({ Title = "Fake Quick Recovery", Description = value and "AKTIF" or "NONAKTIF", Duration = 2 })
end

-- ===== PERFECT LANDING =====
local function applyPerfectLanding(char)
    if not _G.Ox.FakePerks.PerfectLandingEnabled then return end
    if not IsSurvivorFake() then return end
    if _G.Ox.FakePerks.perfectLandingBoostActive then return end
    if (tick() - _G.Ox.FakePerks.lastPLTime) < _G.Ox.FakePerks.PerkCooldown then return end

    _G.Ox.FakePerks.perfectLandingBoostActive = true
    _G.Ox.FakePerks.lastPLTime = tick()
    local startTime = tick()

    task.spawn(function()
        while _G.Ox.FakePerks.perfectLandingBoostActive and (tick() - startTime) < 3 do
            if char and char.Parent then
                char:SetAttribute("speedboost", 1.4)
            end
            task.wait()
        end

        if char and char.Parent then
            char:SetAttribute("speedboost", 1)
        end
        _G.Ox.FakePerks.perfectLandingBoostActive = false
    end)
end

local function setupPerfectLanding(char)
    if _G.Ox.FakePerks.plConnection then
        _G.Ox.FakePerks.plConnection:Disconnect()
        _G.Ox.FakePerks.plConnection = nil
    end

    if not char then return end

    _G.Ox.FakePerks.plConnection = char:GetAttributeChangedSignal("speedboost"):Connect(function()
        if not IsSurvivorFake() then return end

        local currentBoost = char:GetAttribute("speedboost")
        if currentBoost == 0.625 then
            applyPerfectLanding(char)
        end
    end)
end

function TogglePerfectLanding(value)
    _G.Ox.FakePerks.PerfectLandingEnabled = value

    if value then
        if LP.Character then
            setupPerfectLanding(LP.Character)
        end
    else
        if _G.Ox.FakePerks.plConnection then
            _G.Ox.FakePerks.plConnection:Disconnect()
            _G.Ox.FakePerks.plConnection = nil
        end

        if _G.Ox.FakePerks.perfectLandingBoostActive and LP.Character then
            LP.Character:SetAttribute("speedboost", 1)
            _G.Ox.FakePerks.perfectLandingBoostActive = false
        end
    end

    Library:Notify({ Title = "Fake Perfect Landing", Description = value and "AKTIF" or "NONAKTIF", Duration = 2 })
end

-- ===== FLOWSTATE =====
local function tryApplyFlowstate(char)
    if not _G.Ox.FakePerks.FlowstateEnabled then return end
    if not char or not char.Parent then return end
    if not IsSurvivorFake() then return end
    if _G.Ox.FakePerks.fsOnCooldown then return end
    if (tick() - _G.Ox.FakePerks.lastFSTime) < _G.Ox.FakePerks.PerkCooldown then return end

    char:SetAttribute("Flowstate", true)
end

local function setupFlowstate(char)
    if _G.Ox.FakePerks.fsConnection then
        _G.Ox.FakePerks.fsConnection:Disconnect()
        _G.Ox.FakePerks.fsConnection = nil
    end
    if _G.Ox.FakePerks.fsAnimConnection then
        _G.Ox.FakePerks.fsAnimConnection:Disconnect()
        _G.Ox.FakePerks.fsAnimConnection = nil
    end

    if not char then return end

    local humanoid = char:WaitForChild("Humanoid", 3)
    if not humanoid then return end

    local animator = humanoid:WaitForChild("Animator", 3)
    if not animator then return end

    _G.Ox.FakePerks.fsAnimConnection = animator.AnimationPlayed:Connect(function(track)
        if not track.Animation then return end
        if track.Animation.AnimationId ~= "rbxassetid://136962284480779" then return end

        local stoppedConn
        stoppedConn = track.Stopped:Connect(function()
            if stoppedConn then
                stoppedConn:Disconnect()
            end

            _G.Ox.FakePerks.lastFSTime = tick()
            _G.Ox.FakePerks.fsOnCooldown = true

            if char and char.Parent then
                char:SetAttribute("Flowstate", false)
            end

            task.delay(_G.Ox.FakePerks.PerkCooldown, function()
                _G.Ox.FakePerks.fsOnCooldown = false

                if _G.Ox.FakePerks.FlowstateEnabled and char and char.Parent then
                    char:SetAttribute("Flowstate", true) 
                end
            end)
        end)
    end)

    _G.Ox.FakePerks.fsConnection = char:GetAttributeChangedSignal("Flowstate"):Connect(function()
        if not _G.Ox.FakePerks.FlowstateEnabled then return end
        if not IsSurvivorFake() then return end

        local current = char:GetAttribute("Flowstate")

        if current == true then
            if _G.Ox.FakePerks.fsOnCooldown or (tick() - _G.Ox.FakePerks.lastFSTime) < _G.Ox.FakePerks.PerkCooldown then
                char:SetAttribute("Flowstate", false)
            end
        elseif current == false then
            if not _G.Ox.FakePerks.fsOnCooldown and (tick() - _G.Ox.FakePerks.lastFSTime) >= _G.Ox.FakePerks.PerkCooldown then
                char:SetAttribute("Flowstate", true)
            end
        end
    end)

    task.wait(0.5)
    if _G.Ox.FakePerks.FlowstateEnabled and char and char.Parent then
        char:SetAttribute("Flowstate", true)
    end
end

function ToggleFlowstate(value)
    _G.Ox.FakePerks.FlowstateEnabled = value

    if value then
        if LP.Character then
            setupFlowstate(LP.Character)
        end
    else
        if _G.Ox.FakePerks.fsConnection then
            _G.Ox.FakePerks.fsConnection:Disconnect()
            _G.Ox.FakePerks.fsConnection = nil
        end
        if _G.Ox.FakePerks.fsAnimConnection then
            _G.Ox.FakePerks.fsAnimConnection:Disconnect()
            _G.Ox.FakePerks.fsAnimConnection = nil
        end

        if LP.Character then
            LP.Character:SetAttribute("Flowstate", false)
        end
    end

    Library:Notify({ Title = "Fake Flowstate", Description = value and "AKTIF" or "NONAKTIF", Duration = 2 })
end

-- ===== SETUP CHARACTER FOR FAKE PERKS =====
local function onCharacterAddedFake(char)
    task.wait(0.5)

    if _G.Ox.FakePerks.QuickRecoveryEnabled then
        setupQuickRecovery(char)
    end

    if _G.Ox.FakePerks.PerfectLandingEnabled then
        setupPerfectLanding(char)
    end

    if _G.Ox.FakePerks.FlowstateEnabled then
        setupFlowstate(char)
    end
end

-- ===== INIT FAKE PERKS =====
if LP.Character then
    onCharacterAddedFake(LP.Character)
end
LP.CharacterAdded:Connect(onCharacterAddedFake)

-- ==================================================
-- STREAMER MODE (SEPERTI PANDU)
-- ==================================================

local function enableStreamerMode()
    if not _G.Ox.StreamerMode.Enabled then return end
    local PlayerGui = LP:FindFirstChild("PlayerGui")
    if not PlayerGui then return end
    
    -- Fungsi untuk mengganti nama player di UI
    local function applySpooferToObj(obj)
        if obj:IsA("TextLabel") or obj:IsA("TextButton") or obj:IsA("TextBox") then
            local function updateText()
                if not _G.Ox.StreamerMode.Enabled then return end
                local currentText = obj.Text
                local changed = false

                if not obj:GetAttribute("OriginalText") then
                    obj:SetAttribute("OriginalText", currentText)
                end

                for _, player in ipairs(Players:GetPlayers()) do
                    if string.find(currentText, player.Name) or string.find(currentText, player.DisplayName) then
                        currentText = string.gsub(currentText, player.Name, "OXIO")
                        currentText = string.gsub(currentText, player.DisplayName, "OXIO")
                        changed = true
                    end
                end
                
                if changed and obj.Text ~= currentText then
                    obj.Text = currentText
                end
            end

            updateText()
            local conn = obj:GetPropertyChangedSignal("Text"):Connect(updateText)
            table.insert(_G.Ox.StreamerMode.SpooferConns, conn)
            
        elseif obj:IsA("ImageLabel") or obj:IsA("ImageButton") then
            local function updateImage()
                if not _G.Ox.StreamerMode.Enabled then return end
                local currentImg = obj.Image
                local changed = false

                for _, player in ipairs(Players:GetPlayers()) do
                    if string.find(currentImg, tostring(player.UserId)) then
                        if not obj:GetAttribute("OriginalImage") then
                            obj:SetAttribute("OriginalImage", currentImg)
                        end
                        changed = true
                        break 
                    end
                end

                if changed and obj.Image ~= "rbxassetid://118755181202304" then
                    obj.Image = "rbxassetid://118755181202304" -- Icon default
                end
            end

            updateImage() 
            local conn = obj:GetPropertyChangedSignal("Image"):Connect(updateImage)
            table.insert(_G.Ox.StreamerMode.SpooferConns, conn)
        end
    end
    
    -- Apply ke semua objek di PlayerGui
    for _, obj in ipairs(PlayerGui:GetDescendants()) do 
        applySpooferToObj(obj) 
    end
    
    -- Pantau objek baru yang ditambahkan
    local conn = PlayerGui.DescendantAdded:Connect(function(obj) 
        task.defer(function() 
            applySpooferToObj(obj) 
        end) 
    end)
    table.insert(_G.Ox.StreamerMode.SpooferConns, conn)
end

local function disableStreamerMode()
    -- Putuskan semua koneksi
    for _, conn in ipairs(_G.Ox.StreamerMode.SpooferConns) do 
        if conn.Connected then 
            conn:Disconnect() 
        end 
    end
    table.clear(_G.Ox.StreamerMode.SpooferConns) 
    
    -- Kembalikan teks asli
    local PlayerGui = LP:FindFirstChild("PlayerGui")
    if PlayerGui then
        for _, obj in ipairs(PlayerGui:GetDescendants()) do
            if obj:IsA("TextLabel") or obj:IsA("TextButton") or obj:IsA("TextBox") then
                local oriText = obj:GetAttribute("OriginalText")
                if oriText then
                    obj.Text = oriText
                    obj:SetAttribute("OriginalText", nil)
                end
            elseif obj:IsA("ImageLabel") or obj:IsA("ImageButton") then
                local oriImage = obj:GetAttribute("OriginalImage")
                if oriImage then
                    obj.Image = oriImage
                    obj:SetAttribute("OriginalImage", nil)
                end
            end
        end
    end
end

-- Fungsi toggle Streamer Mode
local function ToggleStreamerMode(state)
    _G.Ox.StreamerMode.Enabled = state
    if state then 
        enableStreamerMode() 
        Library:Notify({ Title = "Streamer Mode", Description = "AKTIF - Nama player disembunyikan", Duration = 3 })
    else 
        disableStreamerMode()
        Library:Notify({ Title = "Streamer Mode", Description = "NONAKTIF", Duration = 2 })
    end
end

-- Setup ulang saat karakter baru
LP.CharacterAdded:Connect(function()
    task.wait(1)
    if _G.Ox.StreamerMode.Enabled then
        disableStreamerMode()
        task.wait(0.5)
        enableStreamerMode()
    end
end)

-- Setup ulang saat PlayerGui berubah
PlayerGui.ChildAdded:Connect(function()
    task.wait(0.5)
    if _G.Ox.StreamerMode.Enabled then
        enableStreamerMode()
    end
end)

-- ==================================================
-- STRETCH RES
-- ==================================================

local function ToggleStretch(state)
    _G.Ox.StretchRes.Enabled = state
    Library:Notify({ Title = "Stretch Res", Description = state and "AKTIF" or "NONAKTIF", Duration = 2 })
end

-- ==================================================
-- HOOK DATA & CLEAR ESP FUNCTIONS
-- ==================================================

local HookData = {}

function UpdateHookData()
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer and plr.Team and plr.Team.Name == "Survivors" then
            local hookCount = plr:GetAttribute("HookCount") or 0
            HookData[plr.Name] = hookCount
        end
    end
end

function CreateHookESP(parent, hookCount)
    if not _G.Ox.ESPConfig.HookCount then return end
    
    local espName = "HookESP_" .. parent.Name
    local existing = parent:FindFirstChild(espName)
    if existing then existing:Destroy() end
    
    local billboard = Instance.new("BillboardGui")
    billboard.Name = espName
    billboard.Parent = parent
    billboard.AlwaysOnTop = true
    billboard.Size = UDim2.new(0, 60, 0, 18)
    billboard.StudsOffset = Vector3.new(0, 5.5, 0)
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Font = Enum.Font.GothamBold
    label.TextSize = 11
    label.TextScaled = true
    label.Text = string.format("Hooked %d", hookCount)
    label.TextXAlignment = Enum.TextXAlignment.Center
    label.TextYAlignment = Enum.TextYAlignment.Center
    label.Parent = billboard
    
    if hookCount >= 3 then
        label.TextColor3 = Color3.fromRGB(255, 50, 50)
    elseif hookCount >= 2 then
        label.TextColor3 = Color3.fromRGB(255, 200, 50)
    else
        label.TextColor3 = Color3.fromRGB(200, 200, 200)
    end
    
    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(0, 0, 0)
    stroke.Thickness = 2
    stroke.Transparency = 0.5
    stroke.Parent = label
end

function UpdateHookESP()
    if not _G.Ox.ESPConfig.HookCount then 
        ClearHookESP()
        return 
    end
    
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            local isKiller = p.Team and p.Team.Name == "Killer"
            if not isKiller then
                local hookCount = HookData[p.Name] or p:GetAttribute("HookCount") or 0
                CreateHookESP(p.Character, hookCount)
            end
        end
    end
end

function ClearHookESP()
    for _, p in pairs(Players:GetPlayers()) do
        if p.Character then
            for _, child in pairs(p.Character:GetChildren()) do
                if string.match(child.Name, "^HookESP_") then
                    child:Destroy()
                end
            end
        end
    end
end

function ClearESP(tipe) 
    if tipe == "Player" then 
        for _, p in pairs(Players:GetPlayers()) do 
            if p.Character then 
                if p.Character:FindFirstChild("PEH") then p.Character.PEH:Destroy() end
                if p.Character:FindFirstChild("PE_Text") then p.Character.PE_Text:Destroy() end 
            end 
        end 
    elseif tipe == "Killer" then 
        for _, p in pairs(Players:GetPlayers()) do 
            if p.Character then 
                if p.Character:FindFirstChild("KEH") then p.Character.KEH:Destroy() end
                if p.Character:FindFirstChild("KE_Text") then p.Character.KE_Text:Destroy() end 
            end 
        end 
    elseif tipe == "Generator" then 
        for _, v in pairs(CacheESP.Generators) do 
            if v.model and v.model.Parent then 
                if v.model:FindFirstChild("GEH") then v.model.GEH:Destroy() end
                if v.model:FindFirstChild("GE_Text") then v.model.GE_Text:Destroy() end 
            end 
        end 
    elseif tipe == "Pallet" then 
        for _, v in pairs(CacheESP.Pallets) do 
            if v and v.Parent then 
                if v:FindFirstChild("PalletEH") then v.PalletEH:Destroy() end 
            end 
        end
    elseif tipe == "Hook" then 
        for _, v in pairs(CacheESP.Hooks) do 
            if v and v.Parent then 
                if v:FindFirstChild("HookEH") then v.HookEH:Destroy() end
            end 
        end
    elseif tipe == "Window" then 
        for _, v in pairs(CacheESP.Windows) do 
            if v and v.Parent then 
                if v:FindFirstChild("WindowEH") then v.WindowEH:Destroy() end
            end 
        end
    elseif tipe == "Gate" then 
        for _, v in pairs(CacheESP.Gates) do 
            if v and v.Parent then 
                if v:FindFirstChild("GateEH") then v.GateEH:Destroy() end
            end 
        end
    elseif tipe == "SCP" then 
        for _, v in pairs(CacheESP.SCPs) do 
            if v and v.Parent then 
                if v:FindFirstChild("SCPEH") then v.SCPEH:Destroy() end
            end 
        end
    end 
end

function ClearAllESP() 
    ClearESP("Player") 
    ClearESP("Killer") 
    ClearESP("Generator") 
    ClearESP("Pallet") 
    ClearESP("Hook") 
    ClearESP("Gate") 
    ClearESP("SCP") 
    ClearESP("Window")
    ClearHookESP()
end

-- ==================================================
-- UPDATE ESP FUNCTIONS
-- ==================================================

function UpdatePlayerESP()
    if not _G.Ox.ESPConfig.Master then return end
    local myChar = LocalPlayer.Character
    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
    
    -- Killer Warn
    if _G.Ox.ESPConfig.KillerWarn and myHRP and GetRole() == "Survivor" then
        local warn = myHRP:FindFirstChild("KillerWarn")
        local closestDist = math.huge
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Team and p.Team.Name == "Killer" and p.Character then
                local kHrp = p.Character:FindFirstChild("HumanoidRootPart")
                if kHrp then
                    local dist = (kHrp.Position - myHRP.Position).Magnitude
                    if dist < closestDist then closestDist = dist end
                end
            end
        end
        if closestDist <= 80 then
            if not warn then
                warn = Instance.new("BillboardGui")
                warn.Name = "KillerWarn"
                warn.Size = UDim2.new(0, 30, 0, 30)
                warn.AlwaysOnTop = true
                warn.StudsOffset = Vector3.new(0, 4, 0)
                warn.Parent = myHRP
                local txt = Instance.new("TextLabel")
                txt.Name = "WarnText"
                txt.Size = UDim2.new(1,0,1,0)
                txt.BackgroundTransparency = 1
                txt.TextScaled = true
                txt.TextStrokeTransparency = 0
                txt.Font = Enum.Font.GothamBlack
                txt.Parent = warn
            end
            local txt = warn:FindFirstChild("WarnText")
            if txt then
                if closestDist <= 40 then
                    txt.Text = "!!"
                    txt.TextColor3 = Color3.fromRGB(255, 0, 0) 
                else
                    txt.Text = "!"
                    txt.TextColor3 = Color3.fromRGB(255, 255, 0) 
                end
            end
        else
            if warn then warn:Destroy() end
        end
    else
        local warn = myHRP and myHRP:FindFirstChild("KillerWarn")
        if warn then warn:Destroy() end
    end
    
    -- Player & Killer ESP
    if _G.Ox.ESPConfig.Player or _G.Ox.ESPConfig.Killer then
        for _, p in pairs(Players:GetPlayers()) do 
            if p ~= LocalPlayer and p.Character then
                local killerStatus = p.Team and p.Team.Name == "Killer"
                local root = p.Character:FindFirstChild("Head")
                if root and ((not killerStatus and _G.Ox.ESPConfig.Player) or (killerStatus and _G.Ox.ESPConfig.Killer)) then
                    local distance = myHRP and math.floor((root.Position - myHRP.Position).Magnitude) or 0
                    local col = killerStatus and _G.Ox.ESPColors.Killer or _G.Ox.ESPColors.Player
                    local hName = killerStatus and "KEH" or "PEH"
                    local tName = killerStatus and "KE_Text" or "PE_Text"
                    
                    local highlight = p.Character:FindFirstChild(hName)
                    if not highlight then 
                        highlight = Instance.new("Highlight")
                        highlight.Name = hName
                        highlight.Parent = p.Character 
                    end
                    highlight.FillColor = col
                    highlight.OutlineColor = col
                    highlight.FillTransparency = _G.Ox.ESPConfig.Outline and 1 or 0.5
                    highlight.OutlineTransparency = 0
                    
                    if _G.Ox.ESPConfig.Name then
                        local equippedIcon = nil
                        if _G.Ox.ESPConfig.ItemIcon and not killerStatus then
                            local equippedName = p:GetAttribute("EquippedItem")
                            if equippedName and equippedName ~= "" then
                                equippedIcon = getItemIcon(equippedName)
                            end
                        end
                        CreateModernESP(p.Character, tName, {
                            name = p.Name,
                            distance = _G.Ox.ESPConfig.Distance and distance or nil,
                            color = col,
                            icon = equippedIcon
                        })
                    else
                        local old = p.Character:FindFirstChild(tName)
                        if old then old:Destroy() end
                    end
                end
            end 
        end
    end
end

function UpdateSCPESP()
    if not _G.Ox.ESPConfig.Master then return end
    if _G.Ox.ESPConfig.SCP then
        for _, obj in ipairs(CacheESP.SCPs) do
            if obj and obj.Parent then
                local hum = obj:FindFirstChildOfClass("Humanoid")
                local root = obj:FindFirstChild("HumanoidRootPart") or obj:FindFirstChild("Torso") or obj.PrimaryPart
                
                if hum and root and hum.Health > 0 and math.floor(hum.WalkSpeed) == 9 then
                    local col = _G.Ox.ESPColors.SCP
                    local highlight = obj:FindFirstChild("SCPEH")
                    if not highlight then 
                        highlight = Instance.new("Highlight")
                        highlight.Name = "SCPEH"
                        highlight.Parent = obj 
                    end
                    highlight.FillColor = col
                    highlight.OutlineColor = col
                    highlight.FillTransparency = _G.Ox.ESPConfig.Outline and 1 or 0.5
                    highlight.OutlineTransparency = 0
                else
                    local h = obj:FindFirstChild("SCPEH")
                    if h then h:Destroy() end
                end
            end
        end
    else
        ClearESP("SCP")
    end
end

function UpdateStaticESP()
    if not _G.Ox.ESPConfig.Master then return end
    
    -- Generator ESP
    if _G.Ox.ESPConfig.Generator then
        for i, vData in pairs(CacheESP.Generators) do 
            local v = vData.model
            if v and v.Parent then
                local progress = math.floor(v:GetAttribute("RepairProgress") or 0)
                local col = (progress >= 100) and _G.Ox.ESPColors.GeneratorDone or _G.Ox.ESPColors.Generator
                
                local h = v:FindFirstChild("GEH")
                if not h then 
                    h = Instance.new("Highlight")
                    h.Name = "GEH"
                    h.Parent = v 
                end
                h.FillColor = col
                h.OutlineColor = col
                h.FillTransparency = _G.Ox.ESPConfig.Outline and 1 or 0.5
                h.OutlineTransparency = 0
                
                if _G.Ox.ESPConfig.GeneratorName then
                    local displayName = "GEN"
                    local displaySubtext = ""
                    
                    if progress >= 100 then
                        displaySubtext = "DONE 100%"
                    else
                        displaySubtext = string.format("%d%%", progress)
                    end
                    
                    CreateModernESP(v, "GE_Text", {
                        name = displayName,
                        subtext = displaySubtext,
                        color = col,
                        icon = nil
                    })
                else
                    local oldText = v:FindFirstChild("GE_Text")
                    if oldText then oldText:Destroy() end
                end
            end
        end
    end
    
    -- Pallet ESP
    if _G.Ox.ESPConfig.Pallet then
        for _, v in pairs(CacheESP.Pallets) do
            if v and v.Parent then
                local h = v:FindFirstChild("PalletEH")
                if not h then 
                    h = Instance.new("Highlight")
                    h.Name = "PalletEH"
                    h.Parent = v
                end
                h.Adornee = v 
                h.FillColor = _G.Ox.ESPColors.Pallet
                h.OutlineColor = _G.Ox.ESPColors.Pallet
                h.FillTransparency = _G.Ox.ESPConfig.Outline and 1 or 0.5
                h.OutlineTransparency = 0
            end
        end
    end
    
    -- Window ESP
    if _G.Ox.ESPConfig.Window then
        for i, v in ipairs(CacheESP.Windows) do 
            if v and v.Parent then
                local targetPart = v:FindFirstChild("Bottom") or v:FindFirstChildWhichIsA("BasePart")
                if targetPart then
                    local h = v:FindFirstChild("WindowEH")
                    if not h then 
                        h = Instance.new("BoxHandleAdornment")
                        h.Name = "WindowEH"
                        h.Parent = v 
                        h.AlwaysOnTop = true
                        h.ZIndex = 5
                    end
                    h.Adornee = targetPart
                    h.Size = targetPart.Size
                    h.Color3 = _G.Ox.ESPColors.Window
                    h.Transparency = _G.Ox.ESPConfig.Outline and 0.8 or 0.4
                end
            end 
        end
    end
    
    -- Hook ESP
    if _G.Ox.ESPConfig.Hook then
        for i, v in ipairs(CacheESP.Hooks) do 
            if v and v.Parent then
                local h = v:FindFirstChild("HookEH")
                if not h then 
                    h = Instance.new("Highlight")
                    h.Name = "HookEH"
                    h.Parent = v 
                end
                h.FillColor = _G.Ox.ESPColors.Hook
                h.OutlineColor = _G.Ox.ESPColors.Hook
                h.FillTransparency = _G.Ox.ESPConfig.Outline and 1 or 0.5
                h.OutlineTransparency = 0
            end 
        end
    end
    
    -- Gate ESP
    if _G.Ox.ESPConfig.Gate then
        for i, v in ipairs(CacheESP.Gates) do 
            if v and v.Parent then
                local h = v:FindFirstChild("GateEH")
                if not h then 
                    h = Instance.new("Highlight")
                    h.Name = "GateEH"
                    h.Parent = v 
                end
                h.Adornee = v
                h.FillColor = _G.Ox.ESPColors.Gate
                h.OutlineColor = _G.Ox.ESPColors.Gate
                h.FillTransparency = _G.Ox.ESPConfig.Outline and 1 or 0.5
                h.OutlineTransparency = 0
            end 
        end
    end
end

-- ==================================================
-- HOOK DETECTION
-- ==================================================

function SetupHookDetection()
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer then
            plr:GetAttributeChangedSignal("HookCount"):Connect(function()
                HookData[plr.Name] = plr:GetAttribute("HookCount") or 0
                if _G.Ox.ESPConfig.HookCount then
                    UpdateHookESP()
                end
            end)
        end
    end
end

Players.PlayerAdded:Connect(function(plr)
    plr:GetAttributeChangedSignal("HookCount"):Connect(function()
        HookData[plr.Name] = plr:GetAttribute("HookCount") or 0
        if _G.Ox.ESPConfig.HookCount then
            UpdateHookESP()
        end
    end)
end)

task.spawn(function()
    task.wait(2)
    SetupHookDetection()
end)

-- ===== HOOK ESP UPDATE LOOP =====
task.spawn(function()
    while true do
        task.wait(1)
        if _G.Ox.ESPConfig.HookCount then
            UpdateHookData()
            UpdateHookESP()
        else
            ClearHookESP()
        end
    end
end)

-- ==================================================
-- INFINITY ZOOM
-- ==================================================

local function ToggleInfinityZoom(state)
    _G.Ox.InfinityZoom.Enabled = state
    if state then
        LP.CameraMaxZoomDistance = math.huge
        LP.CameraMinZoomDistance = 0
        Library:Notify({ Title = "Infinity Zoom", Description = "AKTIF - Zoom out tanpa batas", Duration = 2 })
    else
        LP.CameraMaxZoomDistance = 12
        LP.CameraMinZoomDistance = 0.5
        Library:Notify({ Title = "Infinity Zoom", Description = "NONAKTIF", Duration = 2 })
    end
end

local function ToggleCameraFOV(state)
    _G.Ox.CameraFOV.Enabled = state
    if not state then
        Camera.FieldOfView = 70
    end
    Library:Notify({ Title = "Camera FOV", Description = state and "AKTIF" or "NONAKTIF", Duration = 2 })
end

local function SetFOVValue(value)
    _G.Ox.CameraFOV.Value = value
    if _G.Ox.CameraFOV.Enabled then
        Camera.FieldOfView = value
    end
end

-- Auto re-apply saat karakter/Map berubah (seperti Pandu Hub)
LP.CharacterAdded:Connect(function()
    task.wait(0.5)
    if _G.Ox.CameraFOV.Enabled then
        Camera.FieldOfView = _G.Ox.CameraFOV.Value
    end
end)

workspace.ChildAdded:Connect(function(child)
    if child.Name == "Map" then
        task.wait(0.5)
        if _G.Ox.CameraFOV.Enabled then
            Camera.FieldOfView = _G.Ox.CameraFOV.Value
        end
    end
end)

-- ==================================================
-- INSTANT TP GATE (SEPERTI PANDU HUB)
-- ==================================================

-- Load GateClientModule
pcall(function()
    _G.Ox.InstantTPGate.GateClientModule = require(game:GetService("ReplicatedStorage").Modules.Items.GateClient)
end)

local function setAllGateDuration(duration)
    for _, obj in getgc(true) do
        if typeof(obj) == "table" and rawget(obj, "gateDuration") and rawget(obj, "gateRemote") then
            rawset(obj, "gateDuration", duration)
        end
    end
end

local function applyHooks()
    if not _G.Ox.InstantTPGate.GateClientModule then return end

    if not _G.Ox.InstantTPGate.oldGateNew then
        _G.Ox.InstantTPGate.oldGateNew = _G.Ox.InstantTPGate.GateClientModule.new
        _G.Ox.InstantTPGate.GateClientModule.new = function(...)
            local obj = _G.Ox.InstantTPGate.oldGateNew(...)
            if _G.Ox.InstantTPGate.Enabled then
                obj.gateDuration = 0
            end
            return obj
        end
    end

    if not _G.Ox.InstantTPGate.oldGateCanUse then
        _G.Ox.InstantTPGate.oldGateCanUse = _G.Ox.InstantTPGate.GateClientModule.CanUse
        _G.Ox.InstantTPGate.GateClientModule.CanUse = function(self)
            if _G.Ox.InstantTPGate.Enabled then
                if self.isSilenced or self.isBuffering then
                    return false
                end
                return os.clock() - self.lastUse >= self.currentCooldown
            end
            return _G.Ox.InstantTPGate.oldGateCanUse(self)
        end
    end
end

local function removeHooks()
    if not _G.Ox.InstantTPGate.GateClientModule then return end

    if _G.Ox.InstantTPGate.oldGateNew then
        _G.Ox.InstantTPGate.GateClientModule.new = _G.Ox.InstantTPGate.oldGateNew
        _G.Ox.InstantTPGate.oldGateNew = nil
    end
    if _G.Ox.InstantTPGate.oldGateCanUse then
        _G.Ox.InstantTPGate.GateClientModule.CanUse = _G.Ox.InstantTPGate.oldGateCanUse
        _G.Ox.InstantTPGate.oldGateCanUse = nil
    end
end

local function ToggleInstantTPGate(state)
    _G.Ox.InstantTPGate.Enabled = state
    
    if state then
        setAllGateDuration(0)
        applyHooks()
        Library:Notify({ Title = "Instant TP Gate", Description = "AKTIF", Duration = 2 })
    else
        setAllGateDuration(1.5)
        removeHooks()
        Library:Notify({ Title = "Instant TP Gate", Description = "NONAKTIF", Duration = 2 })
    end
end

-- Auto re-apply saat karakter/Map berubah
LP.CharacterAdded:Connect(function()
    task.wait(0.5)
    if _G.Ox.InstantTPGate.Enabled then
        setAllGateDuration(0)
        applyHooks()
    end
end)

workspace.ChildAdded:Connect(function(child)
    if child.Name == "Map" then
        task.wait(0.5)
        if _G.Ox.InstantTPGate.Enabled then
            setAllGateDuration(0)
            applyHooks()
        end
    end
end)

-- ==================================================
-- THIRD PERSON KILLER - PERSIS PANDU HUB
-- ==================================================

-- Fungsi helper untuk reset semua transparency
function ResetAllTransparency()
    for _, obj in pairs(Camera:GetChildren()) do
        if obj ~= LocalPlayer.Character then
            for _, part in pairs(obj:GetDescendants()) do
                if part:IsA("BasePart") or part:IsA("Decal") then
                    pcall(function()
                        part.LocalTransparencyModifier = 0
                        part.Transparency = 0
                    end)
                end
            end
        end
    end
end

-- Fungsi helper untuk set transparency dunia
function SetWorldTransparency(transparency)
    for _, obj in pairs(Camera:GetChildren()) do
        if obj ~= LocalPlayer.Character then
            for _, part in pairs(obj:GetDescendants()) do
                if part:IsA("BasePart") or part:IsA("Decal") then
                    pcall(function()
                        part.LocalTransparencyModifier = transparency
                        part.Transparency = transparency
                    end)
                end
            end
        end
    end
end

-- Fungsi helper untuk set transparency karakter
function SetCharacterTransparency(character, transparency)
    if not character then return end
    
    for _, obj in pairs(character:GetChildren()) do
        -- Handle Hat
        if obj.Name == "Hat" then
            for _, child in pairs(obj:GetDescendants()) do
                if child:IsA("BasePart") or child:IsA("Decal") then
                    pcall(function()
                        child.LocalTransparencyModifier = transparency
                        child.Transparency = transparency
                    end)
                end
            end
        end
        
        -- Handle Arm parts
        if obj:IsA("BasePart") and armParts[obj.Name] then
            pcall(function()
                obj.LocalTransparencyModifier = transparency
                obj.Transparency = transparency
            end)
        end
        
        -- Handle Weapon/Tool
        if obj:IsA("Tool") or obj:IsA("Model") then
            local descendants = obj:IsA("BasePart") and {obj} or obj:GetDescendants()
            for _, child in pairs(descendants) do
                if child:IsA("BasePart") or child:IsA("Decal") then
                    pcall(function()
                        child.LocalTransparencyModifier = transparency
                        child.Transparency = transparency
                    end)
                end
            end
        end
    end
end

-- Fungsi helper untuk reset karakter
function ResetCharacterTransparency(isKiller)
    if not LocalPlayer.Character then return end
    
    local transparency = 0
    if isKiller then
        for _, obj in pairs(LocalPlayer.Character:GetChildren()) do
            if obj.Name == "Hat" then
                for _, child in pairs(obj:GetDescendants()) do
                    if child:IsA("BasePart") or child:IsA("Decal") then
                        pcall(function()
                            child.LocalTransparencyModifier = 1
                            child.Transparency = 1
                        end)
                    end
                end
            end
            if obj:IsA("BasePart") and armParts[obj.Name] then
                pcall(function()
                    obj.LocalTransparencyModifier = 1
                    obj.Transparency = 1
                end)
            end
            if obj:IsA("Tool") or obj:IsA("Model") then
                local descendants = obj:IsA("BasePart") and {obj} or obj:GetDescendants()
                for _, child in pairs(descendants) do
                    if child:IsA("BasePart") or child:IsA("Decal") then
                        pcall(function()
                            child.LocalTransparencyModifier = 1
                            child.Transparency = 1
                        end)
                    end
                end
            end
        end
    else
        SetCharacterTransparency(LocalPlayer.Character, 0)
    end
end

-- Main Toggle Function (PERSIS PANDU HUB)
local function ToggleThirdPerson(state)
    local isPlayerKiller = (LocalPlayer.Team and LocalPlayer.Team.Name == "Killer")
    
    if state and not isPlayerKiller then
        Library:Notify({ Title = "Akses Ditolak", Description = "Fitur ini khusus Killer!", Duration = 3 })
        ThirdPerson.Enabled = false
        SafeSetToggle(ThirdPersonToggle, false)
        return
    end

    ThirdPerson.Enabled = state
    
    -- Matikan fitur
    if not state then
        if _G.Ox.State.ThirdPersonConn then
            _G.Ox.State.ThirdPersonConn:Disconnect()
            _G.Ox.State.ThirdPersonConn = nil
        end
        
        if isPlayerKiller then
            LocalPlayer.CameraMode = Enum.CameraMode.LockFirstPerson
            LocalPlayer.CameraMinZoomDistance = 0.5
            LocalPlayer.CameraMaxZoomDistance = 0.5
        else
            LocalPlayer.CameraMode = Enum.CameraMode.Classic
            LocalPlayer.CameraMinZoomDistance = 0.5
            LocalPlayer.CameraMaxZoomDistance = 128
        end
        
        ResetAllTransparency()
        if LocalPlayer.Character then
            ResetCharacterTransparency(false)
        end
        Library:Notify({ Title = "Third Person", Description = "NONAKTIF", Duration = 2 })
    else
        -- Aktifkan fitur
        LocalPlayer.CameraMode = Enum.CameraMode.Classic
        LocalPlayer.CameraMinZoomDistance = 7
        LocalPlayer.CameraMaxZoomDistance = 128
        
        if _G.Ox.State.ThirdPersonConn then
            _G.Ox.State.ThirdPersonConn:Disconnect()
            _G.Ox.State.ThirdPersonConn = nil
        end
        
        -- RenderStepped loop untuk menjaga kamera & transparency
        _G.Ox.State.ThirdPersonConn = RunService.RenderStepped:Connect(function()
            if not ThirdPerson.Enabled then return end
            
            -- Cek kembali apakah masih Killer
            if LocalPlayer.Team and LocalPlayer.Team.Name ~= "Killer" then
                ThirdPerson.Enabled = false
                if _G.Ox.State.ThirdPersonConn then
                    _G.Ox.State.ThirdPersonConn:Disconnect()
                    _G.Ox.State.ThirdPersonConn = nil
                end
                SafeSetToggle(ThirdPersonToggle, false)
                return
            end
            
            -- Set kamera setiap frame
            LocalPlayer.CameraMode = Enum.CameraMode.Classic
            LocalPlayer.CameraMinZoomDistance = 7
            LocalPlayer.CameraMaxZoomDistance = 128
            
            -- Set transparency untuk semua objek selain karakter player
            SetWorldTransparency(1)
            
            -- Set transparency untuk karakter player (visible)
            if LocalPlayer.Character then
                SetCharacterTransparency(LocalPlayer.Character, 0)
            end
        end)
        
        Library:Notify({ Title = "Third Person", Description = "AKTIF - Khusus Killer", Duration = 3 })
    end
end

-- Auto matikan saat bukan Killer
LocalPlayer:GetPropertyChangedSignal("Team"):Connect(function()
    if ThirdPerson.Enabled and LocalPlayer.Team and LocalPlayer.Team.Name ~= "Killer" then
        ToggleThirdPerson(false)
        SafeSetToggle(ThirdPersonToggle, false)
    end
end)

-- Reset saat karakter baru
LocalPlayer.CharacterAdded:Connect(function()
    task.wait(1)
end)

-- ==================================================
-- UNLIMITED VAULT (SEPERTI PANDU HUB)
-- ==================================================

function EnableUnlimitedVault()
    if _G.Ox.UnlimitedVaultEnabled then return end
    _G.Ox.UnlimitedVaultEnabled = true
    
    if _G.Ox.UnlimitedVaultConn then
        _G.Ox.UnlimitedVaultConn:Disconnect()
        _G.Ox.UnlimitedVaultConn = nil
    end
    
    for _, v in ipairs(CollectionService:GetTagged("Blocked")) do
        CollectionService:RemoveTag(v, "Blocked")
    end
    
    _G.Ox.UnlimitedVaultConn = CollectionService:GetInstanceAddedSignal("Blocked"):Connect(function(instance)
        CollectionService:RemoveTag(instance, "Blocked")
    end)
    
    Library:Notify({ Title = "Unlimited Vault", Description = "AKTIF", Duration = 3 })
end

function DisableUnlimitedVault()
    _G.Ox.UnlimitedVaultEnabled = false
    
    if _G.Ox.UnlimitedVaultConn then
        _G.Ox.UnlimitedVaultConn:Disconnect()
        _G.Ox.UnlimitedVaultConn = nil
    end
    
    Library:Notify({ Title = "Unlimited Vault", Description = "NONAKTIF", Duration = 2 })
end

local function ToggleUnlimitedVault(state)
    if state then
        EnableUnlimitedVault()
    else
        DisableUnlimitedVault()
    end
end

-- auto reapply
LocalPlayer.CharacterAdded:Connect(function()
    task.wait(1)
    if _G.Ox.UnlimitedVaultEnabled then
        for _, v in ipairs(CollectionService:GetTagged("Blocked")) do
            CollectionService:RemoveTag(v, "Blocked")
        end
    end
end)

-- ==================================================
-- ANTI SLOW VAULT (PERFECT VAULT) - SEPERTI PANDU HUB
-- ==================================================

task.spawn(function()
    if not game:IsLoaded() then
        game.Loaded:Wait()
    end

    local SurvivorAnimationsController = nil
    local maxAttempts = 10
    local attempts = 0

    while not SurvivorAnimationsController and attempts < maxAttempts do
        attempts = attempts + 1
        local success, result = pcall(function()
            local Modules = ReplicatedStorage:WaitForChild("Modules", 5)
            if not Modules then return nil end
            
            local Survivors = Modules:WaitForChild("Survivors", 5)
            if not Survivors then return nil end
            
            local Controller = Survivors:WaitForChild("SurvivorAnimationsController", 5)
            if not Controller then return nil end
            
            return require(Controller)
        end)

        if success and result then
            SurvivorAnimationsController = result
        else
            task.wait(1)
        end
    end

    if not SurvivorAnimationsController then
        return 
    end

    local original_isFacingStraightEnough = SurvivorAnimationsController._isFacingStraightEnough
    SurvivorAnimationsController._isFacingStraightEnough = function(self, part1, part2, maxAngle)
        if _G.Ox.Config.Surv_PerfectVault then
            self.characterspeed = 20
            return true, 0 
        end
        return original_isFacingStraightEnough(self, part1, part2, maxAngle)
    end

    local original_onVaultAnimation = SurvivorAnimationsController._onVaultAnimation
    SurvivorAnimationsController._onVaultAnimation = function(self, vaultPoint, isSprinting)
        if _G.Ox.Config.Surv_PerfectVault then
            return original_onVaultAnimation(self, vaultPoint, true)
        end
        return original_onVaultAnimation(self, vaultPoint, isSprinting)
    end
end)

-- ==================================================
-- FLOWSTATE NO CD (SEPERTI PANDU HUB)
-- ==================================================

local function setupFlowstateCharacter(char)
    if not char then return end
    
    char:SetAttribute("Flowstate", true)
    
    if _G.Ox.FlowstateNoCD.Connection then
        _G.Ox.FlowstateNoCD.Connection:Disconnect()
        _G.Ox.FlowstateNoCD.Connection = nil
    end
    
    _G.Ox.FlowstateNoCD.Connection = char:GetAttributeChangedSignal("Flowstate"):Connect(function()
        if not _G.Ox.FlowstateNoCD.Enabled then return end
        if char:GetAttribute("Flowstate") == false then
            task.wait(0.05)
            char:SetAttribute("Flowstate", true)
        end
    end)
end

local function ToggleFlowstateNoCD(state)
    _G.Ox.FlowstateNoCD.Enabled = state
    
    if _G.Ox.FlowstateNoCD.Connection then
        _G.Ox.FlowstateNoCD.Connection:Disconnect()
        _G.Ox.FlowstateNoCD.Connection = nil
    end
    
    if _G.Ox.FlowstateNoCD.CharAddedConn then
        _G.Ox.FlowstateNoCD.CharAddedConn:Disconnect()
        _G.Ox.FlowstateNoCD.CharAddedConn = nil
    end
    
    if not state then
        local char = LP.Character
        if char then
            char:SetAttribute("Flowstate", false)
        end
        Library:Notify({ Title = "Flowstate No CD", Description = "NONAKTIF", Duration = 2 })
        return
    end
    
    setupFlowstateCharacter(LP.Character)
    
    _G.Ox.FlowstateNoCD.CharAddedConn = LP.CharacterAdded:Connect(function(char)
        if not _G.Ox.FlowstateNoCD.Enabled then return end
        task.wait(1)
        setupFlowstateCharacter(char)
    end)
    
    Library:Notify({ Title = "Flowstate No CD", Description = "AKTIF", Duration = 2 })
end

-- ==================================================
-- NO FALL DAMAGE (SEPERTI PANDU HUB)
-- ==================================================

local FallEvent = ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("Mechanics"):WaitForChild("Fall")

local rawMT = getrawmetatable(game)
local oldNamecall = rawMT.__namecall
setreadonly(rawMT, false)

rawMT.__namecall = newcclosure(function(self, ...)
    local args = {...}
    local method = getnamecallmethod()

    if _G.Ox.NoFall.Enabled and self == FallEvent and (method == "FireServer" or method == "fireServer") then
        args[1] = 0 
        return oldNamecall(self, unpack(args))
    end

    return oldNamecall(self, ...)
end)

setreadonly(rawMT, true)

local function ToggleNoFall(state)
    _G.Ox.NoFall.Enabled = state
    Library:Notify({ Title = "No Fall Damage", Description = state and "AKTIF" or "NONAKTIF", Duration = 2 })
end

-- ==================================================
-- BYPASS EXIT GATE (SEPERTI PANDU HUB)
-- ==================================================

local function gatherGates()
    local gates = {}

    local map = workspace:FindFirstChild("Map")
    if not map then
        return gates
    end

    for _, obj in pairs(map:GetDescendants()) do
        if obj.Name == "Gate" then
            table.insert(gates, obj)
        end
    end

    return gates
end

local function ToggleBypassGate(state)
    _G.Ox.BypassGate.Enabled = state

    local gates = gatherGates()

    for _, gate in pairs(gates) do
        local leftGate = gate:FindFirstChild("LeftGate")
        local rightGate = gate:FindFirstChild("RightGate")

        local leftEnd = gate:FindFirstChild("LeftGate-end") or gate:FindFirstChild("LeftGate-end2")
        local rightEnd = gate:FindFirstChild("RightGate-end") or gate:FindFirstChild("RightGate-end2")

        local box = gate:FindFirstChild("Box")

        if state then
            if leftGate then
                leftGate.Transparency = 1
                leftGate.CanCollide = false
            end

            if rightGate then
                rightGate.Transparency = 1
                rightGate.CanCollide = false
            end

            if leftEnd then
                leftEnd.Transparency = 0
            end

            if rightEnd then
                rightEnd.Transparency = 0
            end

            if box then
                box.CanCollide = false
            end

        else
            if leftGate then
                leftGate.Transparency = 0
                leftGate.CanCollide = true
            end

            if rightGate then
                rightGate.Transparency = 0
                rightGate.CanCollide = true
            end

            if leftEnd then
                leftEnd.Transparency = 1
            end

            if rightEnd then
                rightEnd.Transparency = 1
            end

            if box then
                box.CanCollide = true
            end
        end
    end

    Library:Notify({ Title = "Bypass Gate", Description = state and "AKTIF" or "NONAKTIF", Duration = 2 })
end

-- Auto re-apply saat Map berubah
workspace.ChildAdded:Connect(function(child)
    if child.Name == "Map" then
        task.wait(1)
        if _G.Ox.BypassGate.Enabled then
            ToggleBypassGate(true)
        end
    end
end)

-- ==================================================
-- SELF HEAL (SEPERTI PANDU HUB)
-- ==================================================

local healAnimListener = nil

local function suppressHealingAnimation()
    if healAnimListener then return end
    local character = LP.Character
    if not character then return end
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if not humanoid then return end
    local animator = humanoid:FindFirstChildOfClass("Animator")
    if not animator then return end

    healAnimListener = animator.AnimationPlayed:Connect(function(activeTrack)
        if activeTrack.Animation and activeTrack.Animation.AnimationId:find("95836365038528") then
            activeTrack:Stop(0)
        end
    end)
end

local function restoreHealingAnimation()
    if healAnimListener then
        pcall(function() healAnimListener:Disconnect() end)
        healAnimListener = nil
    end
end

-- Loop Self Heal (seperti Pandu Hub)
task.spawn(function()
    while _G.Ox.ScriptAlive do
        task.wait(3)
        if not _G.Ox.ScriptAlive then break end
        if _G.Ox.SelfHeal.Enabled then
            if LP.Team and LP.Team.Name ~= "Killer" then
                local character = LP.Character
                if character then
                    local interactState = character:FindFirstChild("CheckInterractable")
                    local rootPart = character:FindFirstChild("HumanoidRootPart")
                    local humanoid = character:FindFirstChildOfClass("Humanoid")

                    local isBusy = false
                    if interactState then
                        if interactState:GetAttribute("isVaulting") 
                        or interactState:GetAttribute("isRepairing") 
                        or interactState:GetAttribute("isUnhooking") 
                        or interactState:GetAttribute("isHealing") 
                        or interactState:GetAttribute("isSliding") then
                            isBusy = true
                        end
                    end

                    if not isBusy and rootPart and humanoid and humanoid.Health < humanoid.MaxHealth then
                        pcall(function()
                            local remoteService = ReplicatedStorage:FindFirstChild("Remotes")
                            if remoteService and remoteService:FindFirstChild("Healing") then
                                remoteService.Healing.HealEvent:FireServer(rootPart, true)
                            end
                        end)
                    end
                end
            end
        end
    end
end)

-- Auto suppress healing animation saat karakter baru
LP.CharacterRemoving:Connect(function()
    restoreHealingAnimation()
end)

LP.CharacterAdded:Connect(function()
    task.wait(5)
    if _G.Ox.SelfHeal.Enabled then
        suppressHealingAnimation()
    end
end)

local function ToggleSelfHeal(state)
    _G.Ox.SelfHeal.Enabled = state
    
    if state then
        if LP.Team and LP.Team.Name == "Killer" then
            Library:Notify({ Title = "Self Heal", Description = "Kamu harus Survivor!", Duration = 3 })
            _G.Ox.SelfHeal.Enabled = false
            if SelfHealToggle then
             SafeSetToggle(SelfHealToggle, false)
            end
            return
        end
        suppressHealingAnimation()
        Library:Notify({ Title = "Self Heal", Description = "AKTIF (Tanpa Animasi)", Duration = 2 })
    else
        restoreHealingAnimation()
        Library:Notify({ Title = "Self Heal", Description = "NONAKTIF", Duration = 2 })
    end
end

-- ==================================================
-- KORLESS MORPH + INSTAN ESCAPE (SEPERTI PANDU HUB)
-- ==================================================

-- ===== APPLY KORLESS =====
local function ApplyKorless()
    local plr = LP

    local function Morph()
        repeat task.wait()
        until plr.Character
            and plr.Character:FindFirstChild("HumanoidRootPart")
            and plr.Character:FindFirstChild("Right Leg")

        task.wait(0.1)
        local char = plr.Character

        pcall(function()
            char.Head.Transparency = 1

            local face = char.Head:FindFirstChild("face")
            if face then
                face:Destroy()
            end

            char["Right Leg"].Transparency = 1

            local mesh = Instance.new("MeshPart")
            mesh.Name = "KorlessHead"
            mesh.Size = Vector3.new(0.85, 0.85, 0.85)
            mesh.CanCollide = false
            mesh.MeshId = "rbxassetid://902942096"
            mesh.TextureID = "rbxassetid://902843398"
            mesh.CFrame = char["Right Leg"].CFrame * CFrame.new(0, 0.7, 0)
            mesh.Parent = char

            local weld = Instance.new("WeldConstraint")
            weld.Part0 = char["Right Leg"]
            weld.Part1 = mesh
            weld.Parent = mesh
        end)
    end

    Morph()

    if _G.Ox.KorlessMorph.Connection then
        _G.Ox.KorlessMorph.Connection:Disconnect()
    end

    _G.Ox.KorlessMorph.Connection = plr.CharacterAdded:Connect(function()
        task.wait(1)
        Morph()
    end)
end

-- ===== INSTAN ESCAPE =====
local function teleportToFinishLine()
    local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
    if not root then return end
    
    local found = nil
    for _, obj in ipairs(workspace:GetDescendants()) do
        if string.lower(obj.Name) == "fininshline" and obj:IsA("BasePart") then
            found = obj
            break
        end
    end
    
    if not found then
        Library:Notify({ Title = "Instan Escape", Description = "Finish line tidak ditemukan!", Duration = 2 })
        return
    end
    
    root.CFrame = found.CFrame + Vector3.new(0, 5, 0)
    Library:Notify({ Title = "Instan Escape", Description = "Berhasil!", Duration = 2 })
end

-- ==================================================
-- DASHBOARD
-- ==================================================
local DashTab = Window:CreateTab("Dashboard", true, false)
local D_Page = DashTab:CreatePage("Dashboard")

local WelcomeCard = D_Page:CreateSection("Welcome")
WelcomeCard:AddProfile(
    "Hello, " .. maskUserName(LP.DisplayName),
    GetGreeting() .. " | @" .. maskUserName(LP.Name)
)

local ServerCard = D_Page:CreateSection("Server")
ServerCard:AddLabel("Informasi sesi kamu", {
    TextSize = 11,
    Color = Color3.fromRGB(130, 130, 145),
    Height = 16,
})
local RoleStat = ServerCard:AddStat("Role", GetRole())
local HpStat = ServerCard:AddStat("Health", GetHealth())
local PlrStat = ServerCard:AddStat("Players", GetPlayers())
local PingStat = ServerCard:AddStat("Ping", GetPing())

local ExecName, ExecVer = GetExecutor()
local ExecCard = D_Page:CreateSection("Executor")
ExecCard:AddLabel("Terdeteksi: dikenal", {
    TextSize = 11,
    Color = Color3.fromRGB(130, 130, 145),
    Height = 16,
})
ExecCard:AddLabel("Nama: " .. ExecName, { Height = 18 })
ExecCard:AddLabel("Versi: " .. ExecVer, { Height = 18 })
ExecCard:AddLabel("Tier: Oxio Hub Premium User", { Height = 18 })
ExecCard:AddLabel("HWID Locked", { Height = 18 })

task.spawn(function()
    while task.wait(1) do
        RoleStat.Set(GetRole())
        HpStat.Set(GetHealth())
        PlrStat.Set(GetPlayers())
        PingStat.Set(GetPing())
    end
end)

-- ==================================================
-- MAIN
-- ==================================================
local MainTab = Window:CreateTab("Main", false, false)
local M_Page = MainTab:CreatePage("Main")
local MovementCard = M_Page:CreateSection("Movement")

local AutoVaultToggle = MovementCard:AddToggle("Auto Vault", false, function(state)
    _G.Ox.autoVaultEnabled = state
    Library:Notify({ Title = "Auto Vault", Description = state and "AKTIF" or "NONAKTIF", Duration = 2 })
end)
AutoVaultToggle:AddKeyPicker("V")

-- AUTO PALLET SLIDE (seperti Pandu)
local AutoSlideToggle = MovementCard:AddToggle("Auto Pallet Slide", false, function(state)
    _G.Ox.autoPalletSlideEnabled = state
    Library:Notify({ Title = "Auto Pallet Slide", Description = state and "AKTIF" or "NONAKTIF", Duration = 2 })
end)
AutoSlideToggle:AddKeyPicker("S")

local AutoRunPCToggle = MovementCard:AddToggle("Auto Run PC", false, function(state)
    _G.Ox.AutoRun.PC = state
    if state then
        task.spawn(function()
            while _G.Ox.AutoRun.PC do
                VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.LeftShift, false, LP:GetMouse())
                task.wait(0.1)
            end
        end)
    else
        VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.LeftShift, false, LP:GetMouse())
    end
end)
AutoRunPCToggle:AddKeyPicker("LShift")

local AutoRunMobileToggle = MovementCard:AddToggle("Auto Run Mobile", false, function(state)
    _G.Ox.AutoRun.Mobile = state
    if state then StartAutoRunMobile() else StopAutoRunMobile() end
end)
AutoRunMobileToggle:AddKeyPicker("RShift")

local SpeedToggle = MovementCard:AddToggle("Enable Speed Boost", false, function(state)
    _G.Ox.Speed.Enabled = state
    Library:Notify({ Title = "Speed Boost", Description = state and "AKTIF" or "NONAKTIF", Duration = 2 })
end)
SpeedToggle:AddKeyPicker("V")

MovementCard:AddSlider("SpeedAmount", 0.1, 3, 0.1, function(v)
    _G.Ox.Speed.Value = v
end, { Increment = 0.1 })

-- ==================================================
-- CAMERA SETTINGS
-- ==================================================
local CameraCard = M_Page:CreateSection("Camera Settings")

-- STRETCH RES
local StretchToggle = CameraCard:AddToggle("Enable Stretch Res", false, function(state)
    ToggleStretch(state)
end)
StretchToggle:AddKeyPicker("Z")

CameraCard:AddSlider("Stretch Amount", 0.40, 1.00, 0.67, function(v)
    _G.Ox.StretchRes.Amount = v
end, { Increment = 0.01 })

-- THIRD PERSON KILLER
ThirdPersonToggle = CameraCard:AddToggle("Third Person (Killer)", false, function(state)
    ToggleThirdPerson(state)
end)
ThirdPersonToggle:AddKeyPicker("T")

CameraCard:AddLabel("Mengubah kamera ke belakang karakter (Killer Only)", {
    TextSize = 11,
    Color = Color3.fromRGB(130, 130, 145),
    Height = 16,
})

-- INFINITY ZOOM
local InfinityToggle = CameraCard:AddToggle("Infinity Zoom Out", false, function(state)
    ToggleInfinityZoom(state)
end)
InfinityToggle:AddKeyPicker("I")

CameraCard:AddLabel("Zoom Out tanpa batas", {
    TextSize = 11,
    Color = Color3.fromRGB(130, 130, 145),
    Height = 16,
})

-- CAMERA FOV
local FOVToggle = CameraCard:AddToggle("Camera FOV", false, function(state)
    ToggleCameraFOV(state)
end)
FOVToggle:AddKeyPicker("F")

CameraCard:AddSlider("FOV Value", 60, 120, 90, function(v)
    SetFOVValue(v)
end, { Increment = 0 })

CameraCard:AddLabel("Atur jarak pandang kamera (60-120)", {
    TextSize = 11,
    Color = Color3.fromRGB(130, 130, 145),
    Height = 16,
})

-- ==================================================
-- STREAMER MODE UI
-- ==================================================
local StreamerCard = M_Page:CreateSection("Streamer Mode")

local StreamerToggle = StreamerCard:AddToggle("Streamer Mode", false, function(state)
    ToggleStreamerMode(state)
end)
StreamerToggle:AddKeyPicker("H")

StreamerCard:AddLabel("Menyembunyikan nama player di UI", {
    TextSize = 11,
    Color = Color3.fromRGB(130, 130, 145),
    Height = 16,
})

local UtilityCard = M_Page:CreateSection("Utility")
local InvisToggle = UtilityCard:AddToggle("Invisibility [OP]", false, function(state)
    _G.Ox.Utility.InvisEnabled = state
    ToggleInvisibility(state)
end)
InvisToggle:AddKeyPicker("I")

local BandageToggle = UtilityCard:AddToggle("Fast Bandage", false, function(state)
    _G.Ox.Utility.BandageEnabled = state
    ToggleFastBandage(state)
end)
BandageToggle:AddKeyPicker("B")

UtilityCard:AddSlider("BandageSpeed", 10, 200, 50, function(v)
    _G.Ox.Utility.BandageSpeed = v
    if FastBandageAPI and FastBandageAPI.Config then
        FastBandageAPI.Config.PreClicksCount = v
    end
end, { Increment = 1 })

task.spawn(function()
    task.wait(2)
    LoadInvis()
    LoadFastBandage()
end)

-- ==================================================
-- COPY AVATAR / FAKE AVATAR (LOGIC PANDU HUB ASLI)
-- ==================================================
FakeAvatarSection = M_Page:CreateSection("Copy Avatar")

-- ==================================================
-- VARIABLES
-- ==================================================
Pandu_RANDOM_IDS = {978663613, 5261700291, 1846241644, 4993456331, 424866237, 4312175249}

Pandu_CURRENT_AVATAR = nil
Pandu_ORIGINAL_ID = LP.UserId
Pandu_FAVORITES = {}
Pandu_Applying = false
Pandu_RespawnConnection = nil
Pandu_SelectedFav = nil

-- ==================================================
-- DISPLAY PICTURE
-- ==================================================
Pandu_FakeThumbUserId = nil
Pandu_ThumbConn = nil
Pandu_ThumbPropConns = {}

function Pandu_ThumbUrl(userId, w, h)
    w = w or 150
    h = h or 150
    return string.format(
        "rbxthumb://type=AvatarHeadShot&id=%d&w=%d&h=%d",
        tonumber(userId) or 0, w, h
    )
end

function Pandu_ExtractThumbUserId(img)
    if type(img) ~= "string" or img == "" then return nil end
    return tonumber(img:match("rbxthumb://[^%s]*id=(%d+)"))
end

function Pandu_IsAvatarThumb(img)
    if type(img) ~= "string" then return false end
    return img:find("rbxthumb://", 1, true) ~= nil
        and (
            img:find("AvatarHeadShot", 1, true)
            or img:find("AvatarBust", 1, true)
            or img:find("type=Avatar", 1, true)
        )
end

function Pandu_IsOurCurrentThumb(obj)
    if not (obj:IsA("ImageLabel") or obj:IsA("ImageButton")) then
        return false
    end
    if obj:GetAttribute("PanduFakeThumb") == true then
        return true
    end
    local shown = Pandu_ExtractThumbUserId(obj.Image)
    if not shown or not Pandu_IsAvatarThumb(obj.Image) then
        return false
    end
    return shown == Pandu_ORIGINAL_ID
end

function Pandu_ForceSetThumb(obj, userId)
    if not obj or not userId then return end
    if not (obj:IsA("ImageLabel") or obj:IsA("ImageButton")) then return end
    if not Pandu_IsOurCurrentThumb(obj) then return end

    if not obj:GetAttribute("PanduOrigThumb") then
        obj:SetAttribute("PanduOrigThumb", obj.Image)
    end

    local w, h = tostring(obj.Image):match("w=(%d+).*h=(%d+)")
    local url = Pandu_ThumbUrl(userId, tonumber(w) or 150, tonumber(h) or 150)

    obj:SetAttribute("PanduFakeThumb", true)
    obj:SetAttribute("PanduFakeThumbId", tonumber(userId))
    if obj.Image ~= url then
        obj.Image = url
    end

    if Pandu_ThumbPropConns[obj] then return end
    Pandu_ThumbPropConns[obj] = obj:GetPropertyChangedSignal("Image"):Connect(function()
        if not Pandu_FakeThumbUserId or not obj.Parent then
            if Pandu_ThumbPropConns[obj] then
                Pandu_ThumbPropConns[obj]:Disconnect()
                Pandu_ThumbPropConns[obj] = nil
            end
            return
        end

        local shown = Pandu_ExtractThumbUserId(obj.Image)
        if shown and shown ~= Pandu_ORIGINAL_ID and shown ~= Pandu_FakeThumbUserId then
            obj:SetAttribute("PanduFakeThumb", nil)
            obj:SetAttribute("PanduFakeThumbId", nil)
            obj:SetAttribute("PanduOrigThumb", nil)
            if Pandu_ThumbPropConns[obj] then
                Pandu_ThumbPropConns[obj]:Disconnect()
                Pandu_ThumbPropConns[obj] = nil
            end
            return
        end

        local wantW, wantH = tostring(obj.Image):match("w=(%d+).*h=(%d+)")
        local want = Pandu_ThumbUrl(Pandu_FakeThumbUserId, tonumber(wantW) or 150, tonumber(wantH) or 150)
        if obj.Image ~= want then
            obj.Image = want
        end
    end)
end

function Pandu_RestoreThumb(obj)
    if not obj then return end
    if not (obj:IsA("ImageLabel") or obj:IsA("ImageButton")) then return end
    if obj:GetAttribute("PanduFakeThumb") ~= true then return end

    local orig = obj:GetAttribute("PanduOrigThumb")
    obj.Image = (type(orig) == "string" and orig ~= "") and orig or Pandu_ThumbUrl(Pandu_ORIGINAL_ID)

    obj:SetAttribute("PanduFakeThumb", nil)
    obj:SetAttribute("PanduFakeThumbId", nil)
    obj:SetAttribute("PanduOrigThumb", nil)

    if Pandu_ThumbPropConns[obj] then
        Pandu_ThumbPropConns[obj]:Disconnect()
        Pandu_ThumbPropConns[obj] = nil
    end
end

function Pandu_SweepSetThumb(userId)
    local roots = { CoreGui, LP:FindFirstChild("PlayerGui") }
    for _, root in ipairs(roots) do
        if root then
            for _, d in ipairs(root:GetDescendants()) do
                if Pandu_IsOurCurrentThumb(d) then
                    pcall(Pandu_ForceSetThumb, d, userId)
                end
            end
        end
    end
end

function Pandu_SweepRestoreThumb()
    local roots = { CoreGui, LP:FindFirstChild("PlayerGui") }
    for _, root in ipairs(roots) do
        if root then
            for _, d in ipairs(root:GetDescendants()) do
                pcall(Pandu_RestoreThumb, d)
            end
        end
    end
end

function Pandu_SetFakeDisplayPicture(userId)
    userId = tonumber(userId)
    if not userId then return end

    Pandu_FakeThumbUserId = userId
    Pandu_SweepSetThumb(userId)
    task.delay(0.4, function()
        if Pandu_FakeThumbUserId == userId then Pandu_SweepSetThumb(userId) end
    end)
    task.delay(1.2, function()
        if Pandu_FakeThumbUserId == userId then Pandu_SweepSetThumb(userId) end
    end)

    if Pandu_ThumbConn then
        Pandu_ThumbConn:Disconnect()
        Pandu_ThumbConn = nil
    end
    Pandu_ThumbConn = CoreGui.DescendantAdded:Connect(function(obj)
        if not Pandu_FakeThumbUserId then return end
        task.defer(function()
            if Pandu_IsOurCurrentThumb(obj) then
                pcall(Pandu_ForceSetThumb, obj, Pandu_FakeThumbUserId)
            end
        end)
    end)
end

function Pandu_ClearFakeDisplayPicture()
    Pandu_SweepRestoreThumb()
    task.delay(0.3, Pandu_SweepRestoreThumb)

    Pandu_FakeThumbUserId = nil
    if Pandu_ThumbConn then
        Pandu_ThumbConn:Disconnect()
        Pandu_ThumbConn = nil
    end
    for obj, conn in pairs(Pandu_ThumbPropConns) do
        pcall(function() conn:Disconnect() end)
        Pandu_ThumbPropConns[obj] = nil
    end
end

-- ==================================================
-- FAKE DISPLAY NAME
-- ==================================================
Pandu_FakeDisplayName = nil
Pandu_OriginalDisplayName = LP.DisplayName
Pandu_NameCharConn = nil

function Pandu_SetCharacterDisplayName(name)
    local char = LP.Character
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    if hum and name then
        hum.DisplayName = name
    end
end

function Pandu_LooksLikeOurName(text)
    if type(text) ~= "string" or text == "" then return false end
    return text == LP.Name or text == LP.DisplayName or text == Pandu_OriginalDisplayName
end

function Pandu_ApplyNameToLabel(obj, newName)
    if not obj or not newName then return end
    if not (obj:IsA("TextLabel") or obj:IsA("TextButton")) then return end
    if obj:GetAttribute("PanduFakeName") == true or Pandu_LooksLikeOurName(obj.Text) then
        if not obj:GetAttribute("PanduOrigName") then
            obj:SetAttribute("PanduOrigName", obj.Text)
        end
        obj:SetAttribute("PanduFakeName", true)
        obj.Text = newName
    end
end

function Pandu_RestoreNameLabel(obj)
    if not obj then return end
    if not (obj:IsA("TextLabel") or obj:IsA("TextButton")) then return end
    if obj:GetAttribute("PanduFakeName") ~= true then return end
    local orig = obj:GetAttribute("PanduOrigName")
    if type(orig) == "string" and orig ~= "" then
        obj.Text = orig
    else
        obj.Text = Pandu_OriginalDisplayName
    end
    obj:SetAttribute("PanduFakeName", nil)
    obj:SetAttribute("PanduOrigName", nil)
end

function Pandu_SweepNames(newName)
    if not newName then return end
    local roots = { CoreGui, LP:FindFirstChild("PlayerGui") }
    for _, root in ipairs(roots) do
        if root then
            for _, d in ipairs(root:GetDescendants()) do
                pcall(Pandu_ApplyNameToLabel, d, newName)
            end
        end
    end
end

function Pandu_SweepRestoreNames()
    local roots = { CoreGui, LP:FindFirstChild("PlayerGui") }
    for _, root in ipairs(roots) do
        if root then
            for _, d in ipairs(root:GetDescendants()) do
                pcall(Pandu_RestoreNameLabel, d)
            end
        end
    end
end

function Pandu_SetFakeDisplayName(name)
    if not name or name == "" then return end
    Pandu_FakeDisplayName = name
    Pandu_SetCharacterDisplayName(name)
    Pandu_SweepNames(name)

    task.delay(0.3, function()
        if Pandu_FakeDisplayName == name then
            Pandu_SetCharacterDisplayName(name)
            Pandu_SweepNames(name)
        end
    end)
    task.delay(1, function()
        if Pandu_FakeDisplayName == name then
            Pandu_SetCharacterDisplayName(name)
            Pandu_SweepNames(name)
        end
    end)

    if Pandu_NameCharConn then
        Pandu_NameCharConn:Disconnect()
        Pandu_NameCharConn = nil
    end
    Pandu_NameCharConn = LP.CharacterAdded:Connect(function(char)
        task.wait(0.5)
        if Pandu_FakeDisplayName then
            local hum = char:WaitForChild("Humanoid", 5)
            if hum then
                hum.DisplayName = Pandu_FakeDisplayName
            end
        end
    end)
end

function Pandu_ClearFakeDisplayName()
    Pandu_FakeDisplayName = nil
    Pandu_SetCharacterDisplayName(Pandu_OriginalDisplayName)
    Pandu_SweepRestoreNames()
    task.delay(0.3, Pandu_SweepRestoreNames)

    if Pandu_NameCharConn then
        Pandu_NameCharConn:Disconnect()
        Pandu_NameCharConn = nil
    end
end

-- ==================================================
-- LOAD ASSET (PANDU HUB ASLI)
-- ==================================================
function Pandu_LoadAssetKeKarakter(appearance, character, head)
    local meshItems = {}
    local bodyColors, shirt, pants, shirtGraphic = nil, nil, nil, nil
    local accessories = {}

    local function collectItems(parent)
        for _, item in pairs(parent:GetChildren()) do
            if item:IsA("CharacterMesh") then
                table.insert(meshItems, item)
            elseif item:IsA("BodyColors") then
                bodyColors = item
            elseif item:IsA("Shirt") then
                shirt = item
            elseif item:IsA("Pants") then
                pants = item
            elseif item:IsA("ShirtGraphic") then
                shirtGraphic = item
            elseif item:IsA("Accessory") then
                table.insert(accessories, item)
            elseif item:IsA("SpecialMesh") then
                table.insert(meshItems, item)
            elseif item:IsA("Decal") and item.Name == "face" then
                table.insert(meshItems, item)
            elseif item:IsA("Folder") or item:IsA("Model") then
                collectItems(item)
            end
        end
    end

    collectItems(appearance)

    if bodyColors then
        local existing = character:FindFirstChildOfClass("BodyColors")
            or character:FindFirstChild("Body Colors")
        if existing then existing:Destroy() end
        bodyColors:Clone().Parent = character
    end

    if shirt then
        local existing = character:FindFirstChildOfClass("Shirt")
        if existing then existing:Destroy() end
        shirt:Clone().Parent = character
    end

    if pants then
        local existing = character:FindFirstChildOfClass("Pants")
        if existing then existing:Destroy() end
        pants:Clone().Parent = character
    end

    if shirtGraphic then
        local existing = character:FindFirstChildOfClass("ShirtGraphic")
        if existing then existing:Destroy() end
        shirtGraphic:Clone().Parent = character
    end

    for _, mesh in pairs(meshItems) do
        if mesh:IsA("CharacterMesh") then
            pcall(function()
                mesh:Clone().Parent = character
            end)
        end
    end

    if head then
        for _, mesh in pairs(meshItems) do
            if mesh:IsA("SpecialMesh") then
                pcall(function()
                    local headMesh = head:FindFirstChildOfClass("SpecialMesh")
                    if headMesh then headMesh:Destroy() end
                    mesh:Clone().Parent = head
                end)
            elseif mesh:IsA("Decal") and mesh.Name == "face" then
                pcall(function()
                    local existingFace = head:FindFirstChild("face")
                    if existingFace then existingFace:Destroy() end
                    mesh:Clone().Parent = head
                end)
            end
        end
    end

    if #accessories > 0 then
        task.spawn(function()
            for _, accessory in pairs(accessories) do
                pcall(function()
                    local clone = accessory:Clone()
                    local handle = clone:FindFirstChild("Handle")
                    if handle then
                        handle.CanCollide = false
                        handle.Massless = true
                        local att = handle:FindFirstChildOfClass("Attachment")
                        if att then
                            local targetAtt = character:FindFirstChild(att.Name, true)
                            if targetAtt and targetAtt.Parent then
                                local weld = Instance.new("Weld")
                                weld.Part0 = handle
                                weld.Part1 = targetAtt.Parent
                                weld.C0 = att.CFrame
                                weld.C1 = targetAtt.CFrame
                                weld.Parent = handle
                            end
                        end
                        clone.Parent = character
                    else
                        clone.Parent = character
                    end
                end)
            end
        end)
    end
end

-- ==================================================
-- APPLY FAKE AVATAR
-- ==================================================
function Pandu_ApplyFakeAvaAppearance(userId)
    local character = LP.Character
    if not character or not userId then return false end

    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if not humanoid then return false end

    local success, appearance = pcall(function()
        return Players:GetCharacterAppearanceAsync(userId)
    end)
    if not success or not appearance then
        Library:Notify({ Title = "Error", Description = "Gagal load appearance", Duration = 3 })
        return false
    end

    for _, obj in pairs(character:GetChildren()) do
        if obj:IsA("Accessory") or obj:IsA("Shirt") or obj:IsA("Pants")
            or obj:IsA("ShirtGraphic") or obj:IsA("BodyColors")
            or obj:IsA("CharacterMesh") or obj.Name == "Body Colors" then
            obj:Destroy()
        end
    end

    local head = character:FindFirstChild("Head")
    if head then
        local headMesh = head:FindFirstChildOfClass("SpecialMesh")
        if headMesh then headMesh:Destroy() end
        local faceMesh = head:FindFirstChild("face")
        if faceMesh then faceMesh:Destroy() end
    end

    local appearanceInfo = nil
    local ok, info = pcall(function()
        return Players:GetCharacterAppearanceInfoAsync(userId)
    end)
    if ok and info then appearanceInfo = info end

    if appearanceInfo and appearanceInfo.scales then
        for scaleName, val in pairs(appearanceInfo.scales) do
            local s = humanoid:FindFirstChild(scaleName)
            if not s then
                s = Instance.new("NumberValue")
                s.Name = scaleName
                s.Parent = humanoid
            end
            s.Value = val
        end
    end

    task.wait(0.2)
    Pandu_LoadAssetKeKarakter(appearance, character, head)

    if appearanceInfo and appearanceInfo.assets and head then
        task.spawn(function()
            for _, asset in pairs(appearanceInfo.assets) do
                if asset.assetType and asset.assetType.name == "Face" then
                    local existingFace = head:FindFirstChild("face")
                    if existingFace then existingFace:Destroy() end
                    pcall(function()
                        local f = Instance.new("Decal")
                        f.Name = "face"
                        f.Texture = "rbxassetid://" .. asset.id
                        f.Face = Enum.NormalId.Front
                        f.Parent = head
                    end)
                    break
                end
            end
        end)
    end

    pcall(function() appearance:Destroy() end)
    return true
end

-- ==================================================
-- RESOLVE + APPLY
-- ==================================================
function Pandu_ResolveUser(input)
    local txt = tostring(input):gsub("%s+", ""):gsub("^@", "")
    local id = tonumber(txt)
    if id then
        local ok, name = pcall(function()
            return Players:GetNameFromUserIdAsync(id)
        end)
        if ok and name then return id, name end
    else
        local ok, uid = pcall(function()
            return Players:GetUserIdFromNameAsync(txt)
        end)
        if ok and uid then
            local ok2, name = pcall(function()
                return Players:GetNameFromUserIdAsync(uid)
            end)
            if ok2 and name then return uid, name end
        end
    end
    return nil, nil
end

function Pandu_ApplyAvatar(useridOrName)
    if Pandu_Applying then return end
    Pandu_Applying = true

    task.spawn(function()
        local id, name = Pandu_ResolveUser(useridOrName)
        if not id then
            Library:Notify({ Title = "Error", Description = "User tidak ditemukan", Duration = 3 })
            Pandu_Applying = false
            return
        end

        Pandu_CURRENT_AVATAR = { id = id, name = name }

        if Pandu_RespawnConnection then
            Pandu_RespawnConnection:Disconnect()
            Pandu_RespawnConnection = nil
        end

        local ok = false
        if LP.Character then
            ok = Pandu_ApplyFakeAvaAppearance(id)
        end

        Pandu_SetFakeDisplayPicture(id)
        Pandu_SetFakeDisplayName(name)

        Pandu_RespawnConnection = LP.CharacterAdded:Connect(function()
            task.wait(1)
            if Pandu_CURRENT_AVATAR then
                Pandu_ApplyFakeAvaAppearance(Pandu_CURRENT_AVATAR.id)
                Pandu_SetFakeDisplayPicture(Pandu_CURRENT_AVATAR.id)
                Pandu_SetFakeDisplayName(Pandu_CURRENT_AVATAR.name)
            end
        end)

        Library:Notify({ 
            Title = "Copy Avatar", 
            Description = ok and ("Berhasil copy " .. name) or ("Gagal apply " .. name),
            Duration = 3 
        })
        Pandu_Applying = false
    end)
end

-- ==================================================
-- FAVORITES
-- ==================================================
function Pandu_LoadFavorites()
    Pandu_FAVORITES = {}
    pcall(function()
        if not isfolder("PanduCopyAvatar") then makefolder("PanduCopyAvatar") end
        if isfile("PanduCopyAvatar/favorites.json") then
            Pandu_FAVORITES = HttpService:JSONDecode(readfile("PanduCopyAvatar/favorites.json"))
        end
    end)
end

function Pandu_SaveFavorites()
    pcall(function()
        if not isfolder("PanduCopyAvatar") then makefolder("PanduCopyAvatar") end
        writefile("PanduCopyAvatar/favorites.json", HttpService:JSONEncode(Pandu_FAVORITES))
    end)
end
Pandu_LoadFavorites()

-- ==================================================
-- RESET AVATAR
-- ==================================================
function Pandu_ResetAvatar()
    Pandu_CURRENT_AVATAR = nil
    if Pandu_RespawnConnection then
        Pandu_RespawnConnection:Disconnect()
        Pandu_RespawnConnection = nil
    end

    Pandu_ClearFakeDisplayPicture()
    Pandu_ClearFakeDisplayName()

    task.spawn(function()
        local ok = Pandu_ApplyFakeAvaAppearance(Pandu_ORIGINAL_ID)
        Library:Notify({ 
            Title = "Reset Avatar", 
            Description = ok and "Avatar direset" or "Gagal reset",
            Duration = 3 
        })
    end)
end

-- ==================================================
-- DAFTAR AVATAR UNTUK DROPDOWN
-- ==================================================
Pandu_AvatarList = {
    "Self Avatar", "Random 1", "Random 2", "Random 3", "Random 4", "Random 5", 
    "Random 6", "Random 7", "WoozyNate", "Nicholas", "yvlyf", "traevp", "J0LLY", 
    "LucashDev", "CEOofIsaac", "Stealthy", "Wildes", "Talon", "Relukt", 
    "Sammy", "Diesel", "S4ans03", "Aura", "iJava", "White Guy", 
    "Purple King", "Mpruyyy"
}

Pandu_AvatarIDs = {
    ["Self Avatar"] = LP.UserId, 
    ["Random 1"] = 2888298851, 
    ["Random 2"] = 10074747755,
    ["Random 3"] = 5209567453, 
    ["Random 4"] = 8991982843, 
    ["Random 5"] = 5796319029, 
    ["Random 6"] = 9744452117,
    ["Random 7"] = 8476755006, 
    ["WoozyNate"] = 146089324, 
    ["Nicholas"] = 909635, 
    ["yvlyf"] = 181751703,
    ["traevp"] = 471607078, 
    ["J0LLY"] = 1073847038, 
    ["LucashDev"] = 2525651744,
    ["CEOofIsaac"] = 63238912, 
    ["Stealthy"] = 56602747, 
    ["Wildes"] = 40397833,
    ["Talon"] = 75974130, 
    ["Relukt"] = 65042011, 
    ["Sammy"] = 2678001507,
    ["Diesel"] = 9123921576, 
    ["S4ans03"] = 35439794, 
    ["Aura"] = 2275806428,
    ["iJava"] = 276557820, 
    ["White Guy"] = 8843268357, 
    ["Purple King"] = 9070758608,
    ["Mpruyyy"] = 8340163775
}

function Pandu_GetFavoritesList()
    local list = {}
    for _, fav in ipairs(Pandu_FAVORITES) do
        table.insert(list, fav.name)
    end
    return list
end

-- ==================================================
-- UI OXIO HUB
-- ==================================================
Pandu_SelectedAvatarOption = "Self Avatar"

-- DROPDOWN PILIH AVATAR
FakeAvatarSection:AddDropdown("Pilih Avatar", Pandu_AvatarList, false, function(option)
    Pandu_SelectedAvatarOption = option
end)

-- TEXTBOX USERNAME
Pandu_UsernameInput = ""
FakeAvatarSection:AddTextbox("Username / ID", "Masukkan username atau ID...", function(input)
    Pandu_UsernameInput = input:gsub("^@", "")
end)

-- BUTTON APPLY
FakeAvatarSection:AddButton("Apply Avatar", function()
    if Pandu_UsernameInput ~= "" then
        Pandu_ApplyAvatar(Pandu_UsernameInput)
    elseif Pandu_SelectedAvatarOption and Pandu_SelectedAvatarOption ~= "" then
        local id = Pandu_AvatarIDs[Pandu_SelectedAvatarOption]
        if id then
            Pandu_ApplyAvatar(id)
        else
            Library:Notify({ Title = "Error", Description = "Pilih avatar atau masukkan username!", Duration = 3 })
        end
    else
        Library:Notify({ Title = "Error", Description = "Pilih avatar atau masukkan username!", Duration = 3 })
    end
end)

-- BUTTON RANDOM
FakeAvatarSection:AddButton("Random Avatar", function()
    Pandu_ApplyAvatar(Pandu_RANDOM_IDS[math.random(1, #Pandu_RANDOM_IDS)])
end)

-- BUTTON RESET
FakeAvatarSection:AddButton("Reset Avatar", function()
    Pandu_ResetAvatar()
end)

-- BUTTON ADD FAVORITE
FakeAvatarSection:AddButton("Add to Favorite", function()
    if not Pandu_CURRENT_AVATAR then
        Library:Notify({ Title = "Error", Description = "Tidak ada avatar", Duration = 3 })
        return
    end
    for _, v in ipairs(Pandu_FAVORITES) do
        if tonumber(v.id) == tonumber(Pandu_CURRENT_AVATAR.id) then
            Library:Notify({ Title = "Info", Description = "Sudah ada di favorite", Duration = 2 })
            return
        end
    end
    table.insert(Pandu_FAVORITES, { id = Pandu_CURRENT_AVATAR.id, name = Pandu_CURRENT_AVATAR.name })
    Pandu_SaveFavorites()
    Pandu_FavDropdown:SetValues(Pandu_GetFavoritesList())
    Library:Notify({ Title = "Favorite", Description = "Ditambahkan ke favorite", Duration = 2 })
end)

-- DROPDOWN FAVORITES
Pandu_FavDropdown = FakeAvatarSection:AddDropdown("Favorites", Pandu_GetFavoritesList(), false, function(option)
    for _, fav in ipairs(Pandu_FAVORITES) do
        if fav.name == option then
            Pandu_SelectedFav = fav.id
            Pandu_ApplyAvatar(fav.id)
            break
        end
    end
end)

-- BUTTON DELETE FAVORITE
FakeAvatarSection:AddButton("Delete Favorite", function()
    if not Pandu_SelectedFav then
        Library:Notify({ Title = "Error", Description = "Pilih favorite dulu", Duration = 3 })
        return
    end
    for i, v in ipairs(Pandu_FAVORITES) do
        if tonumber(v.id) == tonumber(Pandu_SelectedFav) then
            table.remove(Pandu_FAVORITES, i)
            break
        end
    end
    Pandu_SaveFavorites()
    Pandu_FavDropdown:SetValues(Pandu_GetFavoritesList())
    Pandu_SelectedFav = nil
    Library:Notify({ Title = "Favorite", Description = "Favorite dihapus", Duration = 2 })
end)

-- ==================================================
-- AUTO REAPPLY SAAT RESPAWN
-- ==================================================
LP.CharacterAdded:Connect(function(char)
    task.wait(1)
    if Pandu_CURRENT_AVATAR then
        Pandu_ApplyFakeAvaAppearance(Pandu_CURRENT_AVATAR.id)
        Pandu_SetFakeDisplayPicture(Pandu_CURRENT_AVATAR.id)
        Pandu_SetFakeDisplayName(Pandu_CURRENT_AVATAR.name)
    end
end)

-- FLY GUI
UtilityCard:AddButton("Fly GUI", function()
    local found = false
    for _, v in pairs(game.CoreGui:GetChildren()) do
        if v.Name:lower():find("fly") then found = true end
    end
    if not found then
        loadstring(game:HttpGet("https://raw.githubusercontent.com/XNEOFF/FlyGuiV3/main/FlyGuiV3.txt"))()
        Library:Notify({ Title = "Fly GUI", Description = "Fly GUI Dimuat", Duration = 3 })
    else
        Library:Notify({ Title = "Fly GUI", Description = "Fly GUI sudah terbuka", Duration = 2 })
    end
end)

-- ==================================================
-- FAKE PERKS UI
-- ==================================================
local FakePerksCard = M_Page:CreateSection("Fake Perks")

QR_Toggle = FakePerksCard:AddToggle("Quick Recovery", false, function(state)
    ToggleQuickRecovery(state)
end)
QR_Toggle:AddKeyPicker("Q")

PL_Toggle = FakePerksCard:AddToggle("Perfect Landing", false, function(state)
    TogglePerfectLanding(state)
end)
PL_Toggle:AddKeyPicker("P")

FS_Toggle = FakePerksCard:AddToggle("Flowstate", false, function(state)
    ToggleFlowstate(state)
end)
FS_Toggle:AddKeyPicker("F")

FakePerksCard:AddSlider("Perk Cooldown", 0, 180, 10, function(v)
    _G.Ox.FakePerks.PerkCooldown = v
end, { Increment = 1 })

-- ==================================================
-- SURVIVOR
-- ==================================================
local SurvivorTab = Window:CreateTab("Survivor", false, false)
local S_Page = SurvivorTab:CreatePage("Survivor")
local GenCard = S_Page:CreateSection("Generator Settings")

SkillCheckToggle = GenCard:AddToggle("Auto Skill Check", false, function(state)
    _G.Ox.Auto.SkillCheck = state
    if state then
        startSkillCheck()
    else
        if _G.Ox.Connections.SkillHeartbeat then
            _G.Ox.Connections.SkillHeartbeat:Disconnect()
            _G.Ox.Connections.SkillHeartbeat = nil
        end
    end
end)
SkillCheckToggle:AddKeyPicker("F")

GenCard:AddDropdown("Skill Check Mode", {
    "Legit", "Instant", "Normal", "Random"
}, false, function(mode)
    _G.Ox.Auto.SkillCheckMode = mode
    _G.Ox.State.RandomMode_IsNormal = false
end)

GenCard:AddDropdown("Gen Boost Mode", {
    "Manual Repair",
    "Auto Repair",
}, false, function(Value)
    _G.Ox.BypassGenMode = Value
end)

GenBoostToggle = GenCard:AddToggle("Gen Boost (Multi-Repair)", false, function(Value)
    _G.Ox.BypassToggleValue = Value
    _G.Ox.BypassGenEnabled = Value
    _G.Ox.BypassButton.Visible = Value and UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled
    if not Value then
        _G.Ox.ProcessedGens = {}
        _G.Ox.AutoRepairEnabled = false
        _G.Ox.AutoCurrentGenModel = nil
        if _G.Ox.AutoRepairThread then
            task.cancel(_G.Ox.AutoRepairThread)
            _G.Ox.AutoRepairThread = nil
        end
        _G.Ox.StopAutoRepair()
    end
end)
GenBoostToggle:AddKeyPicker("G")

local AbilityCard = S_Page:CreateSection("Survivor Ability")

-- INSTANT TP GATE
InstantTPToggle = AbilityCard:AddToggle("Instant TP Gate", false, function(state)
    ToggleInstantTPGate(state)
end)
InstantTPToggle:AddKeyPicker("None")

-- UNLIMITED VAULT
UnlimitedVaultToggle = AbilityCard:AddToggle("Unlimited Vault", false, function(state)
    ToggleUnlimitedVault(state)
end)
UnlimitedVaultToggle:AddKeyPicker("None")

-- ANTI SLOW VAULT
AntiSlowVaultToggle = AbilityCard:AddToggle("Anti Slow Vault", false, function(state)
    _G.Ox.Config.Surv_PerfectVault = state
    Library:Notify({ Title = "Anti Slow Vault", Description = state and "AKTIF" or "NONAKTIF", Duration = 2 })
end)
AntiSlowVaultToggle:AddKeyPicker("None")

-- FLOWSTATE NO CD
FlowstateToggle = AbilityCard:AddToggle("Flowstate No CD", false, function(state)
    ToggleFlowstateNoCD(state)
end)
FlowstateToggle:AddKeyPicker("None")

-- NO FALL DAMAGE
NoFallToggle = AbilityCard:AddToggle("No Fall Damage", false, function(state)
    ToggleNoFall(state)
end)
NoFallToggle:AddKeyPicker("None")

-- BYPASS EXIT GATE
BypassGateToggle = AbilityCard:AddToggle("Bypass Exit Gate", false, function(state)
    ToggleBypassGate(state)
end)
BypassGateToggle:AddKeyPicker("None")

SelfHealToggle = AbilityCard:AddToggle("Self Heal", false, function(state)
    ToggleSelfHeal(state)
end)
SelfHealToggle:AddKeyPicker("None")

AbilityCard:AddLabel("Heal diri sendiri tanpa animasi", {
    TextSize = 11,
    Color = Color3.fromRGB(130, 130, 145),
    Height = 16,
})

AbilityCard:AddButton("Apply Korless", function()
    ApplyKorless()
    Library:Notify({ Title = "Korless", Description = "Korless Morph Applied!", Duration = 3 })
end)

AbilityCard:AddButton("Instan Escape", function()
    teleportToFinishLine()
end)

local ParryCard = S_Page:CreateSection("Auto Parry")

local AutoParryToggle = ParryCard:AddToggle("Auto Parry", false, function(state)
    _G.Ox.ParryConfig.AutoParry = state
    Library:Notify({ Title = "Auto Parry", Description = state and "AKTIF" or "NONAKTIF", Duration = 2 })
end)
AutoParryToggle:AddKeyPicker("None")

local CustomParryToggle = ParryCard:AddToggle("Auto Parry Custom GUI", false, function(state)
    if state then
        if not _G.Ox.AutoParryCustom.Gui then CreateCustomParryGUI() end
        _G.Ox.AutoParryCustom.Gui.Enabled = true
        _G.Ox.AutoParryCustom.GuiVisible = true
        UpdateCustomParryGUI(_G.Ox.ParryConfig.AutoParry)
        Library:Notify({ Title = "Auto Parry GUI", Description = "Gui Dimuat", Duration = 2 })
    else
        if _G.Ox.ParryConfig.AutoParry then
            _G.Ox.ParryConfig.AutoParry = false
            _G.Ox.AutoParryCustom.IsActive = false
            SafeSetToggle(AutoParryToggle, false)
        end
        if _G.Ox.AutoParryCustom.Gui then
            _G.Ox.AutoParryCustom.Gui.Enabled = false
            _G.Ox.AutoParryCustom.GuiVisible = false
        end
        Library:Notify({ Title = "Auto Parry GUI", Description = "Gui Disembunyikan", Duration = 2 })
    end
end)

ParryCard:AddToggle("Safety Parry", false, function(state)
    _G.Ox.ParryConfig.ParrySafety = state
end)

ParryCard:AddToggle("Aggressive Mode", false, function(state)
    _G.Ox.ParryConfig.ParryAggressive = state
end)

ParryCard:AddToggle("ESP Range Circle", true, function(state)
    _G.Ox.ParryConfig.ParryCircle = state
end)

ParryCard:AddSlider("Parry Radius", 5, 25, 15, function(v)
    _G.Ox.ParryConfig.ParryRadius = v
end, { Increment = 1 })

ParryCard:AddSlider("Face Sensitivity", -10, 10, 7, function(v)
    _G.Ox.ParryConfig.ParryFace = v / 10
end, { Increment = 1 })

ParryCard:AddToggle("Auto Crouch (Dodge S1)", false, function(state)
    _G.Ox.ParryConfig.AutoCrouch = state
end)

ParryCard:AddButton("Fake Parry GUI", function()
    local success, err = pcall(function()
        loadstring(game:HttpGet("https://pastefy.app/tz2VGaIN/raw"))()
    end)
    if success then
        Library:Notify({ Title = "Fake Parry", Description = "GUI Fake Parry berhasil dimuat!", Duration = 3 })
    else
        Library:Notify({ Title = "Error", Description = "Gagal memuat Fake Parry GUI!", Duration = 3 })
    end
end)

-- ===== SURVIVOR AIMBOT UI =====
local AimbotCard = S_Page:CreateSection("Survivor Aimbot")

local AimbotToggle = AimbotCard:AddToggle("Enable Aimbot", false, function(state)
    _G.Ox.Config.Surv_Aimbot_Enabled = state
    if not state then
        if _G.Ox.survivorAimbotHighlight then _G.Ox.survivorAimbotHighlight.Parent = nil end
        if _G.Ox.SurvivorFOVFrame then _G.Ox.SurvivorFOVFrame.Visible = false end
    end
    Library:Notify({
        Title = "Survivor Aimbot",
        Description = state and "AKTIF" or "NONAKTIF",
        Duration = 2
    })
end)
AimbotToggle:AddKeyPicker("SurvivorAimbotKey", {
    Default = "None",
    Text = "Toggle Aimbot",
    Mode = "Toggle",
    Callback = function(Value)
        _G.Ox.Config.Surv_Aimbot_Enabled = Value
        if not Value then
            if _G.Ox.survivorAimbotHighlight then _G.Ox.survivorAimbotHighlight.Parent = nil end
            if _G.Ox.SurvivorFOVFrame then _G.Ox.SurvivorFOVFrame.Visible = false end
        end
    end
})

AimbotCard:AddToggle("Show FOV Circle", true, function(state)
    _G.Ox.Config.Surv_Aimbot_ShowFOV = state
    if not state and _G.Ox.SurvivorFOVFrame then
        _G.Ox.SurvivorFOVFrame.Visible = false
    end
end)

AimbotCard:AddSlider("FOV Radius", 30, 500, 150, function(v)
    _G.Ox.Config.Surv_Aimbot_Radius = v
end, { Increment = 1 })

AimbotCard:AddSlider("Max Distance", 50, 1000, 300, function(v)
    _G.Ox.Config.Surv_Aimbot_MaxDist = v
end, { Increment = 10 })

AimbotCard:AddSlider("Smoothness", 0.1, 1.0, 0.5, function(v)
    _G.Ox.Config.Surv_Aimbot_Smoothness = v
end, { Increment = 0.05 })

AimbotCard:AddSlider("Predict Offset", 0, 0.1, 0.01, function(v)
    _G.Ox.Config.Surv_Aimbot_Predict = v
end, { Increment = 0.001 })

AimbotCard:AddDropdown("Target Part", {
    "Torso", "Head", "Root"
}, false, function(mode)
    _G.Ox.Config.Surv_Aimbot_TargetPart = mode
end)

AimbotCard:AddLabel("Aktif saat tombol di-hold (RMB di PC / attack di HP)", {
    TextSize = 11,
    Color = Color3.fromRGB(130, 130, 145),
    Height = 16,
})

-- ==================================================
-- SILENT AIM PISTOL (TWIST OF FATE) - PERSIS PANDU
-- ==================================================
do 
    local ToFEvent = ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("Items")["Twist of Fate"].Fire
    local silentAimEnabled = false
    local silentAimPredict = false
    local laserEnabled = true
    local wallcheckEnabled = false
    local isAiming = false
    local Connection = nil
    local LaserBeam = nil
    local silentAimGui = nil
    local targetMode = "Killer"
    local savedUIPos = UDim2.new(0.5, -120, 0, 110)
    
    local velocityCache = {}
    local PREDICT_SETTINGS = {
        PredictionEfficiency = 0.85, 
        LerpSmoothness = 0.4,
        EnableJitter = false,
        MaxJitterStuds = 0,
    }

    function getGunObject()
        local char = LocalPlayer.Character
        if not char then return nil end
        local baseToF = char:FindFirstChild("Twist of Fate", true)
        if not baseToF then return nil end
        local rightArm = baseToF:FindFirstChild("Right Arm")
        if rightArm then
            local gunPart = rightArm:FindFirstChild("gun")
            if gunPart then return gunPart end
            local emperorGun = rightArm:FindFirstChild("EmperorGun")
            if emperorGun then return emperorGun end
        end
        return baseToF
    end

    function isTargetVisible(originPos, torsoPos, targetCharacter)
        local direction = torsoPos - originPos
        local distance = direction.Magnitude
        if distance < 0.1 then return true end

        local rayParams = RaycastParams.new()
        rayParams.FilterType = Enum.RaycastFilterType.Exclude

        local excludeList = {}
        local localChar = LocalPlayer.Character
        if localChar then
            table.insert(excludeList, localChar)
        end
        if targetCharacter and targetCharacter ~= localChar then
            table.insert(excludeList, targetCharacter)
        end
        if LaserBeam then
            table.insert(excludeList, LaserBeam)
        end

        rayParams.FilterDescendantsInstances = excludeList

        local result = workspace:Raycast(originPos, direction.Unit * distance, rayParams)
        return result == nil
    end

    local SCPCache = {}
    local SCPCacheTimer = 0
    
    function GetSCPs()
        if tick() - SCPCacheTimer < 0.5 then return SCPCache end
            
        local newTargets = {}
        local mapFolder = workspace:FindFirstChild("Map")
        
        if mapFolder then
            for _, container in pairs(mapFolder:GetDescendants()) do
                if container:IsA("Model") then
                    local attributes = container:GetAttributes()
                    
                    if container:GetAttribute("CorpseCreated0492") or next(attributes) ~= nil then
                        local root = container:FindFirstChild("HumanoidRootPart")
                        if root then 
                            table.insert(newTargets, root) 
                        end
                    end
                end
            end
        end
        
        SCPCache = newTargets
        SCPCacheTimer = tick()
        return SCPCache
    end
    
    local function getTargetVelocity(targetCharacter)
        local rootPart = targetCharacter:FindFirstChild("HumanoidRootPart")
        if not rootPart then return Vector3.zero end

        local currentPos = rootPart.Position
        local cached = velocityCache[targetCharacter]
        
        if cached then
            local timeDelta = tick() - cached.time
            if timeDelta > 0.001 and timeDelta < 0.1 then
                local rawVel = (currentPos - cached.pos) / timeDelta
                local smoothVel = cached.vel:Lerp(rawVel, PREDICT_SETTINGS.LerpSmoothness)
                velocityCache[targetCharacter] = {
                    pos = currentPos,
                    vel = smoothVel,
                    time = tick()
                }
                return smoothVel
            end
        end

        velocityCache[targetCharacter] = {
            pos = currentPos,
            vel = rootPart.Velocity,
            time = tick()
        }
        return rootPart.Velocity
    end

    local function GetPredictedToFPosition(originPos, targetPart, targetCharacter, bulletSpeed)
        bulletSpeed = bulletSpeed or 500
        local targetPos = targetPart.Position

        local distance = (targetPos - originPos).Magnitude
        if distance < 8 then
            return targetPos
        end

        local targetVel = getTargetVelocity(targetCharacter)
        local relativePos = targetPos - originPos

        local a = targetVel:Dot(targetVel) - (bulletSpeed * bulletSpeed)
        local b = 2 * relativePos:Dot(targetVel)
        local c = relativePos:Dot(relativePos)

        local t = nil
        if math.abs(a) < 0.01 then
            if math.abs(b) > 0.01 then t = -c / b end
        else
            local discriminant = b * b - 4 * a * c
            if discriminant >= 0 then
                local sqrtD = math.sqrt(discriminant)
                local t1 = (-b - sqrtD) / (2 * a)
                local t2 = (-b + sqrtD) / (2 * a)
                if t1 > 0.001 then t = t1 elseif t2 > 0.001 then t = t2 end
            end
        end

        local finalPredictedPos = targetPos
        if t then
            local nerfedVelocity = targetVel * PREDICT_SETTINGS.PredictionEfficiency
            finalPredictedPos = targetPos + (nerfedVelocity * t)

            if PREDICT_SETTINGS.EnableJitter then
                local jitter = Vector3.new(
                    (math.random() - 0.5) * PREDICT_SETTINGS.MaxJitterStuds,
                    (math.random() - 0.5) * (PREDICT_SETTINGS.MaxJitterStuds / 2),
                    (math.random() - 0.5) * PREDICT_SETTINGS.MaxJitterStuds
                )
                finalPredictedPos = finalPredictedPos + jitter
            end
        end

        return finalPredictedPos
    end

    function getTargetPosition()
        local cam = workspace.CurrentCamera
        local gunObj = getGunObject()
        local char = LocalPlayer.Character
        if not (gunObj and char) then return nil, nil, nil, nil end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if not hrp then return nil, nil, nil, nil end

        local myPos = hrp.Position
        local originPos
        if char:GetAttribute("IsCarried") then
            originPos = char.HumanoidRootPart.Position + (char.HumanoidRootPart.CFrame.LookVector * 2)
        else
            pcall(function()
                originPos = gunObj:IsA("BasePart") and gunObj.Position 
                            or gunObj:FindFirstChildOfClass("BasePart") and gunObj:FindFirstChildOfClass("BasePart").Position
            end)
            originPos = originPos or Vector3.new(myPos.X, myPos.Y + 1.5, myPos.Z)
        end

        local function predictTarget(targetPart, targetCharacter)
            local targetPos = targetPart.Position
            
            if wallcheckEnabled and not isTargetVisible(originPos, targetPos, targetCharacter) then
                return nil, nil, nil, nil
            end

            local gunObj = getGunObject()
            local checkOriginPos = originPos
            
            pcall(function()
                if gunObj and gunObj:IsA("BasePart") then
                    checkOriginPos = gunObj.Position
                elseif gunObj then
                    local part = gunObj:FindFirstChildOfClass("BasePart")
                    if part then checkOriginPos = part.Position end
                end
            end)

            local distance = (targetPos - checkOriginPos).Magnitude
            if distance < 8 then
                return (targetPos - checkOriginPos).Unit, gunObj, checkOriginPos, targetPos
            end

            if not silentAimPredict then
                return (targetPos - checkOriginPos).Unit, gunObj, checkOriginPos, targetPos
            end

            local predictedPos = GetPredictedToFPosition(checkOriginPos, targetPart, targetCharacter, 500)

            return (predictedPos - checkOriginPos).Unit, gunObj, checkOriginPos, predictedPos
        end

        if targetMode == "Killer" then
            local closestTorso = nil
            local closestChar = nil
            local shortestDist = math.huge
            for _, player in ipairs(Players:GetPlayers()) do
                if player ~= LocalPlayer then
                    local isKiller = player.Team and player.Team.Name == "Killer"
                    if isKiller and player.Character then
                        local torso = player.Character:FindFirstChild("Torso")
                        if torso then
                            local dist = (myPos - torso.Position).Magnitude
                            if dist < shortestDist then
                                shortestDist = dist
                                closestTorso = torso
                                closestChar = player.Character
                            end
                        end
                    end
                end
            end
            if not closestTorso then return nil, nil, nil, nil end
            return predictTarget(closestTorso, closestChar)

        elseif targetMode == "Survivors" then
            local bestTorso = nil
            local bestChar = nil
            local bestDot = -math.huge
            local cam = workspace.CurrentCamera
            local camLook = cam.CFrame.LookVector

            for _, player in ipairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Team and player.Team.Name == "Survivors" and player.Character then
                    local torso = player.Character:FindFirstChild("Torso")
                    
                    if torso then
                        local dirToTarget = (torso.Position - cam.CFrame.Position).Unit
                        local dot = camLook:Dot(dirToTarget)
                        
                        if dot > 0.5 and dot > bestDot then
                            bestDot = dot
                            bestTorso = torso
                            bestChar = player.Character
                        end
                    end
                end
            end
            if not bestTorso then return nil, nil, nil, nil end
            return predictTarget(bestTorso, bestChar)

        elseif targetMode == "Zombie" then
            local targets = GetSCPs()
            local bestPart = nil
            local bestDot = -math.huge
            local cam = workspace.CurrentCamera
            local camLook = cam.CFrame.LookVector

            for _, root in ipairs(targets) do
                if root and root.Parent then
                    local dirToTarget = (root.Position - cam.CFrame.Position).Unit
                    local dot = camLook:Dot(dirToTarget)
                    
                    if dot > 0.5 and dot > bestDot then
                        bestDot = dot
                        bestPart = root
                    end
                end
            end
            if not bestPart then return nil, nil, nil, nil end
            return predictTarget(bestPart, bestPart.Parent)
        end

        return nil, nil, nil, nil
    end

    function updateLaser(originPos, targetPos)
        if not LaserBeam then
            LaserBeam = Instance.new("Part")
            LaserBeam.Name = "ToFLaser"
            LaserBeam.Anchored = true
            LaserBeam.CanCollide = false
            LaserBeam.CanTouch = false
            LaserBeam.CastShadow = false
            LaserBeam.Material = Enum.Material.Neon
            LaserBeam.Color = Color3.fromRGB(255, 50, 50)
            LaserBeam.Parent = workspace
        end
        local dist = (targetPos - originPos).Magnitude
        LaserBeam.Size = Vector3.new(0.05, 0.05, dist)
        LaserBeam.CFrame = CFrame.new((originPos + targetPos) / 2, targetPos)
        LaserBeam.Transparency = 0
    end

    function clearLaser()
        if LaserBeam then
            LaserBeam:Destroy()
            LaserBeam = nil
        end
    end

    function doShoot()
        if not silentAimEnabled or not isAiming then return end
        
        local char = LocalPlayer.Character
        if char then
            if _G.Ox.AimConfig.Pistol_BlockKnocked and IsDowned(char) then
                return
            end
        end
        
        local targetDirection, gunObject, originPos, targetPos = getTargetPosition()
        if not (targetDirection and gunObject and targetPos and originPos) then return end
        
        local freshDirection = (targetPos - originPos).Unit
        pcall(function()
            ToFEvent:FireServer(gunObject, freshDirection)
        end)
    end

    function startConnection()
        if Connection then return end
        Connection = RunService.Heartbeat:Connect(function()
            if isAiming and silentAimEnabled then
                local targetDirection, gunObject, originPos, targetPos = getTargetPosition()
                if targetDirection and gunObject and targetPos then
                    pcall(function()
                        local char = LocalPlayer.Character
                        local hrp = char and char:FindFirstChild("HumanoidRootPart")
                        if hrp and not char:GetAttribute("IsCarried") then
                            hrp.CFrame = CFrame.new(hrp.Position, Vector3.new(targetPos.X, hrp.Position.Y, targetPos.Z))
                        end
                    end)
                    
                    if _G.Ox.AimConfig.LockAim then
                        local targetCFrame = CFrame.lookAt(Camera.CFrame.Position, targetPos)
                        Camera.CFrame = Camera.CFrame:Lerp(targetCFrame, 0.15)
                    end

                    if laserEnabled and originPos and targetPos then
                        updateLaser(originPos, targetPos)
                    end
                else
                    if LaserBeam then LaserBeam.Transparency = 1 end
                end
            else
                if LaserBeam then LaserBeam.Transparency = 1 end
            end
        end)
    end

    function stopConnection()
        if Connection then
            Connection:Disconnect()
            Connection = nil
        end
        clearLaser()
    end

    local targetGuiRef = nil

    local MODES = {
        { internal = "Killer",    display = "Killer (K)",   activeColor = Color3.fromRGB(180, 45, 45),  activeTxt = Color3.fromRGB(255, 180, 180) },
        { internal = "Survivors", display = "Survivors (J)", activeColor = Color3.fromRGB(25, 80, 150),  activeTxt = Color3.fromRGB(160, 210, 255) },
        { internal = "Zombie",    display = "Zombie (L)",    activeColor = Color3.fromRGB(120, 80, 10),  activeTxt = Color3.fromRGB(255, 210, 100) },
    }

    function createTargetSelectorUI()
        local cg = CoreGui
        if not cg then return end

        if targetGuiRef and targetGuiRef.Parent then return end

        local old = cg:FindFirstChild("ToFTargetSelector")
        if old then old:Destroy() end

        local gui = Instance.new("ScreenGui")
        gui.Name = "ToFTargetSelector"
        gui.ResetOnSpawn = false
        gui.IgnoreGuiInset = true
        gui.Parent = cg

        local frame = Instance.new("Frame")
        frame.Name = "Main"
        frame.Size = UDim2.new(0, 240, 0, 58)
        frame.Position = savedUIPos 
        frame.BackgroundColor3 = Color3.fromRGB(22, 22, 22)
        frame.BorderSizePixel = 0
        frame.Active = true
        frame.Parent = gui
        Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 10)

        local stroke = Instance.new("UIStroke", frame)
        stroke.Color = Color3.fromRGB(55, 55, 55)
        stroke.Thickness = 0.8

        local header = Instance.new("Frame")
        header.Size = UDim2.new(1, 0, 0, 22)
        header.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
        header.BorderSizePixel = 0
        header.Parent = frame
        Instance.new("UICorner", header).CornerRadius = UDim.new(0, 10)

        local headerFix = Instance.new("Frame")
        headerFix.Size = UDim2.new(1, 0, 0, 10)
        headerFix.Position = UDim2.new(0, 0, 1, -10)
        headerFix.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
        headerFix.BorderSizePixel = 0
        headerFix.Parent = header

        local headerDiv = Instance.new("Frame")
        headerDiv.Size = UDim2.new(1, 0, 0, 1)
        headerDiv.Position = UDim2.new(0, 0, 1, -1)
        headerDiv.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
        headerDiv.BorderSizePixel = 0
        headerDiv.Parent = header

        local dragArea = Instance.new("Frame")
        dragArea.Size = UDim2.new(1, -30, 1, 0)
        dragArea.BackgroundTransparency = 1
        dragArea.Parent = header

        local minimizeBtn = Instance.new("TextButton")
        minimizeBtn.Size = UDim2.new(0, 24, 1, 0)
        minimizeBtn.Position = UDim2.new(1, -26, 0, 0)
        minimizeBtn.BackgroundTransparency = 1
        minimizeBtn.Text = "−"
        minimizeBtn.TextColor3 = Color3.fromRGB(120, 120, 120)
        minimizeBtn.Font = Enum.Font.GothamBold
        minimizeBtn.TextSize = 14
        minimizeBtn.TextXAlignment = Enum.TextXAlignment.Center
        minimizeBtn.TextYAlignment = Enum.TextYAlignment.Center
        minimizeBtn.Parent = header

        local headerLbl = Instance.new("TextLabel")
        headerLbl.Size = UDim2.new(1, -30, 1, 0)
        headerLbl.Position = UDim2.new(0, 0, 0, 0)
        headerLbl.BackgroundTransparency = 1
        headerLbl.Text = "TARGET MODE TWIST OF FATE"
        headerLbl.TextColor3 = Color3.fromRGB(120, 120, 120)
        headerLbl.Font = Enum.Font.GothamBold
        headerLbl.TextSize = 9
        headerLbl.TextXAlignment = Enum.TextXAlignment.Center
        headerLbl.Parent = header

        local btnContainer = Instance.new("Frame")
        btnContainer.Size = UDim2.new(1, -16, 0, 26)
        btnContainer.Position = UDim2.new(0, 8, 0, 26)
        btnContainer.BackgroundTransparency = 1
        btnContainer.Parent = frame

        local layout = Instance.new("UIListLayout", btnContainer)
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.SortOrder = Enum.SortOrder.LayoutOrder
        layout.Padding = UDim.new(0, 5)

        local btnRefs = {}
        local isMinimized = false

        function updateButtons()
            for _, m in ipairs(MODES) do
                local btn = btnRefs[m.internal]
                if btn then
                    if m.internal == targetMode then
                        btn.BackgroundColor3 = m.activeColor
                        btn.TextColor3 = m.activeTxt
                    else
                        btn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
                        btn.TextColor3 = Color3.fromRGB(130, 130, 130)
                    end
                end
            end
        end

        function toggleMinimize()
            isMinimized = not isMinimized
            
            if isMinimized then
                minimizeBtn.Text = "+"
                btnContainer.Visible = false
                frame.Size = UDim2.new(0, 240, 0, 22)
            else
                minimizeBtn.Text = "−"
                btnContainer.Visible = true
                frame.Size = UDim2.new(0, 240, 0, 58)
            end
        end

        minimizeBtn.MouseButton1Click:Connect(toggleMinimize)

        for i, m in ipairs(MODES) do
            local btn = Instance.new("TextButton")
            btn.Size = UDim2.new(0, 70, 1, 0)
            btn.BorderSizePixel = 0
            btn.Font = Enum.Font.GothamBold
            btn.TextSize = 11
            btn.Text = m.display
            btn.LayoutOrder = i
            btn.Parent = btnContainer
            Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)

            btn.MouseButton1Click:Connect(function()
                targetMode = m.internal
                updateButtons()
            end)

            btn.InputEnded:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.Touch then
                    targetMode = m.internal
                    updateButtons()
                end
            end)

            btnRefs[m.internal] = btn
        end

        updateButtons()

        local dragging = false
        local dragStart, startPos

        dragArea.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1
            or input.UserInputType == Enum.UserInputType.Touch then
                dragStart = input.Position
                startPos = frame.Position
                dragging = true
            end
        end)

        dragArea.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1
            or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)

        UserInputService.InputChanged:Connect(function(input)
            if not dragging then return end
            if input.UserInputType == Enum.UserInputType.MouseMovement
            or input.UserInputType == Enum.UserInputType.Touch then
                local delta = input.Position - dragStart
                local newPos = UDim2.new(
                    startPos.X.Scale,
                    startPos.X.Offset + delta.X,
                    startPos.Y.Scale,
                    startPos.Y.Offset + delta.Y
                )
                frame.Position = newPos
                savedUIPos = newPos 
            end
        end)

        UserInputService.InputBegan:Connect(function(input, gameProcessed)
            if not silentAimEnabled then return end
            if gameProcessed or input.UserInputType ~= Enum.UserInputType.Keyboard then return end

            local newMode = nil
            if input.KeyCode == Enum.KeyCode.K then
                newMode = "Killer"
                Library:Notify({Title="Target Mode", Description="Killer", Time=1})
            elseif input.KeyCode == Enum.KeyCode.J then
                newMode = "Survivors"
                Library:Notify({Title="Target Mode", Description="Survivors", Time=1})
            elseif input.KeyCode == Enum.KeyCode.L then
                newMode = "Zombie"
                Library:Notify({Title="Target Mode", Description="Zombie", Time=1})
            end

            if newMode and newMode ~= targetMode then
                targetMode = newMode

                for modeName, btn in pairs(btnRefs) do
                    if btn and btn.Parent then
                        local isActive = (modeName == targetMode)
                        for _, m in ipairs(MODES) do
                            if m.internal == modeName then
                                btn.BackgroundColor3 = isActive and m.activeColor or Color3.fromRGB(38, 38, 38)
                                btn.TextColor3 = isActive and m.activeTxt or Color3.fromRGB(130, 130, 130)
                                break
                            end
                        end
                    end
                end
            end
        end)

        targetGuiRef = gui
    end

    function destroyTargetSelectorUI()
        if targetGuiRef then
            targetGuiRef:Destroy()
            targetGuiRef = nil
        end
    end

-- ===== UI =====
local PistolSection = S_Page:CreateSection("Silent Aim (Pistol)")

SilentAimToggle = PistolSection:AddToggle("Silent Aim Twist Of Fate", false, function(Value)
    silentAimEnabled = Value
    if Value then
        createTargetSelectorUI()
        startConnection()
    else
        destroyTargetSelectorUI()
        stopConnection()
    end
end)
SilentAimToggle:AddKeyPicker("SilentAimKey")

PredictToggle = PistolSection:AddToggle("Predict Aim ToF", false, function(Value)
    silentAimPredict = Value
    Library:Notify({ Title = "Predict Aim", Description = Value and "Enabled" or "Disabled", Duration = 2 })
end)

BlockKnockedToggle = PistolSection:AddToggle("Block aim Knocked", false, function(Value)
    _G.Ox.AimConfig.Pistol_BlockKnocked = Value
end)

BulletBlockToggle = PistolSection:AddToggle("Block Peluru (TOF)", false, function(state)
    _G.Ox.BulletBlockConfig.Enabled = state
    Library:Notify({
        Title = "Block Peluru",
        Description = state and "AKTIF - Peluru tidak keluar" or "NONAKTIF",
        Duration = 3
    })
end)
BulletBlockToggle:AddKeyPicker("BulletBlockKey", {
    Default = "None",
    Text = "Toggle Block Peluru",
    Mode = "Toggle",
    Callback = function(Value)
        _G.Ox.BulletBlockConfig.Enabled = Value
    end
})

LockAimToggle = PistolSection:AddToggle("Lock Aim (Twist Of Fate)", false, function(Value)
    _G.Ox.AimConfig.LockAim = Value
end)

WallCheckToggle = PistolSection:AddToggle("Wallcheck", false, function(Value)
    wallcheckEnabled = Value
end)

LaserToggle = PistolSection:AddToggle("Enable Laser Effect", true, function(Value)
    laserEnabled = Value
    if not Value then clearLaser() end
end)

    UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then return end
        if input.UserInputType == Enum.UserInputType.MouseButton2 then
            isAiming = true
        elseif input.UserInputType == Enum.UserInputType.MouseButton1 then
            doShoot()
        end
    end)
    
    local FlashToggle = PistolSection:AddToggle("Silent Aim Flashlight", false, function(Value)
    _G.Ox.AimConfig.Flash_Silent = Value
    Library:Notify({ 
        Title = "Flash Silent Aim", 
        Description = Value and "AKTIF" or "NONAKTIF", 
        Duration = 2 
    })
end)
FlashToggle:AddKeyPicker("FlashKey")

PistolSection:AddSlider("FlashHeadOffset", 0, 15, 8, function(v)
    _G.Ox.AimConfig.Flash_YOffset = v
end, { Increment = 0.1 })

-- ===== FLASHLIGHT INPUT HANDLER (PC) =====
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.UserInputType == Enum.UserInputType.MouseButton2 then
        if _G.Ox.AimConfig.Flash_Silent then
            isAimingFlash = true
        end
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton2 then
        if _G.Ox.AimConfig.Flash_Silent then
            isAimingFlash = false
        end
    end
end)

-- ===== FLASHLIGHT MOBILE SUPPORT =====
task.spawn(function()
    local survivorMob = PlayerGui:FindFirstChild("Survivor-mob")
    if not survivorMob then
        PlayerGui.ChildAdded:Wait()
        survivorMob = PlayerGui:FindFirstChild("Survivor-mob")
    end
    
    if not survivorMob then return end
    
    local controls = survivorMob:FindFirstChild("Controls")
    if not controls then return end
    
    local flashBtn = controls:FindFirstChild("flashlight")
    if not flashBtn then return end
    
    flashBtn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch then
            if _G.Ox.AimConfig.Flash_Silent then
                isAimingFlash = true
            end
        end
    end)
    
    flashBtn.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch then
            if _G.Ox.AimConfig.Flash_Silent then
                isAimingFlash = false
            end
        end
    end)
end)

    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton2 then
            isAiming = false
            if LaserBeam then LaserBeam.Transparency = 1 end
        end
    end)

    function setupMobileButton()
        local playerGui = LocalPlayer:FindFirstChildOfClass("PlayerGui")
        if not playerGui then return end
        local survivorMob = playerGui:FindFirstChild("Survivor-mob")
        if not survivorMob then
            playerGui.ChildAdded:Connect(function(child)
                if child.Name == "Survivor-mob" then task.wait(0.2) setupMobileButton() end
            end)
            return
        end
        local controls = survivorMob:FindFirstChild("Controls")
        if not controls then
            survivorMob.ChildAdded:Connect(function(child)
                if child.Name == "Controls" then task.wait(0.2) setupMobileButton() end
            end)
            return
        end
        local guiMobButton = controls:FindFirstChild("Gui-mob")
        if not guiMobButton then return end

        guiMobButton.InputBegan:Connect(function(input, gameProcessed)
            if input.UserInputType == Enum.UserInputType.Touch
            or input.UserInputType == Enum.UserInputType.MouseButton1 then
                isAiming = true
            end
        end)
        guiMobButton.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.Touch
            or input.UserInputType == Enum.UserInputType.MouseButton1 then
                doShoot()
                isAiming = false
                if LaserBeam then LaserBeam.Transparency = 1 end
            end
        end)
    end

    task.wait(0.5)
    setupMobileButton()

    LocalPlayer.CharacterAdded:Connect(function()
        clearLaser()
        SCPCache = {}
        SCPCacheTimer = 0
        task.wait(0.5)
        setupMobileButton()
    end)
    
    PlayerGui.ChildRemoved:Connect(function(child)
        if not silentAimEnabled then return end
        task.wait(0.5)
        if not silentAimEnabled then return end

        if child.Name == "ToFTargetSelector" then
            createTargetSelectorUI()
        end
    end)
end

-- ==================================================
-- KILLER — SILENT VEIL
-- ==================================================
local KillerTab = Window:CreateTab("Killer", false, false)
local K_Page = KillerTab:CreatePage("Killer")
local VeilCard = K_Page:CreateSection("Silent Veil")

VeilTog = VeilCard:AddToggle("Silent Veil", false, function(v)
    _G.Ox.AimConfig.Aim_SilentVeilV2 = v
    Library:Notify({ Title = "Silent Veil", Description = v and "ON" or "OFF", Duration = 2 })
end)
VeilTog:AddKeyPicker()

VeilCard:AddToggle("Auto Predict", false, function(v)
    _G.Ox.Config.SpearSmart_enable = v
end)

VeilCard:AddToggle("Show FOV", true, function(v)
    _G.Ox.AimConfig.Veil_ShowFOV = v
end)

VeilCard:AddSlider("Lead", 0.5, 5, 1.4, function(v)
    _G.Ox.AimConfig.Veil_LeadMultiplier = v
end, { Increment = 0.1 })

VeilCard:AddSlider("Spear Speed", 50, 200, 165, function(v)
    _G.Ox.AimConfig.SPEAR_Speed = v
end, { Increment = 1 })

VeilCard:AddSlider("Spear Gravity", 10, 200, 103, function(v)
    _G.Ox.AimConfig.SPEAR_Gravity = v
end, { Increment = 1 })

VeilCard:AddSlider("Aura Speed", 50, 200, 165, function(v)
    _G.Ox.AimConfig.SPEAR_AuraSpeed = v
end, { Increment = 1 })

VeilCard:AddSlider("Aura Gravity", 10, 200, 96.5, function(v)
    _G.Ox.AimConfig.SPEAR_AuraGravity = v
end, { Increment = 0.1 })

VeilCard:AddSlider("FOV", 50, 500, 150, function(v)
    _G.Ox.AimConfig.Veil_FOV = v
end, { Increment = 1 })

VeilCard:AddSlider("Max Dist", 50, 1000, 500, function(v)
    _G.Ox.AimConfig.SPEAR_MaxDist = v
end, { Increment = 10 })

local KillerAimbotCard = K_Page:CreateSection("Killer Aimbot")

KillerAimbotToggle = KillerAimbotCard:AddToggle("Enable Aimbot (Melee)", false, function(state)
    _G.Ox.Config.Killer_Aimbot_Enabled = state
    if not state then
        if _G.Ox.killerAimbotHighlight then _G.Ox.killerAimbotHighlight.Parent = nil end
    end
    Library:Notify({
        Title = "Killer Aimbot",
        Description = state and "AKTIF" or "NONAKTIF",
        Duration = 2
    })
end)
KillerAimbotToggle:AddKeyPicker("KillerAimbotKey", {
    Default = "None",
    Text = "Toggle Killer Aimbot",
    Mode = "Toggle",
    Callback = function(Value)
        _G.Ox.Config.Killer_Aimbot_Enabled = Value
        if not Value and _G.Ox.killerAimbotHighlight then
            _G.Ox.killerAimbotHighlight.Parent = nil
        end
    end
})

KillerAimbotCard:AddSlider("Max Distance", 5, 30, 12, function(v)
    _G.Ox.Config.Killer_Aimbot_MaxDist = v
end, { Increment = 1 })

KillerAimbotCard:AddSlider("Smoothness", 0.1, 1.0, 0.5, function(v)
    _G.Ox.Config.Killer_Aimbot_Smoothness = v
end, { Increment = 0.05 })

KillerAimbotCard:AddLabel("Aimbot aktif saat tombol serang ditekan (Killer Only)", {
    TextSize = 11,
    Color = Color3.fromRGB(130, 130, 145),
    Height = 16,
})

local AntiBlindSection = K_Page:CreateSection("Killer Ability")
AntiBlindToggle = AntiBlindSection:AddToggle("Anti Blind (Flashlight)", false, function(state)
    _G.Ox.AntiBlindEnabled = state
    Library:Notify({
        Title = "Anti Blind",
        Description = state and "AKTIF - Immune dari flashlight" or "NONAKTIF",
        Duration = 3
    })
end)
AntiBlindToggle:AddKeyPicker("AntiBlindKey", {
    Default = "None",
    Text = "Toggle Anti Blind",
    Mode = "Toggle",
    Callback = function(Value)
        _G.Ox.AntiBlindEnabled = Value
    end
})

AntiSlowdownToggle = AntiBlindSection:AddToggle("No Slowdown Killer", false, function(state)
    _G.Ox.NoSlowdownEnabled = state
    if state then
        -- Langsung set WalkSpeed ke 16
        local char = LP.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then
                hum.WalkSpeed = 16
            end
        end
        Library:Notify({
            Title = "No Slowdown Killer",
            Description = "AKTIF - WalkSpeed dikunci 16",
            Duration = 3
        })
    else
        Library:Notify({
            Title = "No Slowdown Killer",
            Description = "NONAKTIF",
            Duration = 2
        })
    end
end)
AntiSlowdownToggle:AddKeyPicker("NoSlowdownKey", {
    Default = "None",
    Text = "Toggle No Slowdown",
    Mode = "Toggle",
    Callback = function(Value)
        _G.Ox.NoSlowdownEnabled = Value
        if Value then
            local char = LP.Character
            if char then
                local hum = char:FindFirstChildOfClass("Humanoid")
                if hum then
                    hum.WalkSpeed = 16
                end
            end
        end
    end
})

AntiBlindSection:AddLabel("Mencegah Killer mengalami perlambatan (slowdown)", {
    TextSize = 11,
    Color = Color3.fromRGB(130, 130, 145),
    Height = 16,
})

BypassLeapToggle = AntiBlindSection:AddToggle("Skill Hidden No CD", false, function(state)
    _G.Ox.BypassLeapEnabled = state
    if state then
        StartLeapBypass()
        Library:Notify({
            Title = "Skill Hidden No CD",
            Description = "AKTIF - Cooldown skill Hidden dihilangkan",
            Duration = 3
        })
    else
        if Connections.LeapBypass then
            task.cancel(Connections.LeapBypass)
            Connections.LeapBypass = nil
        end
        Library:Notify({
            Title = "Skill Hidden No CD",
            Description = "NONAKTIF",
            Duration = 2
        })
    end
end)
BypassLeapToggle:AddKeyPicker("BypassLeapKey", {
    Default = "None",
    Text = "Toggle Hidden No CD",
    Mode = "Toggle",
    Callback = function(Value)
        _G.Ox.BypassLeapEnabled = Value
        if Value then
            StartLeapBypass()
        else
            if Connections.LeapBypass then
                task.cancel(Connections.LeapBypass)
                Connections.LeapBypass = nil
            end
        end
    end
})

AntiBlindSection:AddLabel("Menghilangkan cooldown skill Hidden (tryActivate & playM2Animation)", {
    TextSize = 11,
    Color = Color3.fromRGB(130, 130, 145),
    Height = 16,
})

local InfAbyssalToggle = AntiBlindSection:AddToggle("Infinite Corrupt (Abyssal)", false, function(state)
    _G.Ox.InfAbyssalEnabled = state
    if not state then
        StopInfiniteAbyssal()
    end
    Library:Notify({
        Title = "Infinite Corrupt",
        Description = state and "AKTIF - Tekan Q (PC) atau tombol skill (Mobile)" or "NONAKTIF",
        Duration = 3
    })
end)
InfAbyssalToggle:AddKeyPicker("InfAbyssalKey", {
    Default = "Q",
    Text = "Toggle Infinite Corrupt",
    Mode = "Toggle",
    Callback = function(Value)
        _G.Ox.InfAbyssalEnabled = Value
        if not Value then
            StopInfiniteAbyssal()
        end
    end
})

AntiBlindSection:AddLabel("Skill corrupt Abyssal tanpa cooldown (Tekan Q / tombol skill)", {
    TextSize = 11,
    Color = Color3.fromRGB(130, 130, 145),
    Height = 16,
})

-- ===== DASH LOCK =====
DashLockToggle = AntiBlindSection:AddToggle("Dash Lock (Hidden)", false, function(state)
    ToggleDashLock(state)
end)
DashLockToggle:AddKeyPicker("DashKey", {
    Default = "V",
    Text = "Toggle Dash Lock",
    Mode = "Toggle",
    Callback = function(Value)
        ToggleDashLock(Value)
    end
})

AntiBlindSection:AddLabel("Aim lock otomatis ke survivor saat klik kanan (PC) / tombol skill (HP)", {
    TextSize = 11,
    Color = Color3.fromRGB(130, 130, 145),
    Height = 16,
})

-- ===== MASKED GUI SELECTION (PASTEFY) =====
local MaskToggle = AntiBlindSection:AddToggle("Mask Selection GUI", false, function(state)
    ToggleMaskedGUI(state)
end)
MaskToggle:AddKeyPicker("MaskKey", {
    Default = "M",
    Text = "Toggle Mask GUI",
    Mode = "Toggle",
    Callback = function(Value)
        ToggleMaskedGUI(Value)
    end
})

AntiBlindSection:AddLabel("Pilih mask The Masked dengan GUI (Tombol M toggle)", {
    TextSize = 11,
    Color = Color3.fromRGB(130, 130, 145),
    Height = 16,
})
AntiBlindSection:AddLabel("Tombol 1-6 pilih mask, 7 nonaktifkan", {
    TextSize = 11,
    Color = Color3.fromRGB(130, 130, 145),
    Height = 16,
})

-- ==================================================
-- ESP TAB
-- ==================================================
local EspTab = Window:CreateTab("ESP", false, false)
local EspPage = EspTab:CreatePage("ESP")

-- ===== ESP MASTER =====
local EspMasterCard = EspPage:CreateSection("ESP Settings")
local EspMasterToggle = EspMasterCard:AddToggle("Enable ESP", false, function(state)
    _G.Ox.ESPConfig.Master = state
    if state then 
        ForceRefreshMap()
        Library:Notify({ Title = "ESP", Description = "AKTIF", Duration = 2 })
    else 
        ClearAllESP()
        Library:Notify({ Title = "ESP", Description = "NONAKTIF", Duration = 2 })
    end
end)
EspMasterToggle:AddKeyPicker("EspKey", { Default = "None", Text = "ESP Key", Mode = "Toggle" })

PlayerToggle = EspMasterCard:AddToggle("Player ESP", false, function(state)
    _G.Ox.ESPConfig.Player = state
    if _G.Ox.ESPConfig.Master then
        if not state then ClearESP("Player") else ScanMapESP() end
    end
end)
PlayerToggle:AddKeyPicker("PlayerEspKey", {
    Default = "None",
    Text = "Toggle Player ESP",
    Mode = "Toggle",
})
PlayerToggle:AddColorPicker(_G.Ox.ESPColors.Player, {
    Title = "Warna Player ESP",
    Callback = function(color)
        _G.Ox.ESPColors.Player = color
        if _G.Ox.ESPConfig.Master then
            UpdatePlayerESP()
        end
    end
})

KillerToggle = EspMasterCard:AddToggle("Killer ESP", false, function(state)
    _G.Ox.ESPConfig.Killer = state
    if _G.Ox.ESPConfig.Master then
        if not state then ClearESP("Killer") else ScanMapESP() end
    end
end)
KillerToggle:AddKeyPicker("KillerEspKey", {
    Default = "None",
    Text = "Toggle Killer ESP",
    Mode = "Toggle",
})
KillerToggle:AddColorPicker(_G.Ox.ESPColors.Killer, {
    Title = "Warna Killer ESP",
    Callback = function(color)
        _G.Ox.ESPColors.Killer = color
        if _G.Ox.ESPConfig.Master then
            UpdatePlayerESP()
        end
    end
})

NameToggle = EspMasterCard:AddToggle("ESP Name", false, function(state)
    _G.Ox.ESPConfig.Name = state
    if _G.Ox.ESPConfig.Master then ScanMapESP() else ClearAllESP() end
end)
NameToggle:AddKeyPicker("NameEspKey", {
    Default = "None",
    Text = "Toggle ESP Name",
    Mode = "Toggle",
})

DistToggle = EspMasterCard:AddToggle("ESP Distance", false, function(state)
    _G.Ox.ESPConfig.Distance = state
    if _G.Ox.ESPConfig.Master then UpdatePlayerESP() end
end)
DistToggle:AddKeyPicker("DistEspKey", {
    Default = "None",
    Text = "Toggle ESP Distance",
    Mode = "Toggle",
})

IconToggle = EspMasterCard:AddToggle("ESP Item Icon", false, function(state)
    _G.Ox.ESPConfig.ItemIcon = state
    if _G.Ox.ESPConfig.Master then UpdatePlayerESP() end
end)
IconToggle:AddKeyPicker("IconEspKey", {
    Default = "None",
    Text = "Toggle Item Icon",
    Mode = "Toggle",
})

WarnToggle = EspMasterCard:AddToggle("ESP Killer Warn", false, function(state)
    _G.Ox.ESPConfig.KillerWarn = state
    if not state then
        local myHRP = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if myHRP and myHRP:FindFirstChild("KillerWarn") then
            myHRP.KillerWarn:Destroy()
        end
    end
end)
WarnToggle:AddKeyPicker("WarnEspKey", {
    Default = "None",
    Text = "Toggle Killer Warn",
    Mode = "Toggle",
})

OutlineToggle = EspMasterCard:AddToggle("ESP Outline Mode", false, function(state)
    _G.Ox.ESPConfig.Outline = state
end)
OutlineToggle:AddKeyPicker("OutlineEspKey", {
    Default = "None",
    Text = "Toggle Outline Mode",
    Mode = "Toggle",
})

HookCountToggle = EspMasterCard:AddToggle("ESP Hook Count", false, function(state)
    _G.Ox.ESPConfig.HookCount = state
    if state then
        UpdateHookData()
        Library:Notify({ Title = "Hook ESP", Description = "AKTIF", Duration = 2 })
    else
        ClearHookESP()
        Library:Notify({ Title = "Hook ESP", Description = "NONAKTIF", Duration = 2 })
    end
end)
HookCountToggle:AddKeyPicker("HookCountEspKey", {
    Default = "None",
    Text = "Toggle Hook Count",
    Mode = "Toggle",
})

-- ===== PERFORMANCE SETTINGS =====
local PerfCard = EspPage:CreateSection("World Effects")

FullBrightToggle = PerfCard:AddToggle("Full Bright", false, function(state)
    _G.Ox.FullBright = state
    local Lighting = game:GetService("Lighting")
    if not Lighting then return end
    if state then
        Lighting.Brightness = 5
        Lighting.ClockTime = _G.Ox.TimeOfDayValue
        Lighting.FogEnd = 100000
        Lighting.Ambient = Color3.fromRGB(255, 255, 255)
        Lighting.OutdoorAmbient = Color3.fromRGB(255, 255, 255)
        Library:Notify({ Title = "Full Bright", Description = "AKTIF - Map terang total", Duration = 3 })
    else
        Lighting.Brightness = 1
        Lighting.ClockTime = 14
        Lighting.FogEnd = 1000
        Lighting.Ambient = Color3.fromRGB(128, 128, 128)
        Lighting.OutdoorAmbient = Color3.fromRGB(128, 128, 128)
        Library:Notify({ Title = "Full Bright", Description = "NONAKTIF", Duration = 2 })
    end
end)
FullBrightToggle:AddKeyPicker("FullBrightKey", {
    Default = "None",
    Text = "Toggle Full Bright",
    Mode = "Toggle",
})

PerfCard:AddSlider("Time Of Day", 0, 24, 14, function(v)
    _G.Ox.TimeOfDayValue = v
    if _G.Ox.FullBright then
        local Lighting = game:GetService("Lighting")
        if Lighting then
            Lighting.ClockTime = v
        end
    end
end, { Increment = 1 })

NoFogToggle = PerfCard:AddToggle("No Fog", false, function(state)
    _G.Ox.NoFog = state
    local Lighting = game:GetService("Lighting")
    if not Lighting then return end
    if state then
        Lighting.FogStart = 0
        Lighting.FogEnd = 100000
        Lighting.FogColor = Color3.fromRGB(255, 255, 255)
        local atmosphere = Lighting:FindFirstChildOfClass("Atmosphere")
        if atmosphere then
            atmosphere.Density = 0
            atmosphere.Offset = 0
            atmosphere.Glare = 0
            atmosphere.Haze = 0
        end
        Library:Notify({ Title = "No Fog", Description = "AKTIF - Kabut hilang", Duration = 3 })
    else
        Lighting.FogStart = 0
        Lighting.FogEnd = 1000
        local atmosphere = Lighting:FindFirstChildOfClass("Atmosphere")
        if atmosphere then atmosphere.Density = 0.35 end
        Library:Notify({ Title = "No Fog", Description = "NONAKTIF", Duration = 2 })
    end
end)
NoFogToggle:AddKeyPicker("NoFogKey", {
    Default = "None",
    Text = "Toggle No Fog",
    Mode = "Toggle",
})

NoShadowToggle = PerfCard:AddToggle("No Shadow", false, function(state)
    _G.Ox.NoShadow = state
    local Lighting = game:GetService("Lighting")
    if not Lighting then return end
    Lighting.GlobalShadows = not _G.Ox.NoShadow
    Library:Notify({ Title = "No Shadow", Description = state and "AKTIF - Shadow hilang" or "NONAKTIF", Duration = 2 })
end)
NoShadowToggle:AddKeyPicker("NoShadowKey", {
    Default = "None",
    Text = "Toggle No Shadow",
    Mode = "Toggle",
})

-- ===== MAP ESP =====
local MapCard = EspPage:CreateSection("Map ESP")

GenToggle = MapCard:AddToggle("Generator ESP", false, function(state)
    _G.Ox.ESPConfig.Generator = state
    if _G.Ox.ESPConfig.Master then
        if not state then ClearESP("Generator") else ScanMapESP() end
    end
end)
GenToggle:AddKeyPicker("GenEspKey", {
    Default = "None",
    Text = "Toggle Generator ESP",
    Mode = "Toggle",
})
GenToggle:AddColorPicker(_G.Ox.ESPColors.Generator, {
    Title = "Warna Generator",
    Callback = function(color)
        _G.Ox.ESPColors.Generator = color
        if _G.Ox.ESPConfig.Master then
            UpdateStaticESP()
        end
    end
})

GenNameToggle = MapCard:AddToggle("Generator Name & Progress", true, function(state)
    _G.Ox.ESPConfig.GeneratorName = state
    if _G.Ox.ESPConfig.Master then ScanMapESP() end
end)
GenNameToggle:AddKeyPicker("GenNameEspKey", {
    Default = "None",
    Text = "Toggle Gen Name & Progress",
    Mode = "Toggle",
})

MapCard:AddColorPicker("Gen Done Color", _G.Ox.ESPColors.GeneratorDone, {
    Title = "Warna Generator Selesai",
    Callback = function(color)
        _G.Ox.ESPColors.GeneratorDone = color
        if _G.Ox.ESPConfig.Master then
            UpdateStaticESP()
        end
    end
})

PalletToggle = MapCard:AddToggle("Pallet ESP", false, function(state)
    _G.Ox.ESPConfig.Pallet = state
    if _G.Ox.ESPConfig.Master then
        if not state then ClearESP("Pallet") else ScanMapESP() end
    end
end)
PalletToggle:AddKeyPicker("PalletEspKey", {
    Default = "None",
    Text = "Toggle Pallet ESP",
    Mode = "Toggle",
})
PalletToggle:AddColorPicker(_G.Ox.ESPColors.Pallet, {
    Title = "Warna Pallet",
    Callback = function(color)
        _G.Ox.ESPColors.Pallet = color
        if _G.Ox.ESPConfig.Master then
            UpdateStaticESP()
        end
    end
})

WindowToggle = MapCard:AddToggle("Window ESP", false, function(state)
    _G.Ox.ESPConfig.Window = state
    if _G.Ox.ESPConfig.Master then
        if not state then ClearESP("Window") else ScanMapESP() end
    end
end)
WindowToggle:AddKeyPicker("WindowEspKey", {
    Default = "None",
    Text = "Toggle Window ESP",
    Mode = "Toggle",
})
WindowToggle:AddColorPicker(_G.Ox.ESPColors.Window, {
    Title = "Warna Window",
    Callback = function(color)
        _G.Ox.ESPColors.Window = color
        if _G.Ox.ESPConfig.Master then
            UpdateStaticESP()
        end
    end
})

HookToggle = MapCard:AddToggle("Hook ESP", false, function(state)
    _G.Ox.ESPConfig.Hook = state
    if _G.Ox.ESPConfig.Master then
        if not state then ClearESP("Hook") else ScanMapESP() end
    end
end)
HookToggle:AddKeyPicker("HookEspKey", {
    Default = "None",
    Text = "Toggle Hook ESP",
    Mode = "Toggle",
})
HookToggle:AddColorPicker(_G.Ox.ESPColors.Hook, {
    Title = "Warna Hook",
    Callback = function(color)
        _G.Ox.ESPColors.Hook = color
        if _G.Ox.ESPConfig.Master then
            UpdateStaticESP()
        end
    end
})

GateToggle = MapCard:AddToggle("Gate ESP", false, function(state)
    _G.Ox.ESPConfig.Gate = state
    if _G.Ox.ESPConfig.Master then
        if not state then ClearESP("Gate") else ScanMapESP() end
    end
end)
GateToggle:AddKeyPicker("GateEspKey", {
    Default = "None",
    Text = "Toggle Gate ESP",
    Mode = "Toggle",
})
GateToggle:AddColorPicker(_G.Ox.ESPColors.Gate, {
    Title = "Warna Gate",
    Callback = function(color)
        _G.Ox.ESPColors.Gate = color
        if _G.Ox.ESPConfig.Master then
            UpdateStaticESP()
        end
    end
})

SCPToggle = MapCard:AddToggle("SCP ESP", false, function(state)
    _G.Ox.ESPConfig.SCP = state
    if _G.Ox.ESPConfig.Master then
        if not state then ClearESP("SCP") else ScanMapESP() end
    end
end)
SCPToggle:AddKeyPicker("SCPEspKey", {
    Default = "None",
    Text = "Toggle SCP ESP",
    Mode = "Toggle",
})
SCPToggle:AddColorPicker(_G.Ox.ESPColors.SCP, {
    Title = "Warna SCP",
    Callback = function(color)
        _G.Ox.ESPColors.SCP = color
        if _G.Ox.ESPConfig.Master then
            UpdateSCPESP()
        end
    end
})

MapCard:AddButton("Refresh Map", function()
    ForceRefreshMap()
    Library:Notify({ Title = "Map", Description = "Refreshed!", Duration = 2 })
end)

-- ==================================================
-- TELEPORT
-- ==================================================
local TeleportTab = Window:CreateTab("Teleport", false, false)
local T_Page = TeleportTab:CreatePage("Teleport")
local MapCard = T_Page:CreateSection("Teleport Map")

MapCard:AddButton("Refresh Map Cache", function()
    ScanMap()
    _G.Ox.Teleport.GenIndex = 1
    _G.Ox.Teleport.HookIndex = 1
    _G.Ox.Teleport.GateIndex = 1
    _G.Ox.Teleport.PalletIndex = 1
    _G.Ox.Teleport.WindowIndex = 1
    Library:Notify({Title = "Refresh", Description = "Cache map di-refresh!", Duration = 2})
end)

MapCard:AddButton("Teleport ke Generator (Loop)", function()
    if #Cache.Generators == 0 then ScanMap() end
    if _G.Ox.Teleport.GenIndex > #Cache.Generators then _G.Ox.Teleport.GenIndex = 1 end
    local genData = Cache.Generators[_G.Ox.Teleport.GenIndex]
    if genData and genData.part then
        TeleportToPart(genData.part)
        Library:Notify({Title = "TP Generator", Description = "Generator " .. _G.Ox.Teleport.GenIndex, Duration = 1.2})
    else
        Library:Notify({Title = "TP Generator", Description = "Generator tidak ditemukan!", Duration = 2})
    end
    _G.Ox.Teleport.GenIndex = _G.Ox.Teleport.GenIndex + 1
end)

MapCard:AddButton("Teleport ke Hook (Loop)", function()
    if #Cache.Hooks == 0 then ScanMap() end
    if _G.Ox.Teleport.HookIndex > #Cache.Hooks then _G.Ox.Teleport.HookIndex = 1 end
    local v = Cache.Hooks[_G.Ox.Teleport.HookIndex]
    local part = v and (v:FindFirstChild("HookPoint") or v:FindFirstChildWhichIsA("BasePart"))
    if part then
        TeleportToPart(part)
        Library:Notify({Title = "TP Hook", Description = "Hook " .. _G.Ox.Teleport.HookIndex, Duration = 1.2})
    end
    _G.Ox.Teleport.HookIndex = _G.Ox.Teleport.HookIndex + 1
end)

MapCard:AddButton("Teleport ke Gate (Loop)", function()
    if #Cache.Gates == 0 then ScanMap() end
    if _G.Ox.Teleport.GateIndex > #Cache.Gates then _G.Ox.Teleport.GateIndex = 1 end
    local v = Cache.Gates[_G.Ox.Teleport.GateIndex]
    local part = v and v:FindFirstChildWhichIsA("BasePart")
    if part then
        TeleportToPart(part)
        Library:Notify({Title = "TP Gate", Description = "Gate " .. _G.Ox.Teleport.GateIndex, Duration = 1.2})
    end
    _G.Ox.Teleport.GateIndex = _G.Ox.Teleport.GateIndex + 1
end)

MapCard:AddButton("Teleport ke Pallet (Loop)", function()
    if #Cache.Pallets == 0 then ScanMap() end
    if _G.Ox.Teleport.PalletIndex > #Cache.Pallets then _G.Ox.Teleport.PalletIndex = 1 end
    local v = Cache.Pallets[_G.Ox.Teleport.PalletIndex]
    local part = v and (v:FindFirstChild("PrimaryPartPallet") or v:FindFirstChildWhichIsA("BasePart"))
    if part then
        TeleportToPart(part)
        Library:Notify({Title = "TP Pallet", Description = "Pallet " .. _G.Ox.Teleport.PalletIndex, Duration = 1.2})
    end
    _G.Ox.Teleport.PalletIndex = _G.Ox.Teleport.PalletIndex + 1
end)

MapCard:AddButton("Teleport ke Window (Loop)", function()
    if #Cache.Windows == 0 then ScanMap() end
    if _G.Ox.Teleport.WindowIndex > #Cache.Windows then _G.Ox.Teleport.WindowIndex = 1 end
    local v = Cache.Windows[_G.Ox.Teleport.WindowIndex]
    local part = v and (v:FindFirstChild("Bottom") or v:FindFirstChildWhichIsA("BasePart"))
    if part then
        TeleportToPart(part)
        Library:Notify({Title = "TP Window", Description = "Window " .. _G.Ox.Teleport.WindowIndex, Duration = 1.2})
    end
    _G.Ox.Teleport.WindowIndex = _G.Ox.Teleport.WindowIndex + 1
end)

local PlayerCard = T_Page:CreateSection("Teleport Player")
local selectedPlayer = nil
local playerList = {}

local function UpdatePlayerList()
    playerList = {}
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= LP and plr.Character then
            table.insert(playerList, plr.Name)
        end
    end
    table.sort(playerList)
end
UpdatePlayerList()

local PlayerDropdown = PlayerCard:AddDropdown("Pilih Player", playerList, false, function(v)
    selectedPlayer = v
end)

PlayerCard:AddButton("Refresh Player List", function()
    UpdatePlayerList()
    Library:Notify({Title = "Refresh", Description = "List player di-refresh! (" .. #playerList .. " player)", Duration = 2})
end)

PlayerCard:AddButton("Teleport ke Player", function()
    if not selectedPlayer or selectedPlayer == "" then
        Library:Notify({Title = "Error", Description = "Pilih pemain terlebih dulu!", Duration = 3})
        return
    end
    local target = Players:FindFirstChild(selectedPlayer)
    if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
        local char = LP.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            char.HumanoidRootPart.CFrame = target.Character.HumanoidRootPart.CFrame + Vector3.new(0, 3, 0)
            Library:Notify({Title = "Teleport", Description = "Ke " .. selectedPlayer, Duration = 2})
        end
    else
        Library:Notify({Title = "Error", Description = "Target tidak valid!", Duration = 2})
    end
end)

-- ==================================================
-- SETTINGS
-- ==================================================
local SettingsTab = Window:CreateTab("Settings", false, false)
local S_Page1 = SettingsTab:CreatePage("Settings")
local AppearanceCard = S_Page1:CreateSection("UI Config")

AppearanceCard:AddKeybind("Menu Keybind", "RightShift", function(key)
    Window:SetMenuKey(key)
end)
AppearanceCard:AddToggle("Transparency Toggle", false, function(state)
    Window:SetTransparency(state and 0.2 or 0)
end)
AppearanceCard:AddToggle("Lock GUI", false, function(state)
    Window:SetLocked(state)
end)

local SavesCard = S_Page1:CreateSection("Config")
SavesCard:AddConfigManager("PanduHub/Configs")

-- ==================================================
-- INIT
-- ==================================================
task.spawn(function()
    task.wait(2)
    ScanMap()
end)

LP.CharacterAdded:Connect(function()
    _G.Ox.State.busy = false
    _G.Ox.State.RandomMode_IsNormal = false
    if _G.Ox.Auto.SkillCheck then
        task.wait(1)
        startSkillCheck()
    end
end)

LP.CharacterAdded:Connect(function()
    task.wait(1.5)
    if _G.Ox.NoSlowdownEnabled then
        local char = LP.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then
                hum.WalkSpeed = 16
            end
        end
    end
end)

-- ==================================================
-- SKILL HIDDEN NO CD - AUTO RE-APPLY
-- ==================================================
LP.CharacterAdded:Connect(function()
    task.wait(1.5)
    if _G.Ox.BypassLeapEnabled then
        StartLeapBypass()
    end
end)

-- ===== SURVIVOR AIMBOT - INPUT HANDLER =====
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.UserInputType == Enum.UserInputType.MouseButton2 then
        if _G.Ox.Config.Surv_Aimbot_Enabled and GetRole() == "Survivor" then
            _G.Ox.isAimbotHolding = true
            _G.Ox.lockedAimbotTarget = getBestAimbotTarget()
        end
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton2 then
        _G.Ox.isAimbotHolding = false
        _G.Ox.lockedAimbotTarget = nil
        if _G.Ox.survivorAimbotHighlight then _G.Ox.survivorAimbotHighlight.Parent = nil end
    end
end)

-- ===== SURVIVOR AIMBOT - MOBILE SUPPORT =====
task.spawn(function()
    while _G.Ox.ScriptAlive do
        task.wait(1)
        if not _G.Ox.ScriptAlive then break end
        
        if _G.Ox.Config.Surv_Aimbot_Enabled and GetRole() == "Survivor" and UserInputService.TouchEnabled then
            local playerGui = LP:FindFirstChild("PlayerGui")
            if playerGui then
                local survivorMob = playerGui:FindFirstChild("Survivor-mob")
                if survivorMob then
                    local controls = survivorMob:FindFirstChild("Controls")
                    if controls then
                        local attackBtn = controls:FindFirstChild("attack") or controls:FindFirstChild("Gui-mob")
                        if attackBtn and attackBtn.Visible then
                            if not _G.Ox.isAimbotHolding then
                                _G.Ox.isAimbotHolding = true
                                _G.Ox.lockedAimbotTarget = getBestAimbotTarget()
                            end
                        else
                            if _G.Ox.isAimbotHolding then
                                _G.Ox.isAimbotHolding = false
                                _G.Ox.lockedAimbotTarget = nil
                                if _G.Ox.survivorAimbotHighlight then _G.Ox.survivorAimbotHighlight.Parent = nil end
                            end
                        end
                    end
                end
            end
        else
            if _G.Ox.isAimbotHolding and UserInputService.TouchEnabled then
                _G.Ox.isAimbotHolding = false
                _G.Ox.lockedAimbotTarget = nil
                if _G.Ox.survivorAimbotHighlight then _G.Ox.survivorAimbotHighlight.Parent = nil end
            end
        end
    end
end)

-- ===== KILLER AIMBOT - INPUT HANDLER (PC) =====
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        if _G.Ox.Config.Killer_Aimbot_Enabled and GetRole() == "Killer" then
            _G.Ox.isKillerAimbotHolding = true
            _G.Ox.lockedKillerAimbotTarget = getBestKillerAimbotTarget()
        end
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        _G.Ox.isKillerAimbotHolding = false
        _G.Ox.lockedKillerAimbotTarget = nil
        if _G.Ox.killerAimbotHighlight then _G.Ox.killerAimbotHighlight.Parent = nil end
    end
end)

-- ===== KILLER AIMBOT - MOBILE SUPPORT =====
task.spawn(function()
    while _G.Ox.ScriptAlive do
        task.wait(1)
        if not _G.Ox.ScriptAlive then break end
        
        if _G.Ox.Config.Killer_Aimbot_Enabled and GetRole() == "Killer" and UserInputService.TouchEnabled then
            local playerGui = LP:FindFirstChild("PlayerGui")
            if playerGui then
                local killerUI = nil
                for _, gui in pairs(playerGui:GetChildren()) do
                    if string.sub(gui.Name, -4) == "-mob" and gui.Name ~= "Survivor-mob" then
                        killerUI = gui
                        break
                    end
                end
                
                if killerUI then
                    local controls = killerUI:FindFirstChild("Controls")
                    if controls then
                        local attackBtn = controls:FindFirstChild("attack")
                        if attackBtn and attackBtn.Visible then
                            if not _G.Ox.isKillerAimbotHolding then
                                _G.Ox.isKillerAimbotHolding = true
                                _G.Ox.lockedKillerAimbotTarget = getBestKillerAimbotTarget()
                            end
                        else
                            if _G.Ox.isKillerAimbotHolding then
                                _G.Ox.isKillerAimbotHolding = false
                                _G.Ox.lockedKillerAimbotTarget = nil
                                if _G.Ox.killerAimbotHighlight then _G.Ox.killerAimbotHighlight.Parent = nil end
                            end
                        end
                    end
                end
            end
        else
            if _G.Ox.isKillerAimbotHolding and UserInputService.TouchEnabled then
                _G.Ox.isKillerAimbotHolding = false
                _G.Ox.lockedKillerAimbotTarget = nil
                if _G.Ox.killerAimbotHighlight then _G.Ox.killerAimbotHighlight.Parent = nil end
            end
        end
    end
end)

LocalPlayer.CharacterAdded:Connect(function()
    task.wait(1.5)
    local Lighting = game:GetService("Lighting")
    if Lighting then
        ReapplyPerformance()
    end
end)

workspace.ChildAdded:Connect(function(child)
    if child.Name == "Map" then
        task.wait(1)
        local Lighting = game:GetService("Lighting")
        if Lighting then
            ReapplyPerformance()
        end
    end
end)

task.spawn(function()
    task.wait(2)
    local Lighting = game:GetService("Lighting")
    if Lighting then
        ReapplyPerformance()
    end
end)

Library:Notify({ Title = "Oxio Hub", Description = "Loaded + Silent Veil", Duration = 3 })